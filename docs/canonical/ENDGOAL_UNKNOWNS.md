# End-Goal Unknowns

This file answers a narrow question:

What is still unknown between the current state of verified truth and the actual end goal?

## End Goal

The end goal is not just "understand Lumen."

The end goal is a clean-room replacement that can:

- reproduce correct camera participation
- reproduce correct framing and crop behavior
- merge correctly enough to avoid ghosting, trailers, and contributor misregistration
- hold that quality bar at `28mm`, `35mm`, `70mm`, and `150mm`

## Remaining Canonical Blockers

At `TRUTH 3.0.315`, no claim-ledger blocker remains for canonical profile-3
processing of structurally complete local LRIs. This remains an active
investigation: implementation audits or new corpus evidence may demonstrate a
new pixel-changing gap, which must be opened rather than hidden by the current
zero count.

TRUTH `3.0.305` classifies payload slot 15 as conditional
linear-ProPhoto/D50 materialization and proves the matching-config copy path.
TRUTH `3.0.306` closes tested profile-3 incidence as exact copy across the
Unit-1 quartet plus targeted Unit-2 wide/tele controls. Generic unequal
selector formulas remain outside the admitted route.

TRUTH `3.0.307` also closes the remaining Pile-2 provenance/schema
bookkeeping: three full Unit-1 shared calibration-payload digests, all 28
exact installed VST rows, the selected leaf wire contract, and explicit
`packed=true` options for four repeated-float calibration fields.

TRUTH `3.0.308` establishes that initial Lumen editor construction uses the
same calibrated profile-3 IRAMP topology at canonical `28/35/70/150mm`.
One Unit-1 `28mm` brush edit reuses prepared state without fresh heavy merge
work. Later byte joins prove editor/export pixels differ exactly across the
seven-stage display pipeline; exact edit formulas remain optional
editor-compatibility scope, not unknowns in the base LRI-to-image route.

TRUTH `3.0.309` joins two standalone physical-body calibration packages to
all five calibration payloads embedded in 81 new complete photographs. The
common SensorData payload is invariant across all 9,323 complete old/new
photographs, and the expanded corpus has no third firing topology. Standalone
zoom and HotPixelMap files are decoded but are not admitted as additional
selected profile-3 photograph inputs.

TRUTH `3.0.311` closes editor output-pyramid geometry at tested Unit-1
`28mm`, the immediate default level-4 route through exact `lt::PipelineCache`
and the per-level Color pipeline, correct type-13 queue-record custody, exact
float-to-byte packing, and installed conditional `GL_BGRA` / `GL_RGBA` GUI
upload policy. Byte-exact before/after joins isolate the editor/export
difference to seven named callbacks. Remaining display-specific callback
formulas and alternate DOF/mode behavior remain optional editor-compatibility
work, not base LRI-to-image blockers.

TRUTH `3.0.312` formula-closes the selected ACRE exposure/toe/LUT/hue core by
byte-exact replay of one complete `256x256` worker tile and proves the live
lens/contrast callbacks are exact no-ops on the tested request. Remaining
editor-specific unknowns at that checkpoint were the selected color converter,
display index-10 color correction, EV/LUT public origins, and alternate
DOF/mode routes.

TRUTH `3.0.313` closes that selected color converter with runtime target and
branch custody plus a second byte-exact full-tile clean-room replay. Remaining
editor-specific unknowns at that checkpoint were display index-10 color
correction, EV/LUT public origins, and alternate DOF/mode routes.

TRUTH `3.0.314` closes the selected EV/LUT origins: public capture-normalization
fields generate `tone_mapping.ev_offset` through the exact admitted ratio and
`log2f`, while public property `tone_mapping.type=light_v1` selects enum `4`,
ACRE curve index `1`, and exact LUT `libcp+0x5e41b4`. TRUTH `3.0.315` closes
selected display index-10 color correction from body-specific public
`macbeth_data` through an exact clean-room optimizer/endpoint join and
zero-byte-difference full-image replay. Remaining editor-specific unknowns are
alternate DOF/mode and other untested control/focal/level routes.

TRUTH `3.0.316` closes public mode-1 DOF activation from exact
`Settings.DOF.f_num/focus_depth`, the exact local cache-selection predicate,
and one final packed-buffer effect at Unit-1 `28mm`. Remaining editor-specific
unknowns include the internal `0x3f0b90` blur/depth-compositing formula, modes
`2/3/4`, other bodies/focals, and other untested controls/edit semantics.

TRUTH `3.0.317` narrows that formula boundary: installed optical constants,
public `sensor_data_surface.data_scale` pitch ancestry, exact near/far focus
range, and exact SSE tile-radius bucketing are closed. Remaining internal DOF
work is narrowed again by TRUTH `3.0.318`, which closes both vec4/scalar
`ImageCircleFilter` specializations as uniform integer-disk averages with
clamped-edge replication. TRUTH `3.0.321` then closes the installed mode-1
tiled layer/occlusion compositor through exact layer construction, neighbor
opacity, blur dispatch, cubic B-spline resampling, and reverse premultiplied
source-over. Modes `2/3/4`, nonempty secondary-layer and distinct-rectangle
runtime, other bodies/focals, and runtime generalization beyond the tested
Unit-1 `28mm` treatment remain.

`CLM-COMPAT-002` left this list after all three complete focal/topology
exceptions were joined to their exact primary stereo, MonoFusion/C6, five-warp,
direct-contributor, IRAMP-scale, public crop/orientation, and completed-output
routes. Public reference camera/firing topology, never focal alone, selects
wide versus tele processing.

`CLM-STATE-001` left this list after a 13-verifier audit closed every
demonstrated image-consequential State/object path and excluded anonymous
padding, numeric labels, and unconsumed fields from implementation scope.

`CLM-VALIDATION-001` left this list at TRUTH `3.0.282`: ten complete index-5
samples per focal, exact full-map lookup coupling, deterministic
undistorted-plane references, and focal-specific final-output envelopes now
define the layered clean-room validation oracle.

`CLM-DENOISE-002` left this list at TRUTH `3.0.274`: selected radius-2 and
radius-4 worker formulas, callback roles, zero-filled boundaries, weight
floor, and reciprocal normalization are closed by installed proof and
two-body runtime replay, joined to prior Unit-1 four-focal liveness/store
coverage.

The selected index-5 Skip-mask predicate is closed at TRUTH `3.0.275` by
installed proof, direct Unit-1 `28mm` pattern/task receipts, and byte-identical
Unit-1 four-focal full-mask replay. Untested pattern arms remain scoped rather
than blockers for the selected canonical route.

Selected consumption is closed at TRUTH `3.0.331`: mask zero computes the
normalized G-42 local term, mask nonzero supplies zero unary cost, and both
arms receive ordinary eight-path SGM Cost-volume and first-argmin outputs.
The later guided upsample is downstream of the complete index-5 Depth map.

The disparity-direction convention is closed at TRUTH `3.0.276`: increasing
hypothesis lanes move from farther/lower inverse depth toward nearer/higher
inverse depth, with exact `d-1/d/d+1` SGM neighbor assignment.

`CLM-LRI-001` is closed: installed schemas, runtime consumers, and a census
of all `9,242` structurally complete local LRIs account for every valid
record and image-producing block role. The long sections below are retained
as closure history and scope guards. Within those narratives, earlier "open"
language records the boundary at that evidence stage and is superseded by
later admissions and the claim ledger.

The closed sections below are chronological closure history. Their local
"What is unknown" and "Why it blocks" text describes the state at that point
in the campaign and must not be read as the current blocker set.

### Closed: Exact pre-fusion merge/reduction mechanism behind `src1` / `src2`

- Claim IDs: `CLM-PREFUSION-001`, `CLM-PREFUSION-002`
- Naming note:
  `N-to-1 reducer` is a search shorthand. It must not be treated as proof that Lumen uses one tidy reducer body; the real mechanism may be distributed across candidate scoring, selection, warp generation, accumulation, and acceptance.
- What is known:
  visible `src1` is exact tier-anchor `ReferenceImageCache`; visible `src2` is exact `PipelineCache::processLevel1` one-generated-descriptor resampling/materialization. The descriptor is tier-dependent: wide targets A1 and contains A2 `lt::MonoFusion` ancestry, while tele is direct B4.
  The canonical four-zoom bridge HDR quartet all hit the visible `src1`, `src2`, contributor-wrapper, and accumulator surfaces at `0x3ecc10`, `0x3ecd80`, `0x3eced0`, and `0x369fa1`.
  Four-zoom runtime proof now identifies `PipelineCache+0x8` as a five-entry packed `(int32 width, int32 height)` level-vector header, not an image/composite pointer; `28mm` / `35mm` entry `0` is `10432x7824`, `70mm` / `150mm` entry `0` is `8896x6672`, and all four runs use entry `1 = 4160x3120` for both visible `src1` / `src2` wrapper dimensions.
  Installed-bundle and runtime proof now bounds the first visible wrapper bodies, the visible `src1` lookup key, the visible `src1` payload constructor path and level vector, the visible `src1` versus direct-contributor payload-family split, the visible `src1` versus direct-contributor payload-internal secondary-callable split, the visible `src1` secondary-callable live handoff into `0x3e2e90` for the first captured tile, the first captured `src1` worker/projection-record handoff through `0x3e4c50` and payload-internal callable slot `0x3e42e0`, the `0x3e42e0` coordinate-transform formula, the `0x3e42e0` field-pack producer through `0x3f6170` / `0x3f6200` / `0x3f6940` / `0x145580` / `0x144f50` / `0x144a70`, the four-zoom runtime fact that this dispatcher boundary sees keys `0,5..9` at `28mm` / `35mm` and `8,10..14` at `70mm` / `150mm` with no tele key `15`, the constructor/watchpoint fact that tele key `15` is constructed active at item `+0x30 = 1` and later cleared at `libcp+0x3c90a5`, the direct payload candidate-loop fact that tele key `15` reaches the loop but has post-mutation `object+0x30 = 0` and skips before class compare / dispatcher call, the stereo-side keyed-record loop fact that tele key `15` reaches the `0x3f2c40` loop but has post-mutation `object+0x30 = 0` and skips before both tested `0xf2720` getter callsites, the visible `src1` worker source-image producer topology through `0x1bdc80` / `0x1be750` / `0x31af30` / `0x31acf0` / `0x33ede0` / `0x33f480` / `0x33f180`, the keyed-helper/vector-builder boundary fact that `0x1bdc80` is live while `0x1be750` and `0x1be270` are zero-hit under complete canonical bridge HDR runs, the first captured visible-`src1` source-producer virtual target `0x65b3c8/+0x30 = 0x341770` as per-source region-adapter / record-update work, follow-up gated lower-virtual census facts for `0x33f3e8` / `0x33f94f` across all four zooms and `0x33ffd4` at `28mm` / `35mm` only under the tested gated runs, the capped target-family visible-body static classification, the bound indirect target chains `0x342d99 -> 0x342b80 -> 0x2eb560` and `0x3449f0 -> 0x345920 -> 0x2f53d0`, the `0x341770` helper `0x2e8680` / callback `0x2e8cc0` as one-source Bayer/RAW region-helper work, the visible `src2` hot state and static `0x3ebb80` descriptor/state/executor boundary, the four-zoom visible-`src2` executor gate/dispatch target `0x65f7e8/+0x30 = 0x3ed2e0` as one-source descriptor resampling/materialization work, the visible-`src2` callback `+0x08` descriptor producer through `PipelineCache+0x1d8` slot `+0x18 = 0x406a10`, the post-wrapper `initResAmp` per-key record/wrapper branch, the visible `PipelineCache+0x270` per-key wrapper read path, visible `src1` read path, visible `src1` payload provenance, first visible payload runtime/config/process surfaces, the deeper `0x3e2e90` / `0x3e4c50` single-source projection/resampling worker, owner cache-selection/read-rescale layer, owner `+0xf0` callable-install custody edge at `0x3ea980 -> 0x3d0120` with installed address point `0x65f5e0/+0x30 = 0x3ec960`, owner alternate-cache/helper setup surfaces, visible `+0x678/+0x680` constructor / immediate runtime / selected layer-virtual / record-consumer surfaces, the `StereoLayer<false>::runPass` projection/sampling cost path reached by the canonical four-zoom bridge HDR quartet, the sibling `StereoLayer<false>::compute` projection/sampling cost surface excluded under complete no-auto-LRIS quartet probes, the high-address `HigherWarpDebug::renderDebugView` helper-caller surface excluded under complete no-auto-LRIS quartet probes, the four-zoom-sampled prefusion callable gate, complete-render runtime liveness for all thirteen `CalibDataProcessor::State ()` `operator()` bodies, post-`State()` helper chain, first heavy consumers, dispatcher / selector layers, callback reuse path, candidate block-geometry helper family, visible feature-selection lane, four-zoom candidate-scoring family, and four-zoom candidate-scorer output-vector custody into `0x2439b0` away from merge/reducer closure. SHA-pinned RTTI now additionally names owner `+0x6a8` as `lt::ImageCaches`, visible `src1` as A1-wide/B4-tele `lt::ReferenceImageCache`, and direct contributors as B1..B5/C1..C5 `lt::SourceImageCache`.
  Follow-up four-zoom record-state histogram proof now bounds `0x2439b0` as a live gate over those custody-bound scorer-output vectors: wide family-A boundary histograms are unchanged, tele family-B boundary histograms promote target-2 records from state `3` to state `4`, and sampled downstream `0x241fd0` / `0x2416d0` / watched-store sites did not match the exact known scorer-output vector under this probe. Public record-state semantics, downstream image effect, final acceptance, and reducer closure remain open.
  Follow-up tele promoted-record data-watch proof shows selected target-2 records promoted by `0x2439b0` are consumed later under clean canonical `70mm` / `150mm` renders, and at least one watched promoted record per tele seed advances from state `4` to state `5` through `0x2416d0`. This proves a downstream consumer path for watched promoted records, but public state `5` semantics, selected-index sinks, downstream image effect, final acceptance, and reducer closure remain open.
  Follow-up tele selected-index proof shows promoted target-2 indices captured at `0x2439b0` enter concrete `0x2416d0` selected-index vectors under clean canonical `70mm` / `150mm` renders, and the small promoted sets captured by that probe reach `(state=5,target=2)` stores. This proves selected-index/state-relabel flow for those admitted promoted sets, but public state `5` semantics, downstream state-5 consumers, image effect, final acceptance, and reducer closure remain open.
  Follow-up tele later-watch proof shows watched promoted records that become `(state=5,target=2)` continue into `0x244560` and the already-bounded `0x25d090` candidate block-geometry / active-block helper family under clean canonical `70mm` / `150mm` renders. Follow-up four-zoom runtime proof bounds the admitted `0x25d090` effect as block-owned pair-vector growth plus descriptor-build / geometry-predicate / active-byte gating. Follow-up caller-decision proof then bounds the `0x244560` / `0x245a40` decisions after paired `0x25d090` calls as continuing with exactly one active block into `0x2457c0` callsites, with zero watched sentinel-fill aborts. This proves later state/candidate/geometry flow and downstream block-decision / coordinate-output custody, but downstream image/source contribution after that bounded path, final acceptance, and reducer closure remain open.
  Follow-up four-zoom state-5 coordinate-output proof shows `0x2457c0` is live and normally returning under clean canonical `28mm`, `35mm`, `70mm`, and `150mm` bridge HDR renders; sampled hits at the admitted `0x24593b` store-path site have `record+0x24 == 5`; and every admitted return leaves finite non-sentinel coordinate pairs in `state+0x1e8`. This proves coordinate-output materialization, but the downstream `state+0x1e8` consumer/effect, image effect, final acceptance, and reducer closure remain open.
  Follow-up four-zoom state-5 coordinate-consumer proof shows representative finite non-sentinel coordinate pairs emitted by `0x2457c0` into `state+0x1e8` are later read by `0xe8e70` vector-copy work under both State-helper copy-out paths (`0x224d70 -> 0x245a40` and `0x224e50 -> 0x245a20 -> 0x244560`). This proves coordinate-vector custody / copy-out, but image-effecting non-copy use of the copied destination, final acceptance, and reducer closure remain open.
  Follow-up four-zoom coordinate-copy-destination proof shows representative finite non-sentinel destination pairs copied out by the State-helper `0xe8e70` path are touched again by `0xe8e70` vector-copy work. Runtime stacks and static caller windows bind the admitted later caller frames to State-helper recopy sites plus higher node-vector materialization/copy sites at `0x22a61a -> 0xe8e70 -> 0x22a61f` and `0x22c93a -> 0xe8e70 -> 0x22c93f`. This proves coordinate-vector custody / propagation, but image-effecting non-copy use of the propagated destination vectors, final acceptance, and reducer closure remain open.
  Follow-up four-zoom coordinate-node-destination proof shows representative finite non-sentinel destination pairs copied into the `0x22a61a -> 0xe8e70 -> 0x22a61f` node-vector destination reach non-copy candidate/index/scoring-selection code under `0x21b2e0` and its `0x21c4f0` callback path. The admitted capped window followed at least one finite node-destination pair per run, while the sibling node-vector copy site `0x22c93a -> 0x22c93f` had zero observed call/return hits in this proof. This proves representative non-copy candidate/index/scoring-selection consumption, but image effect, final acceptance, and reducer closure remain open.
  Follow-up four-zoom same-address custody proof shows one finite non-sentinel coordinate pair copied into the `0x22a61a -> 0xe8e70 -> 0x22a61f` node-vector destination per focal tier is later the same runtime address rewritten through `0x21b923` / `0x21b92a` into full `(-1.0, -1.0)` at `0x21b930`, then sampled in downstream touches while still sentinel. This links the representative node-destination consumer, sentinel-write, and downstream-touch boundaries, but all-pairs coverage, image/source contribution, final acceptance, and reducer closure remain open.
  Follow-up four-zoom same-address branch-custody proof shows one representative copied node-destination pair per focal tier reaches `0x20b912` at that same runtime address after sentinelization, still reads `(-1.0, -1.0)`, steps through `0x20b91d -> 0x20ba90` and `0x20baab -> 0x20bafd`, and does not visit the local update-write block at `0x20bac0..0x20bac8`. This links copied node-destination identity to one sampled local `0x20b5e0` skip path, but all-pairs coverage, image/source contribution, final acceptance, and reducer closure remain open.
  Follow-up four-zoom same-address `0x20ca00` source-copy proof shows one representative copied node-destination pair per focal tier is source-read at that same runtime address by `0xe0ae0` under caller return `0x20d309`, the second local vector copy inside `0x20ca00`, while still full `(-1.0, -1.0)`. This links copied node-destination identity to the `0x20ca00` source-copy surface, but destination-slot identity, local gate selection, image/source contribution, final acceptance, and reducer closure remain open.
  Follow-up four-zoom same-address `0x20ca00` source/gate index proof shows that, for one representative copied node-destination pair per focal tier, every captured `0x20d309` source/gate packet before the watchpoint cap has readable `source_index` and parent `gate_index` with `source_index != gate_index`. This proves capped local non-selection for one watched address per tier, but destination-slot terminality, image/source contribution, final acceptance, and reducer closure remain open.
  Follow-up selected-representative same-address `0x20ca00` gate-custody proof joins copied node-destination identity through sentinelization, the source copy, computed destination slot, and the local skip branch for one `28mm` pair (`source_index == gate_index == 5394`) and one `70mm` pair (`source_index == gate_index == 77`). Both copied destinations remain full sentinel at `0x20d363` and step to `0x20d565`. The selected `35mm` observation is cap-limited and the selected `150mm` observation is one-address no-source-copy evidence. This narrows representative local gate behavior, but all-pairs terminality, image/source contribution, final acceptance, and reducer closure remain open.
  Follow-up selected cross-unit validation observes the same full-sentinel local skip on Unit-2 in one complete `35mm` twin-capture run at index `12`, while Unit-2 `28mm` / `70mm` anchors and a targeted `35mm` repeat are cap-limited no-match windows. This narrows concern that the local mechanism is Unit-1-specific, but stable incidence, body causation, all-pairs terminality, image/source contribution, final acceptance, and reducer closure remain open.
  Follow-up byte-verified local-effect proof shows those admitted selected sentinel branches bypass the local keyed-node/coordinate-write path and `ceres::Problem::AddResidualBlock` call. This closes direct residual contribution through that one local iteration path only; shared-solve terminality for skipped pairs, broader solved-value distributions, other consumers, all-pairs terminality, image/source contribution, final acceptance, and reducer closure remain open.
  Follow-up typeinfo/vtable proof identifies `0x20ca00` exactly as the `void(int,int,int)` lambda callback inside `lt::Triangulator::refine3dPoints()`, not the method entry. This identity proof by itself does not close callback argument names, public output meaning, runtime values, image/source contribution, final acceptance, or reducer closure.
  Follow-up byte-verified solved-record custody proof binds the callback's post-solve triple to selected fields `+0x08/+0x0c/+0x10` of the captured owner's `0x14`-stride record vector and proves the parent immediately reduces positive `record+0x10` values into owner `+0x78/+0x7c`. Internal output ownership and that immediate scalar consumer are closed; by itself this does not prove public triple meaning, runtime values, later range consumers, image/source contribution, final acceptance, or reducer closure.
  Follow-up typeinfo/import/formula proof identifies the cost wrapper as `AutoDiffCostFunction<lt::Internal::ReProjectionCost,2,1,...>`, traces a unit-payload captured `CauchyLoss` into `AddResidualBlock`, and verifies a one-scalar ray-scale 3x4 reprojection objective with two coordinate residuals. This names the local residual excluded by admitted sentinel skips, but by itself does not prove public units/LRI origin, runtime solved values, all-pairs terminality, image/source contribution, final acceptance, or reducer closure.
  Follow-up solve-only runtime proof for one complete Unit-1 `28mm` render captures 1,229 unique solve/write groups across ten returned callback frames: the bounded ray-depth scalar changes in 279 groups, remains bit-identical in 950, and every final selected `record+0x10` equals the float32 solved scalar. This closes one body/focal/runtime solved-value and record-materialization boundary; stable distributions across bodies/focals/renders, public calibration/LRI origins and names, all-candidate behavior, shared-solve terminality, image/source contribution, final acceptance, and reducer closure remain open.
  Follow-up static + reused-runtime proof pins helper `0x218940` and shows one watched Unit-1 `70mm` finite positive `record+0x10` value reaches `0x2189c4` 37 times with unchanged bits and falls through into the local record/transform score body by static branch semantics. This closes representative local score-window admission only; direct branch-step proof, all-record behavior, public depth semantics, image/source contribution, final acceptance, and reducer closure remain open.
  Follow-up caller proof binds those same helper samples to `lt::SparseMirrorAngleOptimizer::optimize(...)::$_2` caller `0x219210` and its immediate `[r14+0x18][r15]` output-vector store path. This closes representative caller output-vector custody only; stored values, later consumer identity, image/source contribution, final acceptance, and reducer closure remain open.
  Follow-up parent-consumer proof now runtime-binds that callback output-vector family: one complete Unit-1 `70mm` packet carries the exact same closure, 1,089-float return-vector header, and begin pointer through 64 sampled post-store callback hits and the matched `0x216f60` parent consumer at `0x217a68`; the parent min-like scan selects index `505` before helper side-output gates and selected-record materialization for `0xf33d0`. This closes one-render same-runtime callback-store to parent-consumer vector custody only; record-specific score proof, public acceptance/rejection semantics, image/source contribution, final acceptance, and reducer closure remain open.
  Follow-up canonical four-focal plus exact-focal Unit-2 `35mm` proof now closes the captured local `0x216f60` parent decision: minimum callback score, selected-side `<= 0.25`, selected-side `<= center_side`, optional float32 `selected_score <= 0.8 * center_score`, then selected 24-byte record materialization and completed `0xf33d0` only on acceptance. All 26 captured packets agree between reconstructed arithmetic and runtime branch flags. Public vector/record names, downstream image/source contribution after `0xf33d0`, final merge acceptance, and distributed reducer closure remain open.
  Follow-up accepted-bank watch proof now carries the first accepted winner per run through exact selector-1 destination-bank copy and later unchanged `0x264270` direct-bank/accessor reads across canonical Unit-1 four-focal plus exact-focal Unit-2 `35mm`. A later State-helper selector-1 call replaces the same bank, and selected `0x3f7ec0` materialization sites are zero-hit under the tested no-auto-LRIS runs. Public record names, final image/source contribution, final merge acceptance, and distributed reducer closure remain open.
  Follow-up four-zoom two-phase watch proof carries the exact `0x264270` assembly output into `0x23faf0`, where it is copied into the composer's destination, then carries that destination into its first later use. Wide tiers first load composer fields into `0x239e00` score-input state at `0x23a179`; tele tiers first use the composer destination as scalar input to `0x20dbe0` three-row SIMD matrix composition at `0x20dbef`. Public composer-record semantics, downstream image/source contribution, final merge acceptance, and distributed reducer closure remain open.
  Follow-up four-zoom materialization proof shows wide stores, reads, and compares a bit-exact keyed mean reprojection-residual scalar. Corrected branch proof shows a local minimum selector: the admitted lower candidate is materialized, while the admitted higher candidate retains the existing `state+0x448` record and copies it into the per-camera `state+0xe0` selector-1 CalibStage bank. Exact selected-record custody now continues through terminal composition, keyed BA camera-map normalization, byte-exact same-key selector-1 CalibStage write-back, and one exact second-pass terminal-helper read; exact-focal Unit-1 and Unit-2 `35mm` controls share that two-pass keyed topology. Tele stores an exact composed `3x4` result whose tracked prefix has no intervening touch before cleanup. Complete public record/transform names, post-helper image/source effect, tele alias/alternate-route proof, final acceptance, and reducer closure remain open.
  The visible post-terminal path is now joined to its actual downstream geometry effect: the final processing-state lambda replaces an internal `State+0x2a8` calibration sibling, while exact-focal Unit-1 and Unit-2 `35mm` production runs skip the optional JPEG/overlay diagnostic body and then signal completion. The replacement constructor does not publish a separate alias and the replacement receives no later exact-slot touch before destruction. Sequential canonical four-focal proof shows the terminal whole-State root, not that sibling, is retained at `PipelineCache+0x180` and passed five times through `0x3f7040` to build the five `PipelineCache+0x258` warp-field records from `state+0xe0/+0x448`. The sibling-feed question and whole-State warp-record consequence are closed; whole-State public naming, reducer closure, and final acceptance remain open.
  Follow-up tele same-address scan/score identity proof shows one representative copied node-destination pair at `70mm` and one at `150mm` are later sentinelized at the same runtime address and then sampled at that same address inside the `0x216f60` scan/count window and at the `0x218bc4` score/materialization guard operand while still full `(-1.0, -1.0)`. This links tele node-destination identity to bounded local scan/count and score-guard surfaces, but same-address branch-step, all-pairs coverage, image/source contribution, final acceptance, and reducer closure remain open.
  Fresh same-address tele branch-custody proof advances those two representative pair identities through `0x218bc4 -> 0x218cb8`. Each full-sentinel pair takes the x-nonpositive skip, which deterministic static proof shows bypasses pair-y loading, transform/score formation, local score-sum update, over-threshold-count update, and positive-pair-count update. This closes representative local score/count exclusion, but all-pairs/alias/alternate-route terminality, shared-solve terminality, image/source contribution, final acceptance, and reducer closure remain open.
  Canonical wide `0x218bc4` divergence is closed separately: complete `28mm` / `35mm` runs select `SparseMirrorAngleOptimizer::CostFunction == 1` and sibling `optimize::$_2 -> 0x218940`, while the guard belongs only to zero-hit CostFunction-`0` family `optimize::$_1 -> 0x218b30`. This explains the wide zero-hit result but does not prove global wide-sentinel terminality outside that optimizer family.
  Follow-up four-zoom node sentinel-write proof shows that the downstream `0x21b2e0` path executes coordinate-pair sentinel invalidation writes at `0x21b923` and `0x21b92a`. Runtime samples show finite non-sentinel coordinate pairs before the x-lane store and x already changed to `-1.0` before the y-lane store; static disassembly proves both stores write raw bits `0xbf800000` (`-1.0`). This proves coordinate invalidation/rejection writes, but downstream candidate/source/image consequences, final acceptance, and reducer closure remain open.
  Follow-up four-zoom node sentinel-downstream watch proof shows that selected sentinel-marked node-vector coordinate pairs are touched later by downstream code, while every sampled later touch still observes `(-1.0, -1.0)`. Sampled downstream surfaces include State-family copy/record propagation plus coordinate scan/scoring/materialization windows. This proves downstream sentinel-coordinate custody / consumption, but final image/source-contribution consequences, final acceptance, and reducer closure remain open.
  Follow-up State `0x22ae60` copy/record-surface proof now narrows the sampled State-family copy/record propagation bucket: `0x20bd60` / `"point BA"` is keyed record materialization, `0x25e4b0` is the no-map `0x25e0c0` row-producer variant, `0x20dca0` is keyed record storage, `0x20ca00` is selected Ceres setup with positive-coordinate gates, and `0x239ac0` / `0x239e00` are keyed pair-vector propagation surfaces. Follow-up copied-slot gate proof shows one 70mm watched sentinel copied by the second `0x20ca00` local vector copy has `source_index == gate_index == 774`, is read at `0x20d363` as `(-1.0, -1.0)`, and skips to `0x20d565`; the admitted 28mm/35mm/150mm packets are capped no-match windows for watched sentinel pairs. This is surface classification plus one exact copied-slot gate-skip proof; sentinel terminality, image/source-contribution consequences, final acceptance, and reducer closure remain open.
  Follow-up State-machine return proof now bounds the runtime ordering at dispatcher sites `0x22f3f6` / `0x22f3ff` under accepted no-auto-LRIS canonical bridge HDR runs: all four focal tiers record `38` paired calls, clean `10432x7824` HDR output, and an identical ordered `(operator, pre-state, returned State)` sequence. This removes tested dispatcher return ordering as an unknown, but public State meanings, image/source-contribution consequences, final acceptance, and reducer closure remain open.
  Follow-up exact-body static proof bounds the thirteen State operator shells as direct-call helper surfaces with zero indirect calls and zero direct calls to the listed known IRAMP/wrapper/owner-route VAs. This removes the exact State shells as direct known merge/wrapper entries, but helper transitive behavior, image/source-contribution consequences, final acceptance, and reducer closure remain open.
  Follow-up helper static/runtime proof now bounds State helper `0x23c5f0` and selector-gated `0xf33d0`: `0xf33d0` has selector `0` / selector `1` field-copy paths into two destination offset banks, complete no-auto-LRIS four-zoom runs hit `0x23c5f0` exactly four times per render from State bodies `0x22af80` and `0x22e1d0`, and the static `0x23c5f0 -> 0xf33d0` callsite at `0x23d38d` / return `0x23d392` is live with selector `1` across all four focal tiers. Complete installed-writer census plus exact-focal two-body watches map selector `0` to `factory` at `CapturedImage+0x180` and selector `1` to `current` at `+0x12c`. Complete bank-field and public State meanings, image/source-contribution consequences, final acceptance, and reducer closure remain open.
  Follow-up exit-snapshot proof now bounds `0x23c5f0` post-`0xf33d0` local integer coverage and normal-exit local tree shape across the same no-auto-LRIS four-zoom scope. This proves entry/exit pairing, post-copy local `rbp-0x4e0` integer coverage, and pre-destroy local tree field sets; public field semantics, helper transitive behavior, image/source-contribution consequences, final acceptance, and reducer closure remain open.
  Follow-up `0xf34e0` match proof now shows those copied destination objects are reused by the internal `0x23c5f0 -> 0x264440 -> 0x264270 -> 0xf34e0` helper path before `0x23c5f0` exits. This proves transitive helper custody of copied records through selector-`1` `object+0x12c` bank selection; post-helper image/source-contribution consequences, final acceptance, and reducer closure remain open.
  Follow-up `0x23faf0` record-chain proof now shows the next internal `0x23c5f0` handoff across the canonical four-zoom no-auto-LRIS runs: the `rbp-0x378` output record changes across `0x23cbbc -> 0x23faf0`, remains stable through later node writes, mapped output fields materialize into local tree-node `+0x28..+0x58`, node `i32_0x20` equals the sampled local key, and node `+0xa0` is first forced to `0` before receiving observed final `{0,9,11}` values. This proves helper-record-to-local-tree custody; public node-field semantics, post-node image/source-contribution consequences, final acceptance, and reducer closure remain open.
  Follow-up static/runtime `0x216f60` scan-count proof shows that sampled tele sentinel-coordinate stops inside that scan/count window still read `(-1.0, -1.0)`, while the local vector/scalar count paths count only coordinate pairs where both lanes are positive and require at least eight counted entries before continuing. This proves local non-counting behavior for sampled tele sentinel reads, but downstream image/source-contribution consequences, final acceptance, and reducer closure remain open.
  Follow-up visible-`src2` proof closes the tested descriptor ancestry, and the next two-body/static campaign decodes production-profile MonoFusion mode `0`: public A1/A2 exposure/analog source normalization, installed type-3 VST constants, initializer scaling, normalized 5/3 lifting with exact boundary/packing schedules, exact fixed coefficient weights, patch-noise/Wiener math, half-Hann overlap-add, final target/source scalar blend, and the secondary-map callback formula. Canonical wide uses this A1/A2 worker; canonical tele constructs no MonoFusion and uses direct B4. Corrective constructor proof refutes the different public type-2 LRI VST rows as coefficient custody, and the later profile matrix formally excludes mode `1` from canonical profile `3`.
- What is unknown:
  the exact distributed merge/reduction mechanism body or bodies, unobserved MonoFusion mode `1`, complete inputs/outputs and decision boundaries, helper-family transitive behavior beyond the now-bounded `0x23c5f0` / `0xf33d0` selector-copy edge, `0x23c5f0` local-tree custody, internal `0xf34e0` helper custody, and `0x23faf0` record-to-local-tree custody, public State meanings, complete `CalibStage` bank-field meanings beyond the now-proven `0=factory` / `1=current` mapping, public node-field meanings, upstream collection / remaining item-input LRI origins beyond the admitted raw `f2770` subset, and downstream source-contribution consequence.
- Why it blocks the end goal:
  parity cannot be frozen while the exact upstream behavior behind `src1` / `src2` is still unresolved.

### Closed: Exact producer-side transform record semantics over the ROI-derived pair lattice

- Related claims: `CLM-WARP-001`, `CLM-WARP-002`, `CLM-WARP-003`
- What is known:
  the storage format is packed int32 `(x, y)` pairs on an 8-pixel lattice, `0xf540` is not the pair writer, the first lattice is ROI-derived, a second same-sized transformed lattice is produced before bbox / clipping handling, the consumer formula is proven, the producer-side dispatcher / row-map split / final post-wrapper scale-field normalization are proven, the row producer is bounded to the 4x4 double matrix formula `source_b_product * inverse(source_a_product)`, the immediate source-record constructor / map-provider topology is bounded, `0x23faf0` is bounded as source-record composition, `0x264980` as a two-axis field-shift helper, `0x264460` as a positive two-axis scale helper, `state+0xe0` object resolution is bounded through `0x1be970` / `0xe6ba0`, `0x264270` helper access is bounded through `0xf34e0` CalibStage banks, `0xf3350`, and `0xf3360`, `state+0x448` is bounded as a keyed tree/control object with a first visible byte-gated insertion loop, first payload-field copies, and later direct payload writes through `+0x80`, and the tracked map-provider path is runtime-bound across the canonical quartet to `0x268480 -> UpsampleLayer 0x658eb0/+0x90 = 0x26b590 -> UpsampleLayer+0x90 -> record+0x40`. Accepted `28mm`, `35mm`, and `70mm` writer-core probes prove `0x26ac13 -> 0xf340` copies a populated `4160 x 3120`, stride-`4160` descriptor into `UpsampleLayer+0x90`; accepted `150mm` runtime proves the same provider/storage descriptor boundary without writer-body instrumentation. Four-zoom builder probes prove `0x26aa10 -> 0x29ed90 -> 0x2673a0 -> 0x26ac13` builds that descriptor from a previous-layer `+0x90` `2080 x 1560` descriptor into a `4160 x 3120` output, and installed debug strings label `UpsampleLayer+0x90` as `depth_... .dp`. Four-zoom worker/custody probes prove the `0x29ed90` worker formula and bind its low-resolution float source descriptor to `StereoLayer<false>` index `5`, descriptor `+0x2a8`, returned by vtable slot `+0x90 = 0x26fb50`.
  Follow-up no-auto-LRIS four-zoom probes prove sampled `0x267010` index-image-to-float-table expansion, the immediate `0x26e4c6 -> 0x299c70` source-index producer/custody path from `StereoLayer<false>+0xf8`, the sampled internal `0x299c70` callback worker formula through generic executor `0x5440` slot `+0x30 = 0x29a670`, the internal construction/custody watchpoint path for the tracked index-5 `+0xe0` lookup-vector header and `+0xf8` source object, and the immediate `+0xf8` field assembly path through `0x29a140`, `0x28f420`, and `0xf340`.
  Follow-up lookup-vector public-origin verification proves the tracked index-5 `+0xe0` table is internally generated through `0x28fa60` / `0x28f5a0` / `0x28f860` from `this+0x298/+0x29c = [200.0, 640000.0]` as an exact float32 reciprocal near/far ramp, copied by `0xf02d0`, and consumed unchanged by `0x267010`; direct LRI block and public calibration fixed32 checks find zero hits for the vector.
  Follow-up endpoint/count verification proves that selected `[200.0, 640000.0]` endpoint pair comes from static binary float tables at `0x609428` / `0x609430`, propagated through `0x3ff43c -> 0x2681b0 -> 0x26ba90`, and that the lookup count is computed by `0x28f5a0` from the five `0xa8` source records in `this+0x258`, `this+0x18`, first-record scalar, endpoint reciprocal span, clamp `0x1000`, and mode rounding by `this+0xc = 8`.
  Deterministic depth-bound custody traces the same selected pair through `state+0x100/+0x104` and Triangulator owner `+0x70/+0x74` into imported Ceres lower/upper-bound setters on the scalar used by the ray-depth reprojection cost. All four canonical Unit-1 focal tiers select mode `0` and `[200.0,640000.0]`, admitting the generated reciprocal lookup's internal ray-depth hypothesis-grid role.
  Follow-up SHA-pinned static proof plus Unit-1 four-focal runtime and an exact-focal Unit-2 `28mm` body discriminator joins the index-5 descriptors and `UpsampleLayer+0x90` through depth-cache promotion to the GDepth writer. It admits ray-depth scalars, `[200,640000]` bounds, and depth-map pixels in `mm`, with reciprocal lookup values in `mm^-1`. Complete installed constructor/wrapper reference census proves the sole owner caller hardcodes mode `0`, so the values are installed Triangulator ray-depth lower/upper bounds, not public calibration/LRI/protobuf fields.
  Installed labels plus admitted runtime custody now name the generated chain as previous-layer `Depth map` plus `Skip mask` to per-pixel `(lower,count)` `Range map`, then `Cost volume`, minimum-cost depth-hypothesis index map, and output `Depth map`. The variable source records are per-pixel cost-volume records, not anonymous public records or direct LRI/protobuf messages.
  Follow-up static/four-focal proof exactly names `StereoLayer+0x240` as `Images` and classifies the parallel five `+0x258` records as per-image composed geometry records produced through `state+0xe0`, same-key `state+0x448`, `0x264440`, and `0x23faf0`. SHA-pinned static proof plus Unit-1 four-focal runtime and an exact-focal Unit-2 `28mm` discriminator closes their whole-field operational identity as derived tier-anchor-relative calibrated camera models and joins their fields to public focus-dependent intrinsics, anchor extrinsics, sensor size, and exact same-camera distortion-polynomial inputs. `0x28f5a0` computes their maximum inverse-extrinsic-center separation.
  SHA-pinned control-block RTTI and same-process pointer custody now name every selected `state+0xe0` object exactly as `lt::CapturedImage` across the Unit-1 focal quartet and exact-28mm Unit-2 discriminator. Static RTTI/construction custody also names `state+0xe0/+0xe8` as retained `shared_ptr<lt::RawImageFactory>`, backed by `shared_ptr<lt::CaptureStack>`. Generated-parser/copy proof names `CapturedImage+0x64` as public `CameraModule.frame_index` and factory `+0x10` as its selected-frame lookup key; the admitted renderer-owner path selects frame `0`. Numeric `CalibStage` mapping is also closed as `0=factory` at `CapturedImage+0x180` and `1=current` at `+0x12c`.
  Installed-label and reused-runtime proof names the sampled `0x276860` fields as tier-anchor `Guidance`, `Pixel buf`, `Min cost buf`, and `Line buf`. SHA-pinned constructor/recurrence proof additionally closes target `+0x56 = 1` as SGM `P1`, `+0x58 = 500.0` as the nominal guide-adaptive `P2/P1` ceiling scale, and `+0x60 = log2(e)/(18,48,48)` as body-independent three-channel guide-distance decay tuning. RTTI/call-chain plus descriptor-watch proof names Guidance's public producer as `StereoISP::CreateStereoImage` and its output type as `Image<vec4x8ui>`. Complete Unit-1 four-focal plus exact-focal Unit-2 proof closes pre-YUV collapse2 as `[R,0.5*(G1+G2),B,1]`; corrective direct-custody/full-plane proof closes the following `StereoISP::ConvertToYUV` matrix, signed-power, offset, and pack, naming final Guidance `[Y,U,V,1]` with public AWB and `SENSOR_AR1335` origins. Version `3.0.327` further closes wide A2 from public RAW10 through sensor normalization, body-specific vignetting, exposure scaling, and mono replication over two scenes and both physical bodies. Version `3.0.337` closes selected color-camera stage-3 normalization and stage-12 public vignetting over exact-28mm two-body A1 plus one four-model Unit-1 movable-camera discriminator. Corrective version `3.0.339` proves selected scalar-true cross-talk callback `0x1054d0` is live at all four Unit-1 focals plus Unit-2 exact-70mm and feeds demosaic; version `3.0.340` closes its public camera-ID matrix origin, public-RAW amount fit, installed A/B/C table selection, generated IR/AWB preparation, coordinate/boundary recurrence, limiter, and output blend with exact two-body replay. Follow-up allocator/helper/worker proof closes the remaining sampled recurrence roles as generated `Line buf` predecessor/current path costs, `Min cost buf` baseline/current minimum, baseline-plus-adaptive-`P2` cap, local matching-cost temporary, and Cost-volume accumulation. Bit-exact clean-room replay closes the Bayer noise-LUT generator, and exact pointer-algebra plus live halo replay closes both rank-neighborhood coordinate formulas. Stable full-map distributions remain validation scope; the remaining color-camera public-RAW work is the end-to-end A1/A3/A4/A5 join and sufficient non-anchor camera coverage, not scalar cross-talk arithmetic.
  Follow-up `state+0x448` payload-origin proof admits exact public pose-component origins for first-pass payload `+0x00..+0x2c`: `A1` rotation/translation at `28mm` / `35mm` and `B4` rotation/translation at `70mm` / `150mm`, shared across the first-pass inserted keys. Checked later `+0x30..+0x3c` source slices have zero exact public fixed32-sequence hits; a later-box formula proof now bounds those fields as `0x260e40` outputs from the `0x145980(object)` box and `object+0x114/+0x118 = [4160,3120]`: uniform scale at `+0x30/+0x34` and box origin at `+0x38/+0x3c`. This is still not whole-payload semantic closure.
  Companion static-origin, embedded-schema, and two-body runtime proof closes that formula's public input ancestry: the size pair is the LRI-stored `CameraModule.sensor_data_surface.size`, and `FactoryModuleCalibration.geometry.distortion.polynomial.{distortion_center, normalization, coeffs, fit_cost}` supplies the converted per-camera record consumed by `0x145590 -> 0x145980`. The computed envelope, uniform scale, and full `state+0x448` payload remain derived rather than direct public fields.
  Follow-up G-38 proof closes the derived envelope arithmetic itself: 30 exact Brown-Conrady radial samples, four-point cubic inverse-radius evaluation, 91 vertical and 121 horizontal edge samples, inner extrema, and float32 truncation reproduce all 20 Unit-1 four-focal boxes plus five exact-`70mm` Unit-2 boxes. The envelope remains a derived internal value, but its formula is no longer unknown.
  Embedded-schema extraction and representative wide/tele LRI checks on both physical bodies now close the anonymous names for the captured K path: runtime `object+0x54` is `CameraModule.lens_position`, public geometry `field_6` is `CalibrationFocusBundle.focus_hall_code`, and the input/pose carriers are `intrinsics.k_mat` plus `extrinsics.canonical.rotation/translation`. The complete installed K evaluator policy is also closed: stable Hall sorting, direct one-record copy, sole-pair two-record interpolation/extrapolation, three-record split at the middle Hall coordinate, exact float32 slope/intercept arithmetic for fields `{0,2,4,5}`, and the `0.001` separation guard. Eight exact-focal/two-body LRIs all carry exactly two records per camera; no three-record corpus incidence is claimed. Numeric bank names are also closed as `0=factory` / `1=current`. The remaining unknown is wider B/C record coverage, complete bank-field semantics, stable full-map distributions, and final effect, not Guidance `[Y,U,V,1]`, the names or evaluator policy of those scoped inputs, the polynomial ancestry of the computed envelope, the composed-geometry whole-field identity, or sampled recurrence-term custody.
- What is unknown:
  the exact public calibration semantics and LRI calibration origins for the pair-grid row fields `+0x00..+0x3c` outside the admitted composed-camera-model path, `state+0x448` payload fields beyond the scoped first-pass `+0x00..+0x2c` pose components and later `+0x30..+0x3c` derived box/scale formula, complete selector-bank field names beyond `0=factory` / `1=current`, remaining unclassified `CapturedImage` fields, and stable full-map Cost-volume distributions. Final key-`0` Guidance `[Y,U,V,1]`, its public AWB/sensor-type inputs, and its full formula are now closed. Already named public fields include `CapturedImage+0x30 is_enabled`, `+0x38 sensor_exposure`, `+0x40 sensor_analog_gain`, optional `+0x44 sensor_digital_gain`, `+0x64 frame_index`, and optional `+0x104 sensor_temparature`.

- The former final-stage constant gaps are closed by
  `bundle_static_runtime_final_stage_constants_four_zoom.md`: wavelet detail
  weights are exact float32 `(-1/192,-1/96,-1/48,-1/24)`, the SIMD abs mask is
  four lanes of `0x7fffffff`, the seven-tap sharpen Gaussian is exact and
  four-focal verified, the named bilateral specialization has uniform 5x5
  spatial support by installed static proof, and live NLM configuration is
  `window_size=5`, `patch_size=5`, `step_size=2`, hence search radius `2`,
  across the canonical quartet. These narrow constants do not close wider
  sharpen/denoise policy or merge acceptance.

- The former CalibStage transferred-slice naming gap is closed by
  `bundle_static_runtime_calibstage_slice_public_names.md`: selected-node
  slices `+0x30..+0x53`, `+0x60..+0x83`, and `+0x54..+0x5f` are publicly
  named `intrinsics.k_mat`, `extrinsics.canonical.rotation`, and
  `extrinsics.canonical.translation`, respectively, and are repacked as
  `K,R,t` into selector-1/current. Remaining bank-field unknowns exclude
  these named components and remain limited to unrelated fields or semantics
  outside the proven `0x54`-byte bank.

- The CCM illuminant-variant gap is closed by
  `bundle_static_runtime_ccm_illuminant_selection_four_zoom.md`. Public
  stored types `{0,2,6}` are `{A,D65,F11}`, not `{A,D50,D65}`; D50 is public
  type `1` and absent from the 42-record blocks. The tested four-focal bridge
  path selects same-camera A/D65 matrices and uses the pinned clamped
  reciprocal-temperature interpolation; F11 is stored but unselected in all
  captured calls.

- The former Laplacian-clarity stub is closed by
  `bundle_static_runtime_laplacian_clarity_kernel_28mm.md` and
  `bundle_static_runtime_laplacian_pyramid_construction_shaping_28mm.md`. Installed
  `CreateAndBlendLaplacianPyramids` builds 8049 samples on `[-16,16]` with
  `clamp(x,-2*sigma,2*sigma) + clarity*x*exp(-x^2/(2*sigma^2))`, interpolates
  adjacent transformed pyramids, and blends them by `0.75^level`. Public
  `lpyr_*` names, offsets, defaults, and the 19-value sample vector are pinned.
  The total plane count is `clamp(trunc(log2(min(W,H))-2),2,6)`; exact five-tap
  reduce, parity expand, negative-detail reconstruction, and all five tonal
  field formulas are pinned. Runtime liveness and detail levels `0..4` from
  six total planes are explicitly Unit-1 `28mm` scope.
- Why it blocks the end goal:
  wrong geometry can still look plausible while causing subtle double edges, contributor drift, or ghosting.

### Closed: Full merge topology beyond the four-zoom-proven accumulator, entry signature, and direct contributor identity

- Related claims: `CLM-MERGE-002`, `CLM-MERGE-003`, `CLM-MERGE-004`, `CLM-PREFUSION-001`
- What is known:
  IRAMP is real and merge-critical.
  Its weighted accumulator at `0x369fa1..0x369fa8` is runtime-observed across the canonical four-zoom bridge HDR quartet.
  Its tested bridge-HDR signature is now directly verified across the canonical four-zoom bridge HDR quartet: `src1`, `src2`, `srcs[5]`, `warps[5]`, scale, and ROI.
  The direct IRAMP contributor-vector identity is now verified across the corrected canonical four-zoom bridge HDR quartet: `B1..B5` at `28mm` / `35mm`, and `C1..C5` at `70mm` / `150mm`.
- What is unknown:
  claim-level completion of remaining merge-critical mechanics beyond accumulator, entry-signature, and direct contributor-vector identity participation across `28mm`, `35mm`, `70mm`, and `150mm`.
- Why it blocks the end goal:
  a replacement that only matches one tier or partly inherits the others is not the target.

### Closed: Tele odd-camera routing, especially C6

- Claim IDs: `CLM-C6-001`, `CLM-CCM-001`, `CLM-ZOOM-002`
- What is known:
  C6 fires in the tested `70mm` and `150mm` tele LRIs.
  C6 is absent from the directly observed five-item IRAMP contributor vector at `70mm` and `150mm`.
  The tested visible-`src1` keyed helper / vector-builder boundary is not the positive route: tele helper keys there are `5..14`, with no key `15`.
  The tested visible-`src1` projection field-pack dispatcher boundary is also not the positive route: tele dispatcher keys there are `8,10..14`, with no key `15`.
  The direct `0xe59a4 -> 0xf2770` constructor callsite constructs tele key `15` / C6 with active byte `+0x30 = 1`.
  Hardware write-watchpoint proof captures tele key `15` / C6 changing from item `+0x30 = 1` to `0` at writer `libcp+0x3c90a5` inside body `0x3c8f90` in both canonical tele seeds; static local gate shows this writer clears key `15` when the grouped context `+0x44` value is not group ordinal `2`.
  A focused 24-site direct `0xf2720` callsite census under complete `70mm` and `150mm` bridge HDR runs observes key `15` active at `0x1bdbab` / `0x1bdbdd` key-list helper sites and at mutation-body sites `0x3c9043` / `0x3c9098`, then inactive at later selected key-query sites `0x3b2143`, `0x402df7`, and `0x40d219`; follow-up mutation-identity proof ties the active helper/mutation observations, `0x3c90a5` store, immediate inactive `0x3c90a9` state, and later inactive `0x3b2143` observation to the same tracked key-15 item pointer per tele run. Static inspection classifies helper `0x1bdb60` itself as key-list construction bookkeeping, not image-buffer work. A remaining-direct 34-site census covers the rest of the static direct `call 0xf2720` inventory and finds additional active key-15 observations only at constructor-adjacent key/container/tree materialization sites plus inactive shared-object lookup at `0xe6be0`; together the two proofs cover all 58 direct `0xf2720` callsites under the canonical tele bridge HDR path.
  A post-mutation active-byte read/write watchpoint on the same tracked key-15 `item+0x30` byte records 18 later stops per canonical tele render; every stop still observes `item+0x30 = 0`, and the stopped libcp sites include active-byte gates outside the direct `0xf2720` inventory. This narrows same-byte reactivation and later active-byte consumers, not other C6 fields, aliases, or final image effect.
  A selected-field post-mutation read/write watchpoint on that same tracked item watches `item+0x30`, `item+0x58..0x5f`, `item+0x60..0x67`, and `item+0x100..0x107` in complete canonical `70mm` and `150mm` bridge HDR renders. All pre-output libcp samples preserve active byte `0`, pair `(-1,-1)`, key `15`, and type `3`; the watched `+0x60..+0x67` range is read at `0xf2727` (`+0x60`) and `0xf3327` (`+0x64`), while the watched pair and type/adjoining ranges record only allocator-cleanup stops after output write. This narrows selected fields, not untested fields, object aliases, final effect of the watched `+0x60..+0x67` reads, or final image effect.
  A follow-up post-mutation state-consumer probe proves the immediate caller path after `0x3c8f90` consumes the constructed `ctx+0xa0` object, walks the eleven-entry item vector, observes key `15` inactive at `0x3b2143`, writes derived state fields `+0x0 = 3` and `+0x4 = 1` to context `+0xc8`, and queues context `+0x4b0 = 5` in both canonical tele seeds.
  A follow-up downstream rect-vector probe proves the immediate caller segment rereads context `+0xc8` as state fields `+0x0 = 3` and `+0x4 = 1`, receives `0` from `0x40b0e0`, takes fallback branch `0x3c8c00`, reads context `+0x4b0 = 5`, passes that value as `r8d` into `0x3c8d00`, and returns a five-entry vector of 16-byte integer tuples in both canonical tele seeds.
  A four-zoom follow-up proves that rect-vector route is consumed by the immediate caller into five `context+0x4c0` delta-dimension pairs, passed to `0x3982b0`, and used to build a five-level `CIAPI::ImagePyramid` stored at `context+0x538`.
  A further four-zoom follow-up proves the caller immediately iterates those five ImagePyramid levels, builds a full-image descriptor for each level, and invokes direct zero-fill callsite `0x3b2f54 -> 0xf7c0` once per level with bytes-per-pixel argument `4`; runtime descriptors satisfy `stride_pixels == width` for all twenty level descriptors, and the first 32 bytes sampled after return are zero for all twenty descriptors.
  A downstream-candidate liveness probe across complete canonical bridge HDR renders re-hits the zero-fill checkpoints but records zero hits at selected later static `context+0x538` candidate families.
  A representative hardware data-watch probe across complete canonical bridge HDR renders arms read/write watchpoints after `0x3b2f59` on the first 8 bytes of selected zero-filled ImagePyramid level buffers and records zero watchpoint hits before clean completion.
  An expanded tele hardware data-watch grid across complete canonical `70mm` and `150mm` bridge HDR renders arms first/middle/last 8-byte read/write watchpoints after `0x3b2f59` across all five zero-filled ImagePyramid levels and records zero watchpoint hits before clean completion in all 30 admitted cells.
  A candidate downstream context-consumer probe for `0x3c9540 -> 0xe6c30` records zero hits in both complete canonical tele bridge HDR renders while re-hitting constructor/mutation custody sites; this excludes only that candidate route.
  The direct payload candidate loop immediately upstream of that dispatcher does see key `15` at both tele tiers, but runtime proof shows post-mutation `object+0x30 = 0`; key `15` skips before class compare and before the dispatcher call.
  The stereo-side keyed-record loop inside `0x3f2c40` also sees key `15` at both tele tiers, but runtime proof shows post-mutation `object+0x30 = 0`; key `15` skips before both tested `0xf2720` getter callsites.
  Block 6 CCM coverage is missing A2 and C6, and the tested dispatcher filters missing-CCM cameras.
  The stale `150mm = 6C only` story is not viable.
- Closure:
  repeated canonical `70mm` and `150mm` terminal differentials prove that
  restoring only C6/key-15 `CapturedImage.is_enabled` after `0x3c90a5`
  deterministically reaches the pinned per-key `SourceImageCache`
  mono-module rejection and writes no output, while unchanged baselines
  succeed. Joined to the admitted direct-call, same-byte, selected-field,
  candidate-gate, and zero-filled ImagePyramid evidence, C6 must remain
  excluded from successful canonical tele bridge-HDR image contribution.
  `CLM-C6-001` is no longer an end-goal unknown.

### Closed: Merge acceptance / rejection logic beyond the proven accumulator

- Related claim: `CLM-MERGE-002`
- What is known:
  the weighted accumulator exists and is runtime-observed across the canonical four-zoom bridge HDR quartet.
  The local non-empty IRAMP partner-record consumer path is bounded through coarse SIMD SAD / `phminposuw` winner selection, local absolute-difference refinement, guarded float refinement, 16x16 bilinear vec4 resampling, `0x36cde0`, and a three-float scratch write at `0x369e7e..0x369e91` across the canonical four-zoom quartet.
  `0x36cde0` now has a complete clean-room scalar formula: candidate lane-0 L1 normalization to prepared reference L1; fine/coarse structural vectors from normalized means, variances, and covariance; fixed lifting/detail agreement using `-1/192` and `-1/96`; per-scale `min4`; and `sqrt(coarse_min*fine_min)`. One canonical Unit-1 `35mm` prepared-input replay is bit-exact, and prior complete four-focal probes establish score-site liveness/consequence.
  `0x36e530` now has a complete clean-room formula: five-vector reciprocal normalization selected by `min(v2(x),v2(y),4)` followed by inverse CDF 9/7 lifting at strides `8,4,2,1`, horizontal before vertical, with symmetric endpoints and exact installed float32 order. Exhaustive basis proof matches all `65,536` floats, and baseline/nonbaseline live whole-buffer replays are byte-exact.
  The first downstream tuple consumer is now bounded: it reads the third tuple scalar at `0x36a7d8`, reads the first two tuple floats at `0x36a803` / `0x36a814`, passes an adjusted coordinate pair to `0x372a00`, forms multiplier `(t + 2 * max(0, t - 0.5), t, t, t)`, reaches multiply-add loop `0x36a8c0..0x36a8cb` across all four canonical zooms, and adds the third scalar into a running scalar sum initialized from `0x5df904 = 0.200000003`.
  The immediate post-reciprocal weighted-add path is now bounded: `0x19e7d0` copies/scales descriptor-backed `vec4` buffers by the reciprocal vector, and `0x36aa30..0x36aa57` blends `reciprocal * 0.2` into lane 3, applies `weight[inner] * weight[outer]`, adds into the destination `vec4`, and is reached across all four canonical zooms.
  The immediate post-weighted-add shaping path is now bounded: `0x36abf0..0x36ac15` applies a lane-3-weighted clamped vector update with first-hit scale `(2,0,0,0)` and clamp bounds `[-0.1,0.1]`, and `0x36ad50..0x36adac` applies a fixed 3-vector transform with lane 3 forced to `1.0`; both sites are reached across all four canonical zooms.
  The caller-side handoff after IRAMP returns is now bounded: the caller validates the `rbp-0x60` descriptor dimensions against ROI, wraps it at `rbp-0x88`, calls helper `0xd76a0`, and static helper inspection shows source `vec4` lanes are squared into the destination descriptor; the handoff is reached across all four canonical zooms.
  The caller-side vector-scale handoff after square-copy is now bounded: the caller builds a wrapper over the `rbp-0x70` descriptor with a vector at wrapper `+0x10`, calls helper `0x2d7320`, and static helper inspection shows source `vec4` lanes are multiplied by that vector into the destination descriptor; the handoff is reached across all four canonical zooms.
  The caller-side `0x3e5720` executor setup after vector-scale is now bounded: it allocates/resizes a 6-byte-element destination descriptor, builds callback vtable `0x66b020`, dispatches generic executor `0x5670`, and its visible worker maps source 16-byte `vec4` rows to destination 6-byte rows before calling row callback `0x38a30`; the setup is reached across all four canonical zooms.
  Row callback `0x38a30` is now bounded: for the observed `512`-wide rows it repacks source `vec4` lanes 0..2 into float triples, calls `0xbfef0` with `ecx = 0` and count `3 * width`, and the used `0xbfef0` branch converts float channels to 16-bit binary16 bit patterns; first captured callback rows match the static conversion formula across all four canonical zooms.
  The immediate caller-side storage sink for the `0x3e5720` conversion output is now bounded: body `0x3ec960` is vtable slot `0x65f5e0+0x30`, computes destination descriptor `(*rsi)+0xf0`, allocates/resizes it with element size `6`, passes it to `0x3e5720`, then destroys only the temporary descriptor and returns; runtime packets across all four canonical zooms show the owner-backed descriptor populated as `512x512`, stride `512`.
  The first proven downstream consumer family for owner `+0xf0` is now bounded: `0x3d50f0` allocates a `16`-byte-element destination, dispatches executor `0x5670` with row worker `0x3d5290`, and the selected converter path reaches `0x2ff00 -> 0xc0410`; runtime packets across all four canonical zooms show live `rsi` at `0xc0410` inside the exact owner `+0xf0` data range with `ecx/cl = 0`.
  The immediate handoff after that expansion family is now bounded: `0x3d4e10` calls `0x3d50f0`, and runtime packets at `0x3d502e` across all four canonical zooms show the local source descriptor at `rbp-0x60` points inside the exact owner `+0xf0` data range while the local expanded descriptor at `rbp-0x90` has `16`-byte elements and a first `vec4` sample with lane 3 = `1.0`.
  The destination backing store for that handoff is now bounded: `0x3d4e10` receives a caller-provided context whose `+0x10` field points to the persistent 16-byte destination descriptor, and runtime packets across all four canonical zooms show local destination descriptor `rbp-0x90` is a clipped view into that context descriptor with matching `qword_28`, in-range data pointer, and 16-byte alignment.
  The first captured route after that destination context is now bounded: the accepted first route uses active callable branch `0x3d4842`, active callable slot `0x3ec960`, parent `0x3d01b0` output descriptor `rbp-0x148` as `context+0x10`, caller return `0x3d084d`, and the same temporary descriptor is passed to `0x36f800` at `0x3d08ce` across all four canonical zooms.
  A follow-up first-owner census proves sibling direct branch `0x3d4864` is live for the first captured owner `+0xf0` descriptor at `28mm`, `70mm`, and `150mm`, while `35mm` accepted only `0x3d4842`; every accepted census packet still uses slot `0x3ec960`, returns to caller `0x3d084d`, and preserves the parent/context destination equality checks.
  A follow-up direct-branch post-route proof shows the first owner-matching direct branch at `28mm`, `70mm`, and `150mm` reaches `0x3d08ce -> 0x36f800` with `rsi` equal to the same temporary descriptor captured as `context+0x10`; `35mm` has no owner-matching direct branch under that first-owner probe.
  A follow-up global branch-site census removes the first-owner gate and proves complete canonical bridge HDR renders at `28mm`, `35mm`, `70mm`, and `150mm` hit only caller set `{0x3d0732, 0x3d084d, 0x3ecc5a}` and active callable slot set `{0x3ec960, 0x3e4a80}` at `0x3d4842` / `0x3d4864`, with every hit preserving the parent/context destination equality checks.
  A follow-up post-route family proof classifies those caller families across the canonical quartet: `0x3d0732` is exact-size cleanup with no post call, `0x3d084d` reaches `0x3d08ce -> 0x36f800`, and `0x3ecc5a` reaches `0x3ecc74 -> 0x3edb80` visible-`src1` one-image normalization.
  A follow-up parent-chain ancestry proof shows `0x3d0732` returns through `0x3b07a9 -> 0x41a8d3 -> 0x3adfce -> 0x280e`, `0x3d084d` returns through `0x3bb822 -> 0x3adfce -> 0x280e`, and visible-`src1` `0x3ecc5a` returns through `0x374cf3 -> 0x3665da -> 0x365f50 -> 0x3ec7df -> 0x3eca4b -> 0x3d4842` with some nested read-context continuations. Exact hot direct-branch hit totals are evidence-run counts, not algorithm constants.
  A follow-up static parent-chain body classification separates callback/iteration glue (`0x280e`, `0x3adfce`) from selected owner-cache/direct-render tile surfaces (`0x3b0740`, `0x41a7d0`, `0x3b9770`, `0xfbda0`, `0x3bb2b0`) and visible-`src1` / IRAMP nested wrapper plus owner `+0xf0` sink surfaces (`0x374ac0`, `0x3661b0`, `0x365960`, `0x3ec770`, `0x3ec960`).
  A follow-up static helper-surface classification bounds exposed route plumbing: `0x31b110` is source/RAW/STD adapter into `0x33fb30`, `0xfe720` is a rectangle/ROI record-grid helper, `0x106cb0` is vignetting-data construction/interpolation, `0x2e20` is callback dispatch, and `0xf3570`, `0x3b9660`, `0x3c6ac0`, `0x1bea20`, `0x1bea00`, and `0x1be970` are owner/tile/map/field helpers.
  A follow-up static selected-cache/post-route classification bounds exposed selected-cache plumbing: `0x3d01b0` is level/ROI tile-read executor, `0x3d0650` is exact-size read or read-then-`0x36f800` rescale, `0x3d47d0` is read-context branch routing, `0x3d4e10` / `0x3d50f0` / `0x3d5290` / `0x2ff00` / `0xc0410` are clipped-view / 6-byte-to-vec4 / 16-bit-to-float conversion plumbing, and `0x3edb80` is one-image `sqrt(max())` normalization.
  A follow-up static downstream direct-caller census bounds direct callers of selected downstream helpers in the repo-local static callgraph: `0x36f800` direct callers are selected-cache read/rescale, TileCache-like read/rescale, and IRAMP-internal resample handoff; `0x3d01b0` direct callers are selected-cache reads, visible-`src1` read, source-adapter caller, and DOFCache render caller; `0x3edb80` direct callers are visible-`src1` and visible-`src2` one-image normalization wrappers; and `0x3d50f0` has only the already classified `0x3d4e10` direct caller.
  A follow-up static selected-cache caller census bounds direct callers of `0x3d0650` in the repo-local static callgraph: the 14 direct callers fall into source-adapter-style caller windows, small owner-cache selector `0x3b0740`, multi-branch owner/tile-cache surface `0x3bb2b0`, owner `+0xf0` output-sink branch body `0x3ec960`, and later helper/adaptor caller surfaces around `0x42fb40` and `0x42fd30`.
  A follow-up static `0x3e5720` caller census bounds direct callers of the row-conversion executor setup in the repo-local static callgraph: the only direct callers are active-callable-slot / owner `+0xf0` writer body `0x3e4a80`, owner `+0xf0` output-sink body `0x3ec960`, and DOFCache render body `0x3f0b90`; ancillary `0x432db0` coverage bounds the later selected-cache caller surface `0x42fb40 -> 0x3d0650 -> 0x432db0`.
  A follow-up static `0x3d4e10` caller census bounds direct callers of the owner `+0xf0` expansion handoff in the repo-local static callgraph: the only direct callers are the two already bounded branch-router post-branch handoffs at `0x3d484a` and `0x3d486c`, plus separate indexed-entry loop caller `0x3d5468` inside body `0x3d5400`; `0x3d50f0` has only direct caller `0x3d5029` inside `0x3d4e10`, and `0x3d5290` has no direct callers because it is worker-dispatch plumbing.
  Follow-up static/runtime proof binds that separate loop caller to executor vtable route `0x66a728/+0x30 -> 0x3d53c0 -> 0x3d5400 -> 0x3d5468 -> 0x3d4e10`, and first-hit probes prove liveness at `28mm`, `35mm`, `70mm`, and `150mm`.
  The first gated `0x36f800` worker path after that route is now bounded: callback slot `0x3721d0` enters static worker body `0x372210`, runtime packets reach after-prologue worker-entry probe `0x372224`, and the first captured store at `0x372488` equals four captured source `vec4`s multiplied by four captured weight `vec4`s across all four canonical zooms.
  Helper activity inside that same route is now bounded: `0x372210` converts offset/scale doubles to signed 16.16 fixed-point, `0x372500` builds the row-plan/cache struct, captured `0x372760` row-cache stores match the reconstructed 4-tap horizontal `vec4` formula across all four canonical zooms, and fresh first-dispatch row-plan packets capture all four unique worker regions per zoom with only the middle row-cache segment predicted/live in that dispatch.
  Full-render row-cache segment reachability is now bounded under the canonical quartet: leading/trailing `0x372760` store sites are live at `28mm` and `70mm`, and have zero hits at `35mm` and `150mm` under the tested canonical bridge HDR runs.
- Closure:
  anonymous tuple/cache helper fields are not public schema and do not alter
  the formula-level implementation. The installed candidate-policy census
  closes all local rejection decisions, and the return-only `t=0`
  intervention proves final-file consequence at canonical Unit-1 `35mm` and
  `70mm`, above their repeated-render floors. Joined to complete four-focal
  score-use and descriptor-to-writer custody, no later per-contributor
  predicate remains on the tested profile-3 CLI HDR route.

## Nonblocking Reference Gaps

These are not separate blocker categories. They are the concrete places where the current claim set is still incomplete.

### Historical: `src1` / `src2` formula and route contents

- `CLM-MERGE-003` and `CLM-MERGE-004` are now directly verified across all four canonical zoom seeds, but `CLM-PREFUSION-002` remains open.
- Four-zoom runtime proof now shows the visible `src1` payload constructor path is live, takes key `0` at `28mm` / `35mm` and key `8` at `70mm` / `150mm`, receives the same four-level dimension vector, and produces the expected `0x490` / `0x65f140` / `0x65f388` payload family, but this does not identify the semantic contents of `src1`.
- Four-zoom runtime plus SHA-pinned RTTI proof now names the visible `src1` payload exactly as tier-anchor `lt::ReferenceImageCache` and direct contributor payloads as `lt::SourceImageCache`; exact pixel-production details and the complete distributed reduction policy remain open.
- SHA-pinned IRAMP operand custody plus complete four-focal runtime probes closes the broad contents/role question: `src1` is the coarse byte registration guide and bounds operand, `src2` is the full-vector reference/baseline patch, and direct source/warp pairs are candidates. Both `0x36cde0` score and `0x36e530` scale-normalized inverse reconstruction have complete clean-room formulas. Exhaustive installed-body census closes local candidate/sentinel policy and proves no score threshold; return-only wide/tele final-file differentials close later global output suppression. Remaining closure is MonoFusion mode-`1` disposition.
- Four-zoom runtime plus SHA-pinned RTTI names visible `src2` as `PipelineCache::processLevel1` one-generated-descriptor resampling/materialization. Subsequent two-body proof identifies A1/A2 `lt::MonoFusion` wide versus direct B4 tele, and follow-up proof decodes production mode-0 core math. Version `3.0.338` additionally closes both flow-operand level-0 public joins and the exact `2/4/4/4` FastCollapse reduction through level 4 over two bodies; captured pyramids are no longer an implementation dependency. Scoped compatibility-mode and distributed-policy residuals remain open.
- Bundle plus four-zoom runtime proof now shows the payload-internal secondary callable family also differs between visible `src1` and the direct contributors, but this still does not identify the semantic contents of `src1`.
- Four-zoom runtime proof now shows the visible `src1` secondary callable at `0x3e4a80` is live and passes the same `0x490` payload to `0x3e2e90` for the first captured tile, but this still does not identify the semantic contents of `src1`.
- Four-zoom runtime proof now shows the first captured worker/projection-record path beneath that handoff reaches `0x3e4c50`, loads `payload+0x150` from visible-payload field `+0x170`, and identifies callable slot `+0x30 = 0x3e42e0`, but this still does not identify the semantic contents of `src1`.
- Installed-bundle proof now decodes `0x3e42e0` as coordinate transform math, but this still does not identify the public semantics or LRI origins of the fields it consumes.
- Installed-bundle proof now bounds the producer of those fields through `0x3f6170` and the converged writer `0x145580` / `0x144f50`, including a `4096`-float radius-table writer at `0x144a70`, but this still does not identify public calibration names or semantic `src1` contents.
- Four-zoom gated runtime proof now bounds the first captured visible-`src1` source-producer branch and virtual target: `0x3e3279 -> 0x31af30 -> 0x33ede0 -> 0x33f180 -> 0x33f3e8`, vtable address point `0x65b3c8`, slot `+0x30 = 0x341770`; static inspection bounds `0x341770` to per-source region-adapter / record-update work, and follow-up static proof bounds its helper `0x2e8680` / callback `0x2e8cc0` to one-source Bayer/RAW region-helper work, but this still does not identify camera membership or semantic `src1` contents.
- Follow-up four-zoom gated runtime census now shows the lower visible-`src1` path is broader than that first captured target: `0x33f3e8` and `0x33f94f` are observed at all four zooms, while `0x33ffd4` is observed at `28mm` / `35mm` and has zero hits at `70mm` / `150mm` under the tested gated runs. Nonzero virtual counts are `512`-hit caps and therefore lower bounds, not algorithm constants; this still does not identify camera membership or semantic `src1` contents.
- Follow-up runtime/static proof now binds the two prior indirect-call gaps in that target-family set: `0x342d99 -> 0x342b80 -> 0x2eb560` and `0x3449f0 -> 0x345920 -> 0x2f53d0`. A further gated runtime/static proof bounds the immediate `0x2f53d0` helper chain to validation, descriptor/vector setup, bilateral-kernel-size dispatch, callback-object dispatch through `0x5440`, and one row-executor dispatch through `0x5670`; follow-up static classification bounds the executor callback bodies under that chain to local descriptor transform / filtering / interpolation / normalization / accumulation surfaces. Follow-up runtime proof bounds the selected `0x2fb320` worker under that route as readable same-shaped descriptor fields, a `vec4` coefficient pointer, and approximate reciprocal-normalized weighted `vec4` stores. This still does not identify semantic `src1` contents, public helper-field semantics, public descriptor-field names/origins, or the upstream merge/reduction behavior.
- End-goal consequence:
  the IRAMP call signature, exact visible wrapper identities, tested level-1 descriptor ancestry, production mode-0 core math, live flow, public operand construction, and operand-pyramid reduction are safe to freeze. Unselected compatibility-mode behavior and the complete distributed reduction policy outside those admitted mechanisms are not.

### Closed: `150mm` tele participation

- `CLM-ZOOM-002` now has a dedicated proof: exact-focal public headers at
  `70mm` / `150mm` on both calibration bodies agree on
  `B1..B5,C1..C6`, and completed canonical Unit-1 tele runtime constructs all
  eleven keys initially active.
- End-goal consequence:
  canonical `150mm` initial firing is `5B+6C`; later C6 clearing/exclusion is
  a separate proven stage.

### Closed: Missing-CCM downstream fate

- `CLM-CCM-001` proves the two missing records and their profile-3 fates:
  A2 is consumed inside wide A1/A2 MonoFusion, while C6 is cleared and
  terminally excluded on the tele route.
- End-goal consequence:
  no fallback CCM record is invented for A2 or C6; alternate profiles remain
  outside this scoped admission.

## What Is Not Currently The Critical Path

These topics may matter later, but they are not the current end-goal blockers for a base parity-grade merge replacement.

### 1. Depth / DepthEditor paths

- Claim IDs: `CLM-DEPTH-001`, `CLM-DEPTH-002`
- Current status:
  reference-only, scope-bound facts.
- Reason not on the critical path:
  they are not currently canonical drivers for the base merge spec.

### 2. GUI-only or editing-only paths

- Current status:
  initial profile-3 editor construction is admitted at the topology surfaces
  for all four canonical focal tiers. One Unit-1 28mm brush edit is admitted
  as prepared-state reuse. Exact packing and conditional GUI byte order are
  admitted installed-static plus Unit-1 28mm runtime. The immediate tested
  default level-4 `PipelineCache ->` per-level Color-pipeline route and
  type-13 record identity are also admitted, with byte-exact before/after joins
  and seven named active callbacks. Exact formulas for the remaining
  display-specific callbacks, exact edit formulas, other controls/focals,
  exact debug-object formulas/public meanings, internal QuickSelect segmentation
  and commit behavior, and host-specific active GL branch remain reference-only. The DOF optical
  range, tile-radius, both uniform circle filters, mode-1 layered compositor,
  public five-mode dispatch, complete live DebugView selector set, and default
  QuickSelect mask/blend plus one active public-stroke mask/output join and
  exact RefocusSlider visualization math plus the RefocusPoint post-DOF
  strict-range overlay are closed at scoped Unit-1 `28mm` runtime. The common
  profiles-`1/2` MonoFusion mode-1 scalar body is also formula-closed at TRUTH
  `3.0.336`, with Unit-1 `35mm` and Unit-2 exact-`28mm` numeric scope; remaining
  alternate-profile surface behavior stays reference-only.
- Reason not on the critical path:
  the immediate goal is a modern replacement for the base Lumen merge path, not full GUI feature parity.

### 3. Outlier formats and non-baseline capture variants

- Current status:
  outside the canonical four-zoom baseline validation set.
- Reason not on the critical path:
  the end goal first requires stable parity on the canonical `28/35/70/150` path.

## Practical Reading Of The Unknowns

Current answer: the merge/reducer campaign is formula-closed under its admitted
scope, but the complete LRI-to-merged-image investigation is not. Raw
`RAW_PACKED_10BPP` location/unpack/phase is now closed by `CLM-INPUT-001`.
`DemosaickLightV1` is now formula-closed by `CLM-DEMOSAIC-002`.
Public AWB origin/decode is now closed by `CLM-AWB-001`.
Implementation-required blockers remain in distortion reconstruction,
four-tap table generation, consumed LRI metadata,
sensor/optical corrections, denoise routing, stereo residual policy, live
state fields, output encoding/color tagging, and statistical reference
measurements. The authoritative live set is the ledger `BLOCKER` set.

The numbered list below is superseded merge-campaign historical context:

1. We still do not know every compatibility-route formula for what `src1` / `src2` contain: visible `src1` is the tier-anchor `ReferenceImageCache`, visible `src2` ancestry is A1/A2 `MonoFusion` wide versus direct B4 tele, and production mode-0 source preparation/core math, public flow operands, exact operand-pyramid reduction, live flow, transform boundaries/packing, and the secondary-map callback are decoded. Unselected compatibility behavior and any distributed merge/reduction policy outside the admitted profile-3 mechanisms remain open.
2. We still do not know the exact producer-side row/map calibration semantics over the ROI-derived pair lattice.
3. We still do not have full four-zoom closure on remaining merge topology beyond the accumulator and entry signature.
4. We know C6 is constructed active, queried while active by key-list bookkeeping and constructor-adjacent key/container/tree helpers, covered across all 58 direct static `call 0xf2720` sites under canonical tele runtime census, later cleared at `0x3c90a5`, observed with the same watched active byte still `0` at 18 later post-mutation stops per tele render, selected-field watched with pre-output `+0x60` and adjacent `+0x64` reads plus cleanup-only stops for watched pair/type ranges, consumed inactive by a proven post-mutation state-classification path, passed into a proven immediate five-entry rect-vector builder path, consumed by a proven ImagePyramid construction route, immediately passed through a proven per-level zero-fill callsite, filtered before two canonical tele candidate gates, not routed through candidate consumer `0x3c9540 -> 0xe6c30` under tested tele bridge HDR, not observed at selected later static `context+0x538` candidate families under complete four-zoom bridge HDR renders, not touched at representative watched byte ranges after zero-fill, and not touched at first/middle/last watched 8-byte ranges across all five tele ImagePyramid levels after zero-fill, but still do not know whether the zero-filled ImagePyramid route is later written/read outside those watched ranges or through unprobed helpers/aliases in a final image/merge-effecting way, whether untested C6 fields or object aliases are live, whether the watched `+0x60..+0x67` reads have final image effect, whether that mutation is terminal, or whether C6 has any alternate downstream route with final image effect.
5. Final merge-quality acceptance is closed for the tested profile-3 CLI HDR route: exhaustive local policy plus a return-only score intervention proves final-file consequence, joined to four-focal descriptor-to-writer custody.

Everything else should be treated as either:

- already proven enough to write into the spec, or
- not yet on the critical path for the base parity target.

## Source Priority

This file is only a guide.

For authority, use:

1. `CLAIM_LEDGER.md`
2. `PARITY_BLOCKERS.md`
3. `MERGE_CRITICAL_TRUTH.md`
4. `TRUTH.md`
