# Blocker Resolution Paths

This file records whether there is an existing path to resolve each current parity blocker.

It does not change blocker status.

## Summary

Yes, there are existing paths for all five current blockers.

They are not equal in precision:

- four already have concrete probe or decode paths on record
- one has a real direction, but it is still broader than a single isolated probe site

## 1. Exact reducer behind `src1` / `src2`

- Related blockers:
  `CLM-PREFUSION-001`, `CLM-PREFUSION-002`
- Resolution path exists:
  Yes
- Current best recorded path:
  installed-bundle and LLDB runtime proof now narrows this beyond wrapper construction, four-zoom visible wrapper participation, `PipelineCache+0x8` as a level-dimension vector rather than an image/composite pointer, the visible `src1` lookup key, the visible `src1` payload constructor path and level vector, the visible `src1` versus direct-contributor payload-family split, the visible `src1` versus direct-contributor payload-internal secondary-callable split, the visible `src1` secondary-callable live handoff into `0x3e2e90`, the first captured `src1` worker/projection-record handoff through `0x3e4c50` and payload-internal callable slot `0x3e42e0`, the `0x3e42e0` coordinate-transform formula and field-pack producer, the visible `src1` worker source-image producer topology, the first captured visible-`src1` source-producer virtual target `0x65b3c8/+0x30 = 0x341770` as per-source region-adapter / record-update work, the `0x341770` helper `0x2e8680` / callback `0x2e8cc0` as one-source Bayer/RAW region-helper work, the capped target-family visible-body static classification as thunk / descriptor / region / materialization / cache / executor surfaces, the visible `src2` hot state/static body boundary, the visible `src2` branch-selector constructor-origin and scan-loop custody, the post-wrapper `initResAmp` per-key record/wrapper construction, the visible `PipelineCache+0x270` per-key wrapper read path, the visible `src1` read path, visible `src1` payload provenance, first visible payload runtime / project-worker surfaces, owner cache-selection / read-rescale, owner alternate-cache/helper setup, visible `+0x678/+0x680` constructor and immediate runtime record-pack surfaces, selected layer virtual setters/accessors, the `0x3f7ec0` record-consumer path, `StereoLayer<false>::runPass` cost-path surfaces, sibling `StereoLayer<false>::compute()` cost-path surfaces, scoped `HigherWarpDebug::renderDebugView` helper-caller exclusion, the prefusion callable gate, post-`State()` helpers, heavy consumers, dispatcher / selector layers, callback reuse, candidate block-geometry helpers, the visible feature-selection lane, the four-zoom `0x24c320` / `0x24d610` candidate-scoring family, and the four-zoom candidate-scorer output-vector custody seam into `0x2439b0`. None of those bounded surfaces by themselves close the merge/reduction blocker.
  The next boundary is now narrower too: four-zoom record-state histogram proof bounds `0x2439b0` as a live gate over those custody-bound scorer-output vectors, with unchanged wide family-A boundary histograms and tele family-B target-2 state `3` to state `4` promotion. Sampled downstream `0x241fd0` / `0x2416d0` entries and watched store sites did not match the exact scorer-output vector under that probe. This still does not prove public record-state semantics, final acceptance/rejection, or reducer closure.
  Follow-up tele promoted-record data-watch proof narrows the boundary again: selected tele records promoted by `0x2439b0` are later consumed by downstream code, and at least one watched promoted record per canonical tele seed advances to state `5` through `0x2416d0`. Treat this as a bounded downstream consumer anchor; public state `5` semantics, selected-index sinks, downstream image effect, final acceptance/rejection, and reducer closure remain open.
  Follow-up tele selected-index proof narrows the boundary again: promoted target-2 indices captured at `0x2439b0` enter concrete `0x2416d0` selected-index vectors under clean canonical `70mm` / `150mm` renders, and the small promoted sets captured by that probe reach `(state=5,target=2)` stores. Treat this as selected-index/state-relabel flow only; downstream state-5 consumers, image effect, final acceptance/rejection, and reducer closure remain open.
  Follow-up tele later-watch proof narrows the boundary again: watched promoted records that become `(state=5,target=2)` continue into `0x244560` and the already-bounded `0x25d090` candidate block-geometry / active-block helper family. Treat this as later state/candidate/geometry flow only; `0x25d090` output/effect, image effect, final acceptance/rejection, and reducer closure remain open.
- Path:
  1. BP at `libcp+0x3eb4df` and `libcp+0x3eb557`, where `src1` and `src2` IG shared_ptr targets are written into `PipelineCache+0x238/+0x248`
  2. dump `%rsi` to recover the concrete installed wrapper target
  3. read the installed vtable regions at `0x65f668` and `0x65f6e8` for the recovered wrapper family
  4. use the four-zoom LLDB wrapper proof as participation evidence, not closure: all canonical focal seeds hit `0x3ecc10`, `0x3ecd80`, and `0x3eced0`
  5. use the first visible bodies at `0x3ecc10` and `0x3ecd80` as exclusion anchors, not closure points: they already resolve to `PipelineCache+0x170` and `PipelineCache+0x1e0/+0x1d8`; also treat the post-wrapper `initResAmp` branch at `0x3eb3c0` as bounded per-key map/vector/record/wrapper construction at `PipelineCache+0x258` and `PipelineCache+0x270`
  5. treat `PipelineCache+0x8` as bounded level-dimension metadata, not a hidden image/composite pointer: the canonical quartet proves it is a five-entry packed `(int32 width, int32 height)` vector; `src1` / `src2` wrapper dimensions are read from vector entry `1 = 4160x3120` in all four runs
  6. treat the visible `PipelineCache+0x270` per-key wrapper read path as bounded:
     `0x3eced0` calls one payload path through `0x3d0d20`, then performs in-place square-root normalization; `0x3d0d20` is checked single-payload level/ROI tile processing plus generic executor dispatch
  6. treat the visible `src1` read path as bounded:
     `0x3ecc10` resolves through `PipelineCache+0x170`; `0x3e0af0` is a map/tree lookup; `0x3d01b0` is a checked single-source level/ROI tile read; `0x3edb80` is one-image square-root normalization
  7. treat visible `src1` payload provenance as bounded:
     `PipelineCache+0x170/+0x178` is copied from the incoming `r8` argument at `0x3ea7d0`; the caller at `0x3b3145` passes the `+0x6a8/+0x6b0` shared-ptr-like pair; `0x3dfcc0` builds the first map/tree at object `+0x18`; `0x3dfcc0` stores a `0x490`-byte payload constructed by `0x3e2db0 -> 0x3e27a0` into node `+0x28`; `0x3e0af0` returns that node `+0x28`
  8. treat the visible `src1` payload constructor path as runtime-bounded:
     four-zoom runtime packets show `0x3dfcc0 -> 0x3e2db0 -> 0x3e27a0` is live, the constructor key is `0` at `28mm` / `35mm` and `8` at `70mm` / `150mm`, the constructor receives level vector `(4160,3120)`, `(2080,1560)`, `(1040,780)`, `(520,390)` on all four seeds, and the success packet produces the expected `0x490` payload with vtable `0x65f140`, secondary address point `0x65f388`, and self-pointer at `+0x68`; this proves constructor/provenance shape, not semantic contents
  8. treat the visible `src1` lookup key and adjacent contributor-key vector as bounded:
     four-zoom runtime packets show `0x3e0af0` returns from the `0x3ecc10` path with key `0` at `28mm` / `35mm` and key `8` at `70mm` / `150mm`; the adjacent direct-contributor key vector is `[5..9]` at `28mm` / `35mm` and `[10..14]` at `70mm` / `150mm`
  8. treat the visible `src1` payload family versus direct-contributor payload family as bounded:
     four-zoom runtime packets show the visible `src1` lookup returns a `0x490` payload with vtable address point `0x65f140` through object `+0x18`, while adjacent direct contributor lookups return `0x1f0` payloads with vtable address point `0x65f490` through object `+0x30`; this excludes treating visible `src1` as just another direct contributor-vector payload, but it does not identify `src1` semantic contents
  8. treat the payload-internal secondary callable split as bounded:
     visible `src1` payloads carry secondary address point `0x65f388` at payload `+0x60` and substantive slot `0x3e4a80`, while direct contributor payloads carry secondary address point `0x65f4d8` at payload `+0x60` and substantive slot `0x3e78d0`; `0x3e4a80` routes through the already-bounded `0x3e2e90` payload ROI/process path, and `0x3e78d0` is a different SourceImageCache-guarded path
  8. treat visible `src1` secondary-callable live participation as bounded:
     four-zoom runtime packets show `0x3e4a80` fires, reaches the same caller stack `0x3d4842 <- 0x5d97 <- 0x3873 <- 0x55a2 <- 0x3d03d6`, and the first captured `0x3e4b09` packet passes the same `0x490` payload into `0x3e2e90`; this confirms live handoff for the first captured tile, not payload-composition closure
  8. treat visible `src1` worker/projection-record live participation as bounded:
     four-zoom runtime packets show the first captured `0x3e2e90` worker path reaches `0x3e4c50`, shares the backtrace `0x3e4c50 <- 0x5d97 <- 0x3873 <- 0x55a2 <- 0x3e3bce <- 0x3e4b0e <- 0x3d4842`, lays out callback fields as source image / output image / default vector / projection record / weight table, and loads `payload+0x150` from visible-payload field `+0x170` through a projection record whose low i32 index is `0`; this exposes the next callable body `0x3e42e0`, but it does not prove camera identity or payload-composition closure
  8. treat the exposed `0x3e42e0` callable body as bounded:
     static proof shows it is coordinate-transform math using payload fields `+0xf8/+0xfc`, `+0x100`, and `+0x118..+0x140`; the formula is now known, but public field semantics / LRI origins and `src1` payload composition remain open
  8. treat the `0x3e42e0` field-pack producer as bounded too:
     static proof shows constructor `0x3e27a0` calls `0x3f6170`, the dispatcher routes to same-category `0x3f6200` or cross-category `0x3f6940`, both converge on `0x145580` / `0x144f50`, and `0x144a70` forces the radius table to `4096` floats; this proves producer topology for the transform state, not public calibration semantics or `src1` payload composition
  8. treat the `0x3f6170` projection field-pack dispatcher runtime key boundary as bounded too:
     complete four-zoom bridge HDR runs with `.lris` auto-loading disabled hit `0x3f6170`, `0x3f61b8`, same path `0x3f61ca -> 0x3f6200`, and cross path `0x3f61e1 -> 0x3f6940`; observed keys were `0,5..9` at `28mm` / `35mm` and `8,10..14` at `70mm` / `150mm`, with no tele key `15`; this excludes that tested dispatcher boundary as a positive C6 route, but not C6 globally
  8. treat the visible `src1` worker source-image producer topology as bounded too:
     static proof ties the worker source-image local to keyed helpers `0x1bdc80` / `0x1be750`, vector builder/updater `0x1be270`, ROI/source validator `0x31abd0`, wrappers `0x31af30` / `0x31acf0`, lower producers `0x33ede0` / `0x33f480`, and shared per-source iterator `0x33f180`; this proves producer topology and per-source virtual dispatch, not semantic `src1` contents or reducer closure
  8. treat the keyed-helper/vector-builder boundary as runtime-bounded too:
     complete four-zoom bridge HDR runs with `.lris` auto-loading disabled hit `0x1bdc80` / `0x1bdcfb`, always saw `0xe78e0` count `1` in summarized invocations, and had zero hits at `0x1be750`, helper lazy callsites into `0x1be270`, and direct builder sites `0x1be270` / `0x1be291` / `0x1be2fb` / `0x1be306`; tele helper keys were `5..14`, with no key `15`
  8. treat the first captured visible-`src1` source-producer virtual target as bounded too:
     gated four-zoom runtime proof shows the first captured descendant path is `0x3e3279 -> 0x31af30 -> 0x33ede0 -> 0x33f180`, reaches virtual site `0x33f3e8`, and resolves vtable address point `0x65b3c8` slot `+0x30 = 0x341770`; static inspection bounds `0x341770` to per-source region-adapter / record-update work, not reducer closure
  8. treat the follow-up lower virtual target census as bounded too:
     first-visible-`src1` gated complete runs now show `0x3e3279 -> 0x31af30` at all four zooms, `0x33f3e8` and `0x33f94f` at all four zooms, and `0x33ffd4` at `28mm` / `35mm` with zero `0x33ffd4` hits at `70mm` / `150mm` under the tested gated runs; nonzero virtual-site counts hit the probe's `512` cap, so use them only as lower bounds and capped-window target-family observations
  8. treat the `0x341770` helper branch as bounded too:
     installed-bundle proof shows `0x2e8680` validates one source-domain / Bayer-image descriptor, prepares one destination through `0xf540`, installs callback vtable `0x659fc0`, and dispatches substantive slot `0x2e8cc0`; `0x2e8cc0` is one-source 16-bit SIMD region/pixel work, not reducer, final blend, C6 routing, or acceptance closure
  8. treat the capped lower target-family visible bodies as bounded too:
     installed-bundle static proof classifies the inspected first-level target bodies and directly inspected helper callees as thunks, descriptor clipping/update, ROI/materialization, one-source Bayer/RAW region work, cache/parameter consistency, single-descriptor executor setup, selected tile/read-rescale handoff, or image/materialization helpers; do not reopen those visible bodies as already-closed reducer surfaces
  8. treat the two indirect callable targets behind `0x342ca0` and `0x344470` as bounded too:
     follow-up runtime/static proof under the first visible-`src1` gate binds `0x342d99` to `0x65b948/+0x30 = 0x342b80 -> 0x2eb560` and binds `0x3449f0` to `0x65c798/+0x30 = 0x345920 -> 0x2f53d0` across all four canonical zooms; do not reopen those dynamic target identities
  8. treat the immediate helper chain under bound target `0x2f53d0` as bounded too:
     follow-up gated runtime/static proof shows `0xab590`, `0x2f4470`, `0x2f6420`, `0x135d0`, `0x3066d0`, and postbranch `0xab590` are live in capped windows across the canonical quartet, while `0x3048b0` has zero hits under accepted gated probes; static inspection bounds the chain to validation, descriptor/vector setup, bilateral-kernel-size dispatch, callback-object dispatch through `0x5440`, and one row-executor dispatch through `0x5670`; follow-up static classification bounds the executor callback bodies to local descriptor transform / filtering / interpolation / normalization / accumulation surfaces; follow-up runtime proof under the same first-visible-`src1` gate shows `0x2f6420` selects `0x2fb320` at `0x2f67e2 -> 0x5440` across `28mm`, `35mm`, `70mm`, and `150mm`, while `0x2f78e0` and normalize sites `0x2f8584`, `0x2f859f`, and `0x2f85a5` have zero hits under that route; follow-up runtime worker proof bounds selected `0x2fb320` mechanics to same-shaped descriptor-like fields, a `vec4` coefficient pointer, and approximate reciprocal-normalized weighted `vec4` stores; public helper-field semantics, public descriptor-field names/origins, semantic `src1` contents, and reducer closure remain open
  8. next source-producer proof step:
     move beyond the capped target-family visible bodies, newly bound indirect target identities, and bounded `0x2fb320` helper mechanics toward upstream/downstream surfaces that can still expose real multi-input merge/reduction or distributed selection/acceptance math
  9. treat the visible `src2` hot state and static body boundary as bounded:
     four-zoom runtime packets show `0x3ebb80`, when reached from `0x3ecd80`, reads a tiered `PipelineCache+0x1e0` resample-state object; repo-local static proof shows `0x3ecd80 -> 0x3ebb80 -> 0x3edb80` is descriptor/state/executor orchestration through `PipelineCache+0x1e0` state, `PipelineCache+0x1d8` fallback/source-descriptor plumbing, descriptor validation, a 64-entry scalar table, generic tiled executor dispatch, and one-image normalization; follow-up runtime/static proof binds the accepted visible-`src2` executor gate callback slot across the quartet as `0x65f7e8/+0x30 = 0x3ed2e0`, a one-source descriptor resampling/materialization worker, with accepted dispatch, worker entry, and completed `10432x7824` HDR output proven across `28mm` / `35mm` / `70mm` / `150mm`; follow-up runtime/static proof bounds callback `+0x08` source-descriptor custody through `PipelineCache+0x1d8` vtable slot `+0x18 = 0x406a10` across the quartet; complete-render runtime proof bounds the branch/helper inside `0x406a10` as `28mm` / `35mm` object byte `+0x18 = 1` to `0x31b110` and `70mm` / `150mm` object byte `+0x18 = 0` to `0x31acf0`; semantic `src2` contents, public descriptor origin, public byte meaning, and merge/reducer closure remain open
  9. treat the `FusionCacheBayer+0x18` constructor-origin proof as bounded:
     static and four-zoom runtime proof now binds the `0x406a10` branch selector to `PipelineCache` construction through `0x3eab4c -> 0x406960 -> 0x4064c0 -> 0x402d20`; `28mm` / `35mm` select non-sentinel key `1`, write/read byte `1`, and construct field `+0x20`, while `70mm` / `150mm` leave sentinel `16`, write/read byte `0`, and have zero `+0x20` construction-store hits under complete bridge HDR runs; public key/sentinel semantics, scanned fields `+0x58/+0x60`, upstream collection LRI origin, optional `+0x20` semantic name, semantic `src2` contents, and merge/reducer closure remain open
  9. treat the `0x402d20` scan-loop record proof as bounded too:
     runtime proof shows wide scans `10` records and accepts key `1` as the only target-normalized record with sign-bit fields `+0x58/+0x5c = (-1,-1)`; tele scans `11` records and preserves sentinel `16` because no target-normalized record has a sign-bit pair; tele key `15` has `(-1,-1)` and post-mutation `object+0x30 = 0` but is outside this predicate's target-normalized bucket; static follow-up proves the target-normalized buckets are `0xf6c60` camera-ID group ordinals over ranges `0..4`, `5..9`, and `10..15`, and proves `0xf2770` constructs item `+0x60` plus `+0x58/+0x5c`; public `+0x58/+0x5c` semantics, item-input LRI origins, optional `+0x20` semantic name, semantic `src2` contents, and merge/reducer closure remain open
  10. treat the corrected `CalibDataProcessor::State()` `operator()` family as runtime-live:
     complete accepted four-zoom runtime census now proves all thirteen corrected bodies are live; the body list is `0x229df0`, `0x229ec0`, `0x22a0e0`, `0x22a9b0`, `0x22aaf0`, `0x22ae60`, `0x22af80`, `0x22bdf0`, `0x22bee0`, `0x22c350`, `0x22cd00`, `0x22d250`, and `0x22e1d0`, with identical full-render count pattern `(1,1,4,4,4,1,1,1,5,5,5,5,1)` at `28mm`, `35mm`, `70mm`, and `150mm`; this is entry-liveness/count proof, not return-value or reducer closure
  11. treat `0x247390` as refuted for this State-family census:
     its vtable typeinfo belongs to an adjacent `SparseLNR::markInliers(..., void(int,int,int))` callback table, not `CalibDataProcessor::State()`; the earlier `0x247380` / `0x247390` terminal-State-slot interpretation is superseded by the corrected terminal State body `0x22e1d0`
  12. treat the representative inspected operators (`0x229df0`, `0x229ec0`, `0x22bee0`) as state-materialization surfaces, not automatic reducer closure; runtime liveness for the broader family does not change that static body boundary
  13. treat terminal corrected State body `0x22e1d0` and dispatcher `0x22f0f0` as bounded away from direct reducer closure:
     installed-bundle static proof shows `0x22e1d0` iterates the integer-key vector at `this+0x20`, searches/creates keyed tree nodes under `this+0x28`, calls `0x23c5f0`, `0xe6ba0`, and `0xf33d0`, and returns State value `9`; `0x22f0f0` invokes registered State function objects through slot `+0x30`, stores returned `eax` into the current State slot at `r14+0x6c`, and can notify a callback at `r14+0xe0`; public State semantics and runtime return ordering remain open
  14. bound the first post-`State()` helper chain away from reducer closure:
     `0x224cc0`, `0x224d70`, `0x242a80`, `0x258ea0`, `0x258f00`, `0x242d00`, `0x242dc0`, `0x243770`, and `0x243870` are now bundle-proven setup/copy/reset surfaces
  13. treat `0x244560` and `0x245a40` as bounded heavy consumers, not closure points:
      installed-bundle proof now shows their visible work is still ref/src pyramid, feature-record, coordinate-pair, mask, and descriptor-block state work
  14. treat the next deeper dispatcher / selector layer as bounded too:
      `0x248580`, `0x248960`, and `0x2481a0` all wrap a shared gate at `0x2439b0`, and `0x2439b0` plus `0x241fd0` / `0x2416d0` still only count and relabel 0x2c-stride records while gating installed blocks
  15. treat the next selector-helper tranche as bounded too:
      `0x249020` is a selection-offset / permutation helper over the chosen integer index list, `0x247900` is a 32-bit vector range-copy / growth helper, and `0x249410` materializes repeated 24-byte bitset entries from a source bitset container
  16. use the visible continuation of `0x2416d0` after those helpers as another exclusion anchor:
      the code still performs bitset tests and writes `record+0x24 = 5` for accepted selected records
  17. treat `0x5670` as a generic range / chunk executor, not reducer closure
  18. treat the callback object reached after that executor path as bounded but not as State-family evidence:
      `0x241b60` installs vtable address point `0x6589e0`; the adjacent metadata identifies this as a `SparseLNR::markInliers(..., void(int,int,int))` callback table, not a `CalibDataProcessor::State()` table
  19. treat that callback's substantive `+0x30` slot as already known:
      raw vtable bytes give `0x658a10 = 0x247390`, and the callback proof bounds `0x247390` to coordinate/bitset-style state work rather than exposed pixel reduction
  20. treat the candidate block-geometry helper family as bounded too:
      `0x25c990` is two-float coordinate delta / scale, `0x25ca70` is a geometry predicate, `0x25d090` copies validated coordinate pairs into active block vectors and uses `0x25d2a0` / `0x25ca70` to validate or clear block state, and `0x25d4d0` is int-pair vector insertion / copy
  21. do not reopen this selector / block-geometry branch as the missing reducer
  22. treat the broader `0x258fe0` / `0x2598a0` feature-selection lane as bounded too:
      `0x258fe0` builds pyramid / ROI descriptor records, `0x2598a0` populates per-level feature/candidate containers and scaled coordinate output, `0x259b40` builds a downsampled feature grid and filters 0x24-byte records, `0x25a010` counts candidate records and dispatches mode-driven callback work, `0x25a360` fills 36-byte feature/candidate map records, and `0x25ab50` performs neighbor/top-candidate suppression
  23. do not reopen the visible feature-selection lane or the downstream candidate-scoring family as the missing reducer:
      under the corrected canonical four-zoom bridge HDR quartet, `28mm` and true-`35mm` reach `0x24c320` and not `0x24d610`, while `70mm` and `150mm` reach `0x24d610` and not `0x24c320`; both entry bodies consume `0x24`-stride candidate records, perform bounds / projection / local SIMD patch scoring, and write `0x2c`-stride candidate-result or sentinel records
  23. treat the candidate-scorer output custody seam as bounded too:
      `0x2481a0` installs family-A table `0x667788/+0x30 -> 0x24c2d0 -> 0x24c320` and passes the family-A scorer-context `+0x18` output vector to `0x2439b0`; `0x248580` installs family-B table `0x667808/+0x30 -> 0x24d5c0 -> 0x24d610` and passes the family-B scorer-context `+0x28` output vector to `0x2439b0`; complete four-zoom runtime proof shows the same output-vector pointer and record count at the family gate call and matched `0x2439b0` entry. This is scorer-output custody, not semantic reducer closure
  23. treat the `0x2439b0` record-state gate histogram as bounded too:
      static proof shows `0x2439b0` can promote `0x2c`-stride records from state `3` to state `4` for target `1` at `0x243b2c` or target `2` at `0x243cac`; complete four-zoom runtime proof shows `28mm` / `35mm` family-A boundary histograms unchanged, while `70mm` / `150mm` family-B runs promote target-2 records from state `3` to state `4`; exact-vector samples at `0x241fd0`, `0x2416d0`, `0x241828`, `0x2422a6`, and `0x242306` did not match the known scorer-output vector under this probe. This is boundary behavior, not public state semantics, downstream image effect, or reducer closure
  23. treat the tele promoted-record watch as bounded too:
      selected records promoted by `0x2439b0` from `(state=3,target=2)` to `(state=4,target=2)` are later read by downstream code in clean canonical `70mm` / `150mm` renders; representative watched records hit `0x2416d0`, `0x241fd0`, and local `0x2474c0..0x2476be` consumer windows, and at least one watched record per tele seed advances to `(state=5,target=2)` through `0x2416d0`. This is watched-record downstream consumer proof, not public state semantics, final image contribution, final acceptance/rejection, or reducer closure
  23. treat the tele state-5 selected-index path as bounded too:
      promoted target-2 record indices captured at `0x2439b0` enter concrete `0x2416d0` selected-index vectors under clean canonical `70mm` / `150mm` renders; the first two small captured `70mm` promoted sets and the small captured `150mm` promoted set all reach `(state=5,target=2)` stores. This is selected-index/state-relabel proof for admitted promoted sets, not public state semantics, downstream image contribution, final acceptance/rejection, or reducer closure
  23. treat the tele state-5 later-watch path as bounded too:
      watched promoted records that become `(state=5,target=2)` continue into `0x244560` and then the already-bounded `0x25d090` candidate block-geometry / active-block helper family under clean canonical `70mm` / `150mm` renders. This is later state/candidate/geometry flow, not image-effect proof, final acceptance/rejection, or reducer closure
  23. treat the four-zoom state-5 coordinate-output path as bounded too:
      `0x2457c0` is live and normally returning under clean canonical `28mm`, `35mm`, `70mm`, and `150mm` bridge HDR renders; sampled hits at the admitted `0x24593b` store-path site have `record+0x24 == 5`; and every admitted return leaves finite non-sentinel coordinate pairs in `state+0x1e8`. This is coordinate-output materialization proof, not image-effect proof, final acceptance/rejection, or reducer closure
  23. treat the four-zoom state-5 coordinate-copy-out path as bounded too:
      representative finite non-sentinel pairs emitted by `0x2457c0` into `state+0x1e8` are later read by `0xe8e70` vector-copy work under both State-helper copy-out paths: `0x224d70 -> 0x245a40` and `0x224e50 -> 0x245a20 -> 0x244560`. This is coordinate-vector custody / copy-out proof, not image-effecting non-copy use of the copied destination, final acceptance/rejection, or reducer closure
  23. treat the four-zoom state-5 coordinate-copy-destination path as bounded too:
      representative finite non-sentinel destination pairs copied out by the State-helper `0xe8e70` path are touched again by `0xe8e70` vector-copy work across clean canonical `28mm`, `35mm`, `70mm`, and `150mm` bridge HDR renders. The admitted later caller frames include State-helper recopy sites plus higher node-vector materialization/copy sites at `0x22a61a -> 0xe8e70 -> 0x22a61f` and `0x22c93a -> 0xe8e70 -> 0x22c93f`. This is coordinate-vector custody / propagation proof, not image-effecting non-copy use of the propagated destination vectors, final acceptance/rejection, or reducer closure
  23. treat the four-zoom state-5 coordinate-node-destination non-copy consumer as bounded too:
      representative finite non-sentinel destination pairs copied into the `0x22a61a -> 0xe8e70 -> 0x22a61f` node-vector destination reach non-copy candidate/index/scoring-selection code under `0x21b2e0` and its `0x21c4f0` callback path across clean canonical `28mm`, `35mm`, `70mm`, and `150mm` bridge HDR renders. The admitted capped window followed at least one finite node-destination pair per run; the sibling node-vector copy site `0x22c93a -> 0x22c93f` had zero observed call/return hits in this proof. This is representative non-copy candidate/index/scoring-selection consumption proof, not image-effect proof, final acceptance/rejection, or reducer closure
  24. treat the first visible payload runtime surfaces beneath `0x3e27a0` as bounded too:
      constructor-written address point `0x65f140` exposes destructor slots `0x3e53a0` / `0x3e54c0` and substantive visible virtual slot `0x3e2dc0`; `0x3d0120` and `0x3e55f0` are callable-slot plumbing; `0x3e2dc0` fans out per-level setup through `0x40b370` and quantizes one setting through `0x40b330`; `0x3e2e90` is a single-object single-level ROI/process body with optional `0x3e3f90` ROI adjustment and per-level subobject selection at `this + 0x210 + level * 0xa0`
  25. treat the deeper `0x3e2e90` callback worker as bounded too:
      `0x3e2e90` installs callback address point `0x65f408`; raw vtable bytes show slot `+0x30 = 0x3e4c50`; `0x3e4c50` is a row/range worker that projects each output sample through one callable, samples one source image/cache buffer through a 4x4 SIMD neighborhood, consumes a 64-entry cubic weight table, and writes one `vec4f` output per sample; four-zoom runtime proof now ties the first captured worker packet to visible `src1` and identifies the next projection callable slot as `0x3e42e0`
  26. treat the owner cache-selection layer as bounded too:
      the owner builds `+0x6a8/+0x6b0`, constructs `+0x688/+0x690` through `0x3eaf00 -> 0x3ea7d0` using `&owner+0x6a8` and `&owner+0x678`, selects `owner+0x688` or `owner+0x6b8` at `0x3b0740`, and dispatches the selected cache through `0x3d0650` one-cache level/ROI read and rescale work
  27. treat the constructor-side owner `+0xf0` callable-install custody edge as bounded too:
      `0x3ea980 -> 0x3d0120` installs stack callable address point `0x65f5e0` into target inline callable storage, `0x65f5e0/+0x30 = 0x3ec960`, and the same complete four-zoom bridge HDR runs reach the already-bounded owner `+0xf0` sink body; this is custody proof, not reducer closure or final output semantics
  27. treat the owner alternate-cache/helper setup layer as bounded too:
      optional `+0x698/+0x6a0` is constructed through `0x3d8b70 -> 0x3d8780` from `+0x6a8` and `+0x678`, `+0x6b8/+0x6c0` is constructed through `0x3f06f0 -> 0x3f04d0` from `+0x688`, `+0x698`, and a `0x18`-byte state block, the inspected `+0x6b8` address-point slots are cleanup/destructor/delete slots, and the inspected `+0x678` constructor prefix overlaps known `state+0x448` tree/control setup
  28. treat the visible `+0x678/+0x680` constructor and immediate runtime surfaces as bounded too:
      `0x3f2c40` completes through keyed-record construction, `state+0x448` payload writes already bounded elsewhere, six descriptor/layer seeds through `0x27abb0` / `0x267e80`, `0x267fb0` start/upsample object push, `0x225160` construction at `this+0x280`, and bitset storage; `0x3f75e0`, `0x3f7a40`, `0x3f7b20`, `0x3f7c00`, and `0x3f7ec0` are stop/cleanup, level-gate, callable-slot replacement, byte-count, and record/buffer-materialization surfaces
  29. treat the selected layer virtual targets and the first record/buffer consumer as bounded too:
      `0x267e80`'s selected `+0x40` target is `0x26bbd0`, a `this+0x8` setter; `0x267fb0`'s selected `+0x40` target is `0x26a920`, a `this+0x30` setter; the selected `+0x90` targets through `0x268480` / `0x2684a0` are pointer accessors `0x26fb50` and `0x26b590`; and `0x3f8b30` consumes/writes `0x3f7ec0` materialized record/buffer output
  30. treat the `StereoLayer<false>::runPass(int)` action path as bounded away from reducer closure:
      vtable `0x667cc8` slot `+0x30` is `0x276790`; `0x276790` dispatches to `0x276860` when `this+0xc == 8` and to `0x277e70` otherwise; both worker bodies call the `0x275630` per-tile state builder and then route to `0x2730c0` / `0x2732f0`; `0x2732f0` is a projection/sampling cost body
  31. preserve the runtime scope:
      the canonical four-zoom bridge HDR quartet all reached `0x276790 -> 0x276860 -> 0x275630 -> 0x2732f0` with sampled `layer+0xc == 8`; `0x277e70` and `0x2730c0` had zero hits only under those tested full-render conditions
  32. treat the sibling `StereoLayer<false>::compute()` cost path as bounded away from reducer closure under the tested path:
      installed table `0x667c28` has operator wrapper `0x274b10`, which tail-jumps to worker `0x2727f0`; static inspection shows `0x2727f0` shares the `0x275630` / `0x2730c0` / `0x2732f0` projection-cost family, while complete no-auto-LRIS bridge HDR probes across all four canonical focal lengths recorded zero hits for `0x274b10`, `0x2727f0`, `0x272100`, `0x272640`, and their probed callsites
  33. treat the high-address `HigherWarpDebug::renderDebugView` helper-caller surface as bounded away from the tested bridge HDR path:
      installed callback tables `0x6609e8` and `0x660a68` are tied to `HigherWarpDebug::renderDebugView` names; complete no-auto-LRIS bridge HDR probes across all four canonical focal lengths record zero hits for `0x42c140`, `0x42cb35`, `0x42cb5d`, `0x42cb9c`, `0x42cbc2`, `0x42cc5a`, `0x4300f0`, and `0x430240`, while live controls at `0x3e05f5` and `0x3eb72d` hit five times per tier
  34. treat the sampled prefusion callable gate as bounded away from reducer closure:
      `state+0x220` is an inline `state+0x200` callable; constructor vtable `0x6673f0` has false-return slot `+0x30 = 0x230220`; four-zoom bridge HDR runtime samples at `0x24200d`, `0x24459b`, `0x24477b`, `0x245b29`, `0x24c34f`, and `0x24d64e` used vtable `0x66b0f0`, false-return slot `+0x30 = 0x230640`
  35. do not close the blocker on those payload runtime / worker / owner cache-selection / owner alternate-cache setup / visible `+0x678` constructor-runtime / selected virtual / record-consumer / `StereoLayer<false>::runPass` cost-path / sibling `StereoLayer<false>::compute` cost-path / scoped debug-view exclusion / callable-gate / candidate-scoring surfaces either
  36. do not reopen the `0x3e42e0` constructor field-pack branch as the missing reducer:
      it is now bounded to keyed-record/object field-pack production plus radius-table generation, with public calibration names and LRI field origins still unproven
  37. next move beyond the projection/field-pack branch toward surfaces that can still expose real multi-input merge/reduction or distributed selection/acceptance math
  38. only close the blocker once a downstream surface is proven to have real N-to-1 input shape or equivalent distributed merge/reduction math
- Why this is a real path:
  the construction sites, installed wrapper vtables, visible wrapper bodies, post-wrapper `initResAmp` per-key record/wrapper branch, visible `PipelineCache+0x270` per-key wrapper read path, concrete backing fields, visible `src1` read path, visible `src1` payload provenance and live constructor path, visible `src1` versus direct-contributor payload-family and secondary-callable splits, visible `src1` secondary-callable live handoff, visible `src1` worker/projection-record handoff and `0x3e42e0` coordinate-transform target, `0x3e42e0` field-pack producer, visible `src1` source-image producer topology, the first captured visible-`src1` source-producer virtual target `0x341770`, the capped lower virtual target census, the capped target-family visible-body static classification, the `0x341770` helper `0x2e8680` / callback `0x2e8cc0`, visible `src2` source-descriptor branch plus constructor-origin and scan-loop selector custody, first visible payload runtime/config/process surfaces, the deeper `0x3e2e90` / `0x3e4c50` single-source projection/resampling worker, owner cache-selection / read-rescale layer, owner alternate-cache/setup layer, visible `+0x678` constructor/runtime surfaces, selected layer virtual setters/accessors, the `0x3f7ec0` record-consumer path, the now-bounded `StereoLayer<false>::runPass` projection/sampling cost path, the sibling `StereoLayer<false>::compute` cost path, the scoped `HigherWarpDebug::renderDebugView` helper-caller exclusion, the sampled prefusion callable gate, upstream `CalibDataProcessor::State()` `operator()` family runtime liveness, state-materialization nature of representative operators, downstream selector/callback branch, block-geometry helper family, visible feature-selection lane, downstream candidate-scoring family, candidate-scorer output-vector custody, `0x2439b0` record-state boundary behavior, the tele promoted-record downstream consumer path, the tele state-5 selected-index/state-relabel path, the tele state-5 later-watch path into `0x244560 -> 0x25d090`, the four-zoom `0x2457c0 -> state+0x1e8` coordinate-output materialization path, the four-zoom `state+0x1e8 -> 0xe8e70` coordinate-vector copy-out custody path, and the four-zoom copied-destination coordinate-vector propagation path through additional `0xe8e70` State-helper/node-vector materialization copy sites are now all known in the shipped bundle, so the remaining unknown is narrower than the older generic "somewhere behind `src1` / `src2`" story
- Source trail:
  [lldb_iramp_wrapper_accumulator_four_zoom.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/lldb_iramp_wrapper_accumulator_four_zoom.md), [lldb_src_lookup_and_src2_state_four_zoom.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/lldb_src_lookup_and_src2_state_four_zoom.md), [bundle_proof_fusioncachebayer_flag_origin_static.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_fusioncachebayer_flag_origin_static.md), [lldb_fusioncachebayer_flag_origin_four_zoom.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/lldb_fusioncachebayer_flag_origin_four_zoom.md), [lldb_fusioncachebayer_scan_collection_four_zoom.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/lldb_fusioncachebayer_scan_collection_four_zoom.md), [lldb_src1_contributor_payload_family_four_zoom.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/lldb_src1_contributor_payload_family_four_zoom.md), [bundle_lldb_src1_contributor_secondary_callable_families.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_lldb_src1_contributor_secondary_callable_families.md), [lldb_src1_secondary_callable_live_four_zoom.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/lldb_src1_secondary_callable_live_four_zoom.md), [lldb_src1_worker_projection_record_four_zoom.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/lldb_src1_worker_projection_record_four_zoom.md), [bundle_proof_src1_projection_callable_transform.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_src1_projection_callable_transform.md), [bundle_proof_src1_projection_field_pack_producer.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_src1_projection_field_pack_producer.md), [bundle_proof_src1_source_image_producer_topology.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_src1_source_image_producer_topology.md), [lldb_src1_visible_gated_virtual_four_zoom.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/lldb_src1_visible_gated_virtual_four_zoom.md), [lldb_src1_virtual_target_census_four_zoom.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/lldb_src1_virtual_target_census_four_zoom.md), [bundle_lldb_src1_virtual_target_family_static.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_lldb_src1_virtual_target_family_static.md), [bundle_proof_src1_region_adapter_helper_2e8680.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_src1_region_adapter_helper_2e8680.md), [lldb_src1_payload_constructor_live_four_zoom.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/lldb_src1_payload_constructor_live_four_zoom.md), [bundle_proof_src_wrappers.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_src_wrappers.md), [bundle_proof_initresamp_post_wrapper_records.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_initresamp_post_wrapper_records.md), [bundle_proof_initresamp_per_key_wrapper_read_path.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_initresamp_per_key_wrapper_read_path.md), [bundle_proof_src1_visible_read_path.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_src1_visible_read_path.md), [bundle_proof_src1_payload_provenance.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_src1_payload_provenance.md), [bundle_proof_src1_payload_runtime_surfaces.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_src1_payload_runtime_surfaces.md), [bundle_proof_src1_project_roi_worker.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_src1_project_roi_worker.md), [bundle_proof_src1_owner_cache_selection.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_src1_owner_cache_selection.md), [bundle_proof_owner_f0_constructor_install_four_zoom.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_owner_f0_constructor_install_four_zoom.md), [bundle_proof_src1_alternate_cache_setup.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_src1_alternate_cache_setup.md), [bundle_proof_src1_678_constructor_runtime_surface.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_src1_678_constructor_runtime_surface.md), [bundle_proof_src1_678_virtuals_and_record_consumer.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_src1_678_virtuals_and_record_consumer.md), [bundle_proof_stereolayer_runpass_cost_path_four_zoom.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_stereolayer_runpass_cost_path_four_zoom.md), [bundle_proof_stereolayer_compute_cost_sibling_four_zoom.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_stereolayer_compute_cost_sibling_four_zoom.md), [bundle_proof_higherwarpdebug_renderdebugview_four_zoom.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_higherwarpdebug_renderdebugview_four_zoom.md), [bundle_proof_prefusion_callable_gate_four_zoom.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_prefusion_callable_gate_four_zoom.md), [bundle_proof_prefusion_candidate_scoring_family_four_zoom.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_prefusion_candidate_scoring_family_four_zoom.md), [bundle_proof_calibdataprocessor_lambda_family.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_calibdataprocessor_lambda_family.md), [bundle_proof_prefusion_state_helper_chain.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_prefusion_state_helper_chain.md), [bundle_proof_prefusion_heavy_consumers.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_prefusion_heavy_consumers.md), [bundle_proof_prefusion_dispatch_gate.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_prefusion_dispatch_gate.md), [bundle_proof_prefusion_selection_helpers.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_prefusion_selection_helpers.md), [bundle_proof_prefusion_callback_reuses_known_runner.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_prefusion_callback_reuses_known_runner.md), [bundle_proof_prefusion_block_geometry_helpers.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_prefusion_block_geometry_helpers.md), [bundle_proof_prefusion_feature_selection_lane.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_prefusion_feature_selection_lane.md), `image_resolution_amp_verification.md`, `iramp_camera_identity.md`, `composite_producer.md`
- Additional recent source trail:
  [bundle_lldb_prefusion_candidate_output_custody_four_zoom.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_lldb_prefusion_candidate_output_custody_four_zoom.md), [bundle_lldb_prefusion_record_state_gate_histogram_four_zoom.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_lldb_prefusion_record_state_gate_histogram_four_zoom.md), [bundle_lldb_prefusion_promoted_record_watch_tele.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_lldb_prefusion_promoted_record_watch_tele.md), [bundle_lldb_prefusion_state5_selected_index_path_tele.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_lldb_prefusion_state5_selected_index_path_tele.md), [bundle_lldb_prefusion_state5_later_watch_tele.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_lldb_prefusion_state5_later_watch_tele.md), [bundle_lldb_prefusion_state5_coord_output_four_zoom.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_lldb_prefusion_state5_coord_output_four_zoom.md), [bundle_lldb_prefusion_state5_coord_consumer_watch_four_zoom.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_lldb_prefusion_state5_coord_consumer_watch_four_zoom.md), [bundle_lldb_prefusion_state5_coord_copy_dest_watch_four_zoom.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_lldb_prefusion_state5_coord_copy_dest_watch_four_zoom.md)

## 2. Exact producer-side transform record semantics over the ROI-derived pair lattice

- Related blockers:
  `CLM-WARP-001`, `CLM-WARP-002`, `CLM-WARP-003`
- Resolution path exists:
  Yes
- Current best recorded path:
  installed-bundle proof now closes the basic pair-grid role: the first lattice is ROI-derived, a second same-sized lattice is written from it through per-level transform state, `PipelineCache+0x258` is the live paired transform / warpfield-record vector feeding that second lattice, the consumer-side second-grid transform formula is proven, the producer-side dispatcher and row/map writer split are proven, the post-wrapper `initResAmp` insertion path normalizes `+0x48/+0x4c` to `(1.0, 1.0)`, the row producer is bounded to the 4x4 double matrix formula `source_b_product * inverse(source_a_product)`, the immediate source-record constructor topology is bounded through `0x3faed0` / `0x3fb1a0`, `state+0x448` is bounded as a keyed tree/control object with a first visible byte-gated insertion loop and later direct payload writes through `+0x80`, the tracked map-provider runtime path into `record+0x40` is bound across the canonical quartet, accepted `28mm`, `35mm`, and `70mm` writer-core probes bind `0x26ac13 -> 0xf340` to `UpsampleLayer+0x90` descriptor population, four-zoom builder probes bind `0x26aa10 -> 0x29ed90 -> 0x2673a0 -> 0x26ac13` as the depth-path builder for that descriptor, four-zoom worker/custody probes bind the `0x29ed90` worker formula and its low-resolution float source descriptor to `StereoLayer<false>` index `5` descriptor `+0x2a8`, and `0x374ac0` handles the later clipped block. The remaining unknown is the producer-side row/map public calibration semantics, LRI origins, and public semantic names for the internally bounded depth/map descriptors.
- Path:
  1. start at `libcp+0x365efe..0x365f4b`, where the non-empty-ROI path sizes the output from the ROI rect and passes that same rect into `0x3661b0`
  2. keep the proven writer loop `libcp+0x366500..0x366553`, where the first packed int32 lattice is emitted from the scaled ROI rect on the 8-pixel step
  3. tie those vector record fields back through the live call chain:
     `processLevel0` passes `PipelineCache+0x258` into `0x365960`; `0x365960` validates its count against `PipelineCache+0x270` and packs it into closure `+0x20`; `0x3661b0` consumes one `0x50` record per source-vector index
  4. follow `libcp+0x366b40..0x366dc7`, where the second same-sized pair grid is allocated and filled from the first through the per-level image / map / vector record fields
  5. use the proven consumer formula at `libcp+0x366c96..0x366d59` as the target behavior while decoding the producer-side record fields:
     `H = (m*u)*R0 + (m*v)*R1 + m*R2 + R3`, output `(H[0]/H[2], H[1]/H[2])`, acceptance `[-8, dim + 7)`, then add `0.5` and truncate to int32
  6. use the proven producer dispatcher and composer path as the remaining decode spine:
     `0x3f7040` dispatches same-category records to `0x3f70d0` and cross-category records to `0x3f72f0`; both converge on `0x25e500`; `0x25e500` calls `0x25e0c0` for rows and stores the caller-supplied map pointer at `+0x40`; `0x25e560` / `0x25e590` are reciprocal helpers over `1.0`
  7. use the proven row matrix chain:
     `0x25ec70` is 4x4 double matrix multiply; `0x25e0c0` builds two homogeneous-style 4x4 matrices from each source record, multiplies each pair, runs `0x9db20` to invert the first source product, multiplies the second source product by that inverse, and writes the final matrix as output row floats
  8. use the proven constructor topology:
     `0x3faed0` combines a keyed `state+0x448` node record with a keyed `state+0xe0` object-derived record through `0x23faf0`; `0x3fb1a0` does the cross-category form plus fixed helper adjustments through `0x264460` and `0x264980`; `state+0x448` is initialized as a keyed tree/control object and its first visible insertion path is gated by `object+0x30`; four-zoom runtime proof for the tracked post-wrapper producer entries binds `0x268480` to `UpsampleLayer` vtable address point `0x658eb0`, slot `+0x90 = 0x26b590`, `0x26b590` returning `UpsampleLayer+0x90`, and that return written to `record+0x40`; accepted `28mm`, `35mm`, and `70mm` writer-core probes bind `0x26ac13 -> 0xf340` to a populated `4160 x 3120`, stride-`4160` descriptor copied into `UpsampleLayer+0x90`, while accepted `150mm` runtime proves provider/storage for that descriptor without writer-body instrumentation; four-zoom builder probes bind `0x26aa10` to a previous-layer `+0x90` `2080 x 1560` descriptor, `0x29ed90`, a `4160 x 3120` output descriptor, `0x2673a0`, and final `0xf340` move into `UpsampleLayer+0x90`
  9. use the proven helper bodies:
     `0x23faf0` copies the right source record into `dst`, performs SIMD source-record composition using the left record and current output fields, applies three `0x264980` / `0x264460` adjustment pairs, and returns `dst`; `0x264980` is a two-axis field-shift helper over six scalar fields; `0x264460` is a positive two-axis scale helper with an exact `(1.0, 1.0)` copy fast path
  10. use the proven object lookup / accessor path:
     `state+0xe0` resolves through `0x1be970` / `0xe6ba0` into a shared image-like object by two integer object fields; `0xf34e0` selects one of two `CalibStage` banks at object `+0x12c` or `+0x180`; `0xf3350` returns `object+0x10c`; `0xf3360` delegates through `object+0xa0` to an owner tree lookup keyed by `object+0x60`
  11. continue decoding public calibration semantics, LRI origins, and public semantic names for the structurally bounded `state+0x448` payload fields through `+0x80`, the semantic name of `object+0x30`, the `state+0xe0` object banks, the internally depth-labeled `UpsampleLayer+0x90` map now proven to be stored at `record+0x40`, and the `StereoLayer<false>` index-5 descriptor now proven as the low-resolution source consumed by `0x29ed90`
  12. use `libcp+0x366dff..0x366f1c` plus `libcp+0x374ac0` to pin down how transformed-pair bbox, clipping, and zero-fill handling are applied
- Why this is a real path:
  the unknown is no longer pair existence, writer location, basic lattice role, consumer formula, dispatcher branch, row/map writer split, final scale-field normalization in the post-wrapper insertion path, row production through `source_b_product * inverse(source_a_product)`, immediate constructor/map-provider topology, the tracked map-provider runtime target/value path into `record+0x40`, `UpsampleLayer+0x90` descriptor custody, the four-zoom `0x29ed90` builder handoff, the `0x29ed90` worker formula, the low-resolution source descriptor custody through `StereoLayer<false>` index `5`, helper bodies at `0x23faf0`, `0x264980`, and `0x264460`, `state+0x448` control-object initialization / first insertion gate / later direct payload writes through `+0x80`, or `state+0xe0` object lookup / helper accessor mechanics; it is now the exact public calibration semantics, LRI origins, and public semantic names inside an already-located producer path
- Source trail:
  [LUMEN_APP_PROOF_ONLY_AUDIT.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/LUMEN_APP_PROOF_ONLY_AUDIT.md), [bundle_proof_pair_grid_roi_transform.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_pair_grid_roi_transform.md), [bundle_proof_iramp_live_signature_and_warp_records.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_iramp_live_signature_and_warp_records.md), [bundle_proof_iramp_pair_grid_transform_formula.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_iramp_pair_grid_transform_formula.md), [bundle_proof_iramp_record_producer_scale_and_dispatch.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_iramp_record_producer_scale_and_dispatch.md), [bundle_proof_iramp_row_composition_matrix_chain.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_iramp_row_composition_matrix_chain.md), [bundle_proof_iramp_9db20_matrix_inverse.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_iramp_9db20_matrix_inverse.md), [bundle_proof_iramp_source_record_constructors.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_iramp_source_record_constructors.md), [bundle_proof_iramp_23faf0_composition_helper.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_iramp_23faf0_composition_helper.md), [bundle_proof_iramp_calib_object_accessors.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_iramp_calib_object_accessors.md), [bundle_proof_iramp_state_448_tree_builder.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_iramp_state_448_tree_builder.md), [bundle_proof_iramp_state_448_later_payload_writes.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/bundle_proof_iramp_state_448_later_payload_writes.md), [lldb_iramp_map_provider_four_zoom.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/lldb_iramp_map_provider_four_zoom.md), [lldb_stereolayer_index5_depth_descriptor_custody.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/lldb_stereolayer_index5_depth_descriptor_custody.md)

## 3. Full merge topology beyond the four-zoom-proven accumulator, entry signature, and direct contributor identity

- Related blockers:
  `CLM-MERGE-002`, `CLM-MERGE-003`, `CLM-MERGE-004`, `CLM-PREFUSION-001`
- Resolution path exists:
  Yes
- Nature of the path:
  this is now a validation-and-extension path beyond the already four-zoom-proven accumulator, IRAMP entry-signature, and direct contributor-vector identity surfaces, not a single mystery VA
- Path:
  1. use the canonical four-zoom LRIs from [VALIDATION_POLICY.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/corpus/VALIDATION_POLICY.md)
  2. preserve `CLM-MERGE-002` as already four-zoom proven for narrow accumulator participation
  3. preserve `CLM-MERGE-003` as already four-zoom proven for narrow IRAMP entry signature
  4. preserve `CLM-MERGE-004` as already four-zoom proven for narrow direct contributor-vector identity
  5. complete the dependent remaining pieces: `src1` / `src2` contents, C6 routing, and final acceptance / rejection stages
  6. only then promote full four-zoom merge topology into the spec
- Why this is a real path:
  the project already has a fixed canonical quartet and a fixed validation matrix; the remaining work is closure of open zoom cells, not discovery of an entirely unknown subsystem
- Important limitation:
  this blocker also depends on blocker #1, because full topology is not closed until `src1` / `src2` are decoded
- Source trail:
  [VALIDATION_POLICY.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/corpus/VALIDATION_POLICY.md), [lldb_iramp_wrapper_accumulator_four_zoom.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/lldb_iramp_wrapper_accumulator_four_zoom.md), [lldb_iramp_entry_signature_four_zoom.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/lldb_iramp_entry_signature_four_zoom.md), [lldb_iramp_contributor_identity_four_zoom.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/lldb_iramp_contributor_identity_four_zoom.md), `image_resolution_amp_verification.md`, `iramp_camera_identity.md`, [CLAIM_LEDGER.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/canonical/CLAIM_LEDGER.md)

## 4. Tele odd-camera routing, especially C6

- Related blockers:
  `CLM-C6-001`, `CLM-CCM-001`, `CLM-ZOOM-002`
- Resolution path exists:
  Yes
- Current best recorded paths:
  direct runtime packets now prove C6 fires in the tested tele LRIs but is absent from the five-item IRAMP contributor vector at both `70mm` and `150mm`; the visible-`src1` keyed helper / vector-builder boundary is now excluded as a positive C6 route under complete bridge HDR runs because tele keys there are `5..14`, with no key `15`; the visible-`src1` projection field-pack dispatcher boundary is also excluded because tele keys there are `8,10..14`, with no key `15`; constructor/watchpoint proof shows key `15` is constructed active at item `+0x30 = 1` by `0xf2770` and later cleared at `libcp+0x3c90a5`; a focused 24-site direct `0xf2720` route census proves key `15` is queried while active at key-list helper sites `0x1bdbab` / `0x1bdbdd` and mutation-body sites, and follow-up identity proof ties those active observations, the `0x3c90a5` store, immediate inactive `0x3c90a9` state, and later inactive `0x3b2143` observation to the same tracked key-15 item pointer per tele run; static inspection classifies helper `0x1bdb60` as key-list construction bookkeeping; a remaining-direct `0xf2720` census covers the other 34 static direct sites, finds additional active key-15 observations only at constructor-adjacent key/container/tree materialization surfaces, and together with the focused proof covers all 58 static direct `call 0xf2720` sites under the canonical tele bridge HDR runs; a post-mutation state-consumer probe proves the immediate caller path consumes the constructed `ctx+0xa0` object, observes key `15` inactive in the eleven-entry item walk, writes state fields `+0x0 = 3` and `+0x4 = 1` to context `+0xc8`, and queues context `+0x4b0 = 5`; a downstream rect-vector probe proves that immediate state is reread, `0x40b0e0` returns `0`, fallback `0x3c8c00` runs, `context+0x4b0 = 5` is passed into `0x3c8d00`, and a five-entry vector of 16-byte integer tuples is returned in both canonical tele seeds; a four-zoom follow-up proves the rect-vector route is consumed into `context+0x4c0` delta-dimension pairs passed to `0x3982b0` ImagePyramid construction; a further four-zoom follow-up proves the caller immediately iterates those five ImagePyramid levels and invokes direct zero-fill callsite `0x3b2f54 -> 0xf7c0` once per level with descriptors satisfying `stride_pixels == width`; a downstream-candidate liveness probe re-hits that zero-fill route across the canonical quartet and records zero hits at selected later static `context+0x538` candidate families; a representative hardware data-watch probe arms read/write watchpoints after `0x3b2f59` on selected zero-filled ImagePyramid byte ranges and records zero hits before clean completion; an expanded tele data-watch grid arms first/middle/last 8-byte read/write watchpoints across all five zero-filled ImagePyramid levels at `70mm` and `150mm` and records zero hits before clean completion in all 30 admitted cells; a candidate downstream context-consumer probe excludes `0x3c9540 -> 0xe6c30` under complete canonical tele bridge HDR renders by zero hits while the constructor/mutation custody sites hit; the direct payload candidate loop immediately upstream of that dispatcher proves key `15` is present but filtered by post-mutation `object+0x30 = 0` before class compare and dispatcher call; the stereo-side keyed-record loop inside `0x3f2c40` proves key `15` is present but filtered by post-mutation `object+0x30 = 0` before both tested `0xf2720` getter callsites; selected-field watchpoints now prove pre-output watched-range reads at `+0x60` and adjacent `+0x64`, with cleanup-only stops for watched pair/type ranges; the remaining paths are later write/read behavior outside the watched representative/tele-grid byte ranges or through unprobed helpers/aliases and final C6 image/merge effect of the zero-filled ImagePyramid/geometry route, untested fields/aliases, final effect of the watched `+0x60..+0x67` reads, alternate routes, or proof that the mutation/filter/zero-fill chain is terminal for bridge HDR
- Additional same-byte active-byte boundary:
  a post-mutation read/write watchpoint on the tracked key-15 `item+0x30` byte arms at immediate inactive state `0x3c90a9`; complete canonical `70mm` and `150mm` bridge HDR renders both record 18 later stops, every stop still sees `item+0x30 = 0`, and the stopped libcp sites include active-byte gates outside the direct `0xf2720` inventory. This narrows same-byte reactivation and later active-byte consumers; it does not close other fields, aliases, whole-object terminality, or final image effect. Remaining "non-`0xf2720` route" language here means image-effecting route surfaces outside the now-observed same-byte active-byte stops.
- Additional selected-field boundary:
  a selected-field post-mutation read/write watchpoint on the same tracked key-15 item arms at immediate inactive state `0x3c90a9` and watches `item+0x30`, `item+0x58..0x5f`, `item+0x60..0x67`, and `item+0x100..0x107`; complete canonical `70mm` and `150mm` bridge HDR renders both complete and write output. The watched `+0x60..+0x67` range is read before output at `0xf2727` (`+0x60`) and `0xf3327` (`+0x64`), while the watched pair range and watched type/adjoining range record only allocator-cleanup stops after output write. This narrows selected fields; it does not close untested fields, aliases, final effect of the watched `+0x60..+0x67` reads, whole-object terminality, or final image effect.
- Path A: dispatcher / filter-site route
  1. do not treat `0x3f6170` / `0x3f6200` / `0x3f6940` as the positive C6 route under canonical bridge HDR; it has now been tested and key `15` is absent
  2. preserve the now-proven constructor fact: key `15` / C6 is born active at item `+0x30 = 1` in the tested `0xe59a4 -> 0xf2770` path
  3. preserve the now-proven mutation fact: key `15` / C6 is cleared to item `+0x30 = 0` at `libcp+0x3c90a5` inside body `0x3c8f90` when the grouped context `+0x44` value is not group ordinal `2`
  4. preserve the direct `0xf2720` census plus mutation-identity, same-byte watchpoint, and selected-field watchpoint facts: key `15` is queried while active at `0x1bdbab` / `0x1bdbdd` key-list helper sites, at mutation-body sites, and at constructor-adjacent key/container/tree materialization helper sites; those focused observations are the same tracked item pointer that is later cleared at `0x3c90a5`; helper `0x1bdb60` itself is key-list construction bookkeeping; all 58 static direct `call 0xf2720` sites now have admitted tele runtime census coverage; a post-mutation watchpoint on the same tracked `item+0x30` byte records only `0` at 18 later stops per tele render; and selected-field watchpoints show pre-output `+0x60/+0x64` reads plus cleanup-only watched pair/type ranges. This does not prove downstream image contribution, untested-field/alias absence, alternate route absence, final effect of watched `+0x60..+0x67` reads, or terminality
  5. preserve the now-proven post-mutation state, rect-vector, ImagePyramid, and zero-fill facts: the caller writes context `+0xc8` fields `+0x0 = 3` / `+0x4 = 1`, queues context `+0x4b0 = 5`, immediately rereads both, runs fallback `0x3c8c00`, passes `r8d = 5` into `0x3c8d00`, receives a five-entry vector of 16-byte integer tuples at both tele tiers, derives five `context+0x4c0` delta-dimension pairs, passes them to `0x3982b0` to build a five-level `CIAPI::ImagePyramid`, then iterates all five levels and passes full-image descriptors to direct zero-fill callsite `0x3b2f54 -> 0xf7c0`
  6. preserve the now-proven direct candidate gate fact: key `15` reaches the `0x3e0330` loop at both tele tiers, but post-mutation `object+0x30 = 0` and the key skips before active-pass, class-compare, cross-category, and `0x3e05f5 -> 0x3f6170`
  7. preserve the now-proven stereo-side keyed-record gate fact: key `15` reaches the `0x3f2c40` / `0x3f30a0` loop at both tele tiers, but post-mutation `object+0x30 = 0` and the key skips before the post-gate path and before both tested `0xf2720` getter callsites
  8. preserve the expanded tele hardware read/write watchpoint grid fact: after `0x3b2f59`, first/middle/last 8-byte ranges across all five zero-filled ImagePyramid levels at `70mm` and `150mm` have zero later watchpoint hits; remaining ImagePyramid data-touch work is outside those watched ranges or requires structural alias/consumer proof
  9. trace C6 behavior not covered by the same-byte and selected-field watchpoints: untested fields, object aliases, pre-mutation fan-out, final effect of the watched `+0x60..+0x67` reads, and alternate image-effecting routes; otherwise prove the mutation plus tested filters/zero-fill path is terminal for bridge HDR
- Path B: upstream image-path route
  1. enumerate `SourceImageCache` / pre-RIC fan-out surfaces for the tele path
  2. skip the now-excluded `0x1bdc80` / `0x1be750` / `0x1be270` helper boundary and the now-excluded `0x3f6170` / `0x3f6200` / `0x3f6940` dispatcher boundary unless a non-bridge profile reopens them; treat the `0x1bdbab` / `0x1bdbdd` active key-list helper observations as a real key-query surface whose helper body is bookkeeping, not a positive image route; treat the `0x3c90a5` mutation, `0x3e0330` direct candidate loop, and `0x3f2c40` stereo-side keyed-record loop as proven filter/mutation points; determine whether C6 produces an upstream buffer that is later consumed elsewhere before mutation, or whether it is dropped before RIC/IRAMP
  3. cross-check the result against the recovered upstream anchor path once blocker #1 is decoded
- Why this is a real path:
  current records already exclude two plausible visible-`src1` boundaries, prove C6's constructor-active state, prove active key-list bookkeeping and constructor-adjacent key/container/tree helper query surfaces, cover all 58 direct `0xf2720` callsites under admitted tele runtime census, prove same-byte inactive custody, prove selected-field custody for watched pair/key-adjacent/type-adjacent ranges, prove the immediate post-mutation state-classification consumer, prove the immediate downstream rect-vector path for that state, prove that rect-vector route's ImagePyramid consumer and immediate zero-fill callsite, exclude selected later static `context+0x538` candidate families, exclude representative and tele-grid watched byte ranges after zero-fill, exclude the specific candidate consumer `0x3c9540 -> 0xe6c30`, and prove concrete direct and stereo-side candidate/keyed-record filter points, keeping the search bounded to untested fields/aliases, final effect of watched `+0x60..+0x67` reads, unprobed byte ranges/aliases for later write/read behavior and final image/merge effect of the zero-filled ImagePyramid/geometry route, alternate fan-out before mutation, alternate routes, or terminal-filter proof before the observed merge surfaces
- Same-byte and selected-field narrowing:
  the post-mutation active-byte watchpoint now shows the tracked key-15 `item+0x30` byte remains `0` at all observed later byte consumers in complete tele renders, including active-byte gates outside the direct `0xf2720` census. The selected-field watchpoint then shows pre-output reads of watched `+0x60..+0x67` at true key field `+0x60` and adjacent field `+0x64`, with cleanup-only stops for watched `+0x58..+0x5f` and `+0x100..+0x107` ranges. Remaining C6 work must therefore target untested fields, aliases, image-buffer custody, unprobed byte ranges, final effect of the watched `+0x60..+0x67` reads, alternate route surfaces, or final terminal-filter proof rather than merely rechecking whether this same active byte flips back to `1` under the canonical tele bridge HDR path.
- Important limitation:
  final closure here is partly downstream of blocker #1 because both efforts depend on the same upstream anchor-path decode
- Source trail:
  [lldb_iramp_contributor_identity_four_zoom.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/lldb_iramp_contributor_identity_four_zoom.md), [lldb_src1_keyed_helper_and_builder_boundary_four_zoom.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/lldb_src1_keyed_helper_and_builder_boundary_four_zoom.md), [lldb_src1_projection_field_dispatcher_four_zoom.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/lldb_src1_projection_field_dispatcher_four_zoom.md), [lldb_direct_payload_candidate_gate_c6_four_zoom.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/lldb_direct_payload_candidate_gate_c6_four_zoom.md), [lldb_stereo_candidate_gate_c6_four_zoom.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/lldb_stereo_candidate_gate_c6_four_zoom.md), [lldb_capturedimage_f2770_origin_four_zoom.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/lldb_capturedimage_f2770_origin_four_zoom.md), [lldb_c6_active_byte_mutation_watch_tele.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/lldb_c6_active_byte_mutation_watch_tele.md), [lldb_c6_focused_f2720_route_census_tele.md](/Volumes/Dev/L16_Lumen_ReverseEngineering/docs/evidence/lldb_c6_focused_f2720_route_census_tele.md), [lldb_c6_unprobed_direct_f2720_route_census_tele.md](/Volumes/Dev/L16_Lumen_ReverseEngineering/docs/evidence/lldb_c6_unprobed_direct_f2720_route_census_tele.md), [lldb_c6_postmutation_downstream_rect_vector_tele.md](/Volumes/Dev/L16_Lumen_ReverseEngineering/docs/evidence/lldb_c6_postmutation_downstream_rect_vector_tele.md), [lldb_c6_rect_vector_consumer_four_zoom.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/lldb_c6_rect_vector_consumer_four_zoom.md), [lldb_c6_image_pyramid_zero_fill_four_zoom.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/lldb_c6_image_pyramid_zero_fill_four_zoom.md), [lldb_c6_image_pyramid_downstream_liveness_four_zoom.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/lldb_c6_image_pyramid_downstream_liveness_four_zoom.md), [lldb_c6_image_pyramid_data_watch_representative_four_zoom.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/lldb_c6_image_pyramid_data_watch_representative_four_zoom.md), [lldb_c6_image_pyramid_data_watch_grid_tele.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/lldb_c6_image_pyramid_data_watch_grid_tele.md), `c6_destination_and_depthcache.md`, `a2_destination.md`, `iramp_camera_identity.md`, `c6_verification.md`
- Additional source:
  [lldb_c6_postmutation_active_byte_watch_tele.md](/Volumes/Dev/L16_Lumen_ReverseEngineering/docs/evidence/lldb_c6_postmutation_active_byte_watch_tele.md), [lldb_c6_postmutation_item_field_watch_tele.md](/Volumes/Dev/L16_Lumen_ReverseEngineering/docs/evidence/lldb_c6_postmutation_item_field_watch_tele.md)

## 5. Merge acceptance / rejection logic beyond the proven accumulator

- Related blocker:
  final merge-quality decision logic beyond `CLM-MERGE-002`
- Resolution path exists:
  Yes, but it is broader than the others
- Current best recorded path:
  the pre-rewrite root truth tracked this as `OPEN-IRAMP-BODY`
- Path:
  1. start from the now-bounded non-empty partner-record consumer path, especially `0x369e7e..0x369e91`, `0x36cde0`, and `0x36e530`
  2. decode downstream consumption of the three-float scratch tuple written through the record `+0x40` descriptor
  3. `0x36cde0` is now narrowed to the third-tuple scalar producer and returns `sqrt(xmm0 * xmm1)` after patch-statistics / fixed-transform / weighted-reduction work; do not name the field beyond that until downstream consumption proves the public meaning
  4. `0x36e530` is now bounded as accumulator prep: it receives `rbp-0x4240`, performs reciprocal/selector normalization plus fixed SIMD transform/reduction work, returns `scratch+0x1580` in `rax`, and feeds the accumulator's 16-by-16 outer-product weighting; do not name the weight semantics beyond the captured table until a producer/consumer proves it
  5. the first downstream tuple consumer is now bounded at `0x36a7d8..0x36a8cb`: it reads all three tuple fields, passes adjusted coordinates to `0x372a00`, forms `(t + 2 * max(0, t - 0.5), t, t, t)`, and performs a tuple-scalar-weighted multiply-add across all four canonical zooms
  6. the immediate post-reciprocal weighted-add path is now bounded: `0x19e7d0` copies/scales descriptor-backed `vec4` buffers by the reciprocal vector, and `0x36aa30..0x36aa57` blends `reciprocal * 0.2` into lane 3, applies `weight[inner] * weight[outer]`, and adds into the destination `vec4` across all four canonical zooms
  7. the immediate post-weighted-add shaping path is now bounded: `0x36abf0..0x36ac15` applies a lane-3-weighted clamped vector update with first-hit scale `(2,0,0,0)` and clamp bounds `[-0.1,0.1]`, and `0x36ad50..0x36adac` applies a fixed 3-vector transform with lane 3 forced to `1.0` across all four canonical zooms
  8. the caller-side handoff after IRAMP returns is now bounded: the caller validates the `rbp-0x60` descriptor dimensions against ROI, wraps it at `rbp-0x88`, calls helper `0xd76a0`, and static helper inspection shows source `vec4` lanes are squared into the destination descriptor across all four canonical zooms
  9. the caller-side vector-scale handoff after square-copy is now bounded: the caller builds a wrapper over the `rbp-0x70` descriptor with a vector at wrapper `+0x10`, calls helper `0x2d7320`, and static helper inspection shows source `vec4` lanes are multiplied by that vector into the destination descriptor across all four canonical zooms
  10. the caller-side `0x3e5720` executor setup after vector-scale is now bounded: it allocates/resizes a 6-byte-element destination descriptor, builds callback vtable `0x66b020`, dispatches generic executor `0x5670`, and its visible worker maps source 16-byte `vec4` rows to destination 6-byte rows before calling row callback `0x38a30` across all four canonical zooms
  11. row callback `0x38a30` is now bounded: for the observed `512`-wide rows it repacks source `vec4` lanes 0..2 into float triples, calls `0xbfef0` with `ecx = 0` and count `3 * width`, and the used `0xbfef0` branch converts float channels to 16-bit binary16 bit patterns across all four canonical zooms
  12. the immediate caller-side storage sink is now bounded: body `0x3ec960` is vtable slot `0x65f5e0+0x30`, computes destination descriptor `(*rsi)+0xf0`, allocates/resizes it with element size `6`, passes it to `0x3e5720`, then destroys only the temporary descriptor and returns; runtime packets across all four canonical zooms show owner `+0xf0` populated as `512x512`, stride `512`
  13. the first proven downstream consumer family for owner `+0xf0` is now bounded: `0x3d50f0` allocates a `16`-byte-element destination, dispatches executor `0x5670` with row worker `0x3d5290`, and the selected converter path reaches `0x2ff00 -> 0xc0410`; runtime packets across all four canonical zooms show live `rsi` at `0xc0410` inside the exact owner `+0xf0` data range with `ecx/cl = 0`
  14. the immediate handoff after that expansion family is now bounded: `0x3d4e10` calls `0x3d50f0`, and runtime packets at `0x3d502e` across all four canonical zooms show the local source descriptor at `rbp-0x60` points inside the exact owner `+0xf0` data range while the local expanded descriptor at `rbp-0x90` has `16`-byte elements and a first `vec4` sample with lane 3 = `1.0`
  15. the destination backing store for that handoff is now bounded: `0x3d4e10` receives a caller-provided context whose `+0x10` field points to the persistent 16-byte destination descriptor, and runtime packets across all four canonical zooms show local destination descriptor `rbp-0x90` is a clipped view into that context descriptor with matching `qword_28`, in-range data pointer, and 16-byte alignment
  16. the first captured route after that destination context is now bounded: active callable branch `0x3d4842`, active callable slot `0x3ec960`, parent `0x3d01b0` output descriptor `rbp-0x148` as `context+0x10`, caller return `0x3d084d`, and the same temporary descriptor passed to `0x36f800` at `0x3d08ce` across all four canonical zooms
  17. the first-owner branch census after that route is now bounded: sibling direct branch `0x3d4864` is live at `28mm`, `70mm`, and `150mm`, `35mm` accepted only `0x3d4842`, and all accepted census packets still use slot `0x3ec960` and caller `0x3d084d`
  18. the first direct-branch post-route handoff is now bounded: at `28mm`, `70mm`, and `150mm`, the first owner-matching `0x3d4864` packet reaches `0x3d08ce -> 0x36f800` with `rsi` equal to its captured `context+0x10` temporary descriptor; `35mm` has no owner-matching direct branch under this first-owner probe
  19. the global read-context branch-site caller/slot census is now bounded: complete canonical bridge HDR renders at `28mm`, `35mm`, `70mm`, and `150mm` hit only caller set `{0x3d0732, 0x3d084d, 0x3ecc5a}` and active callable slot set `{0x3ec960, 0x3e4a80}` at `0x3d4842` / `0x3d4864`, with all hits preserving the parent/context destination equality checks
  20. the global post-route family classification is now bounded: `0x3d0732` is exact-size cleanup with no post call, `0x3d084d` reaches `0x3d08ce -> 0x36f800`, and `0x3ecc5a` reaches `0x3ecc74 -> 0x3edb80` visible-`src1` one-image normalization across all four canonical zooms
  21. the global parent-chain ancestry for those families is now bounded: `0x3d0732` returns through `0x3b07a9 -> 0x41a8d3 -> 0x3adfce -> 0x280e`, `0x3d084d` returns through `0x3bb822 -> 0x3adfce -> 0x280e`, and visible-`src1` `0x3ecc5a` returns through `0x374cf3 -> 0x3665da -> 0x365f50 -> 0x3ec7df -> 0x3eca4b -> 0x3d4842` with some nested read-context continuations; exact hot direct-branch counts are evidence-run counts, not algorithm constants
  22. static body classification for those parent chains is now bounded: `0x280e` / `0x3adfce` are callback/iteration glue; `0x3b0740`, `0x41a7d0`, `0x3b9770`, `0xfbda0`, and `0x3bb2b0` are selected owner-cache/direct-render tile surfaces; `0x374ac0`, `0x3661b0`, `0x365960`, `0x3ec770`, and `0x3ec960` are visible-`src1` / IRAMP nested wrapper plus owner `+0xf0` sink surfaces
  23. static helper-surface classification for that route is now bounded: `0x31b110` is source/RAW/STD adapter into `0x33fb30`; `0xfe720` is a 16-byte rectangle/ROI record-grid helper; `0x106cb0` is vignetting-data construction/interpolation; `0x2e20` is callback dispatch; `0xf3570`, `0x3b9660`, `0x3c6ac0`, `0x1bea20`, `0x1bea00`, and `0x1be970` are owner/tile/map/field helpers
  24. static selected-cache/post-route classification for that route is now bounded: `0x3d01b0` is level/ROI tile-read executor; `0x3d0650` is exact-size read or read-then-`0x36f800` rescale; `0x3d47d0` is read-context branch routing; `0x3d4e10`, `0x3d50f0`, `0x3d5290`, `0x2ff00`, and `0xc0410` are clipped-view / 6-byte-to-vec4 / 16-bit-to-float conversion plumbing; `0x3edb80` is one-image `sqrt(max())` normalization
  25. static downstream direct-caller census is now bounded for selected helpers: `0x36f800` direct callers are selected-cache read/rescale, TileCache-like read/rescale, and IRAMP-internal resample handoff; `0x3d01b0` direct callers are selected-cache reads, visible-`src1` read, source-adapter caller, and DOFCache render caller; `0x3edb80` direct callers are visible-`src1` and visible-`src2` one-image normalization wrappers; `0x3d50f0` has only the already classified `0x3d4e10` direct caller
  26. static selected-cache caller census is now bounded for `0x3d0650`: the 14 direct callers fall into source-adapter-style caller windows, small owner-cache selector `0x3b0740`, multi-branch owner/tile-cache surface `0x3bb2b0`, owner `+0xf0` output-sink branch body `0x3ec960`, and later helper/adaptor caller surfaces around `0x42fb40` and `0x42fd30`
  27. static `0x3e5720` caller census is now bounded: direct callers are active-callable-slot / owner `+0xf0` writer body `0x3e4a80`, owner `+0xf0` output-sink body `0x3ec960`, and DOFCache render body `0x3f0b90`; ancillary `0x432db0` coverage bounds the later selected-cache caller surface `0x42fb40 -> 0x3d0650 -> 0x432db0`
  28. static `0x3d4e10` caller census is now bounded: direct callers are the two already bounded branch-router post-branch handoffs at `0x3d484a` and `0x3d486c`, plus separate indexed-entry loop caller `0x3d5468` inside body `0x3d5400`; `0x3d50f0` has only direct caller `0x3d5029` inside `0x3d4e10`, and `0x3d5290` has no direct callers because it is worker-dispatch plumbing
  29. static/runtime `0x3d5400` executor-route liveness is now bounded: `0x3d01b0` builds callback vtable `0x66a728`, executor `0x5670` uses vtable slot `+0x30 = 0x3d53c0`, the thunk jumps to `0x3d5400`, and first-hit probes reach `0x3d5468 -> 0x3d4e10` across all four canonical zooms
  30. the first gated `0x36f800` worker path after that owner `+0xf0` selected-cache route is now bounded: callback slot `0x3721d0`, static worker body `0x372210`, runtime worker-entry probe `0x372224`, and first weighted `vec4` store at `0x372488` across all four canonical zooms
  31. helper activity inside that same route is now bounded: `0x372210` converts offset/scale doubles to signed 16.16 fixed-point, `0x372500` builds the row-plan/cache struct, captured `0x372760` row-cache stores match a reconstructed 4-tap horizontal `vec4` formula across all four canonical zooms, and fresh first-dispatch row-plan packets capture all four unique worker regions per zoom with only the middle row-cache segment predicted/live in that dispatch
  32. full-render row-cache segment reachability is now bounded: leading/trailing `0x372760` store sites are live at `28mm` and `70mm`, and have zero hits at `35mm` and `150mm` under the tested canonical bridge HDR runs
  33. downstream row-image/final policy after the classified caller/helper/post-route/direct-caller/selected-cache-caller/`0x3e5720`-caller/`0x3d4e10`-caller/`0x3d5400`-executor-route families still needs closure without naming public offset/scale/pixel-format semantics until producer/consumer pairs prove them
  34. only after those are bounded, decide whether any later stage still contains contributor suppression / acceptance logic
- Why this is only a broader path:
  the local block-matching/refinement path, first downstream tuple consumer, immediate post-reciprocal weighted-add body, immediate post-weighted-add shaping body, caller-side square-copy handoff, caller-side vector-scale handoff, `0x3e5720` executor setup, row-callback binary16 conversion, owner `+0xf0` storage sink, first owner `+0xf0` downstream expansion family, immediate expansion handoff, expansion destination-context backing, selected-cache route, first-owner branch census, first direct-branch post-route handoff, global branch-site caller/slot census, global post-route family classification, global parent-chain ancestry, static helper-surface classification, static selected-cache/post-route classification, static downstream direct-caller census, static selected-cache caller census, static `0x3e5720` caller census, static `0x3d4e10` caller census, static/runtime `0x3d5400` executor-route liveness, first `0x36f800` weighted store, row-cache helper formula, fresh first-dispatch row-plan segment coverage, and full-render row-cache segment reachability are now isolated, but complete downstream row-image policy and final contributor-suppression policy are not yet isolated to one decision site
- Why it is still real:
  the unresolved area is now bounded to the IRAMP inner body and immediate sub-stages rather than the entire binary
- Source trail:
  superseded root `TRUTH.md` in git history (`OPEN-IRAMP-BODY`), `iramp_substages_verified.md`, `iramp_kernel_body.md`, `docs/evidence/bundle_lldb_iramp_row_callback_38a30_conversion.md`, `docs/evidence/bundle_lldb_iramp_caller_output_descriptor_sink.md`, `docs/evidence/bundle_lldb_owner_f0_downstream_consumer.md`, `docs/evidence/bundle_lldb_owner_f0_expansion_handoff.md`, `docs/evidence/bundle_lldb_owner_f0_expansion_dest_context.md`, `docs/evidence/bundle_lldb_owner_f0_selected_cache_route_static_classification.md`, `docs/evidence/bundle_lldb_downstream_route_caller_census_static.md`, `docs/evidence/bundle_lldb_selected_cache_caller_census_static.md`, `docs/evidence/bundle_lldb_3e5720_caller_census_static.md`, `docs/evidence/bundle_lldb_3d4e10_caller_census_static.md`, `docs/evidence/bundle_lldb_3d5400_executor_vtable_liveness.md`

## Practical Read

If the question is "do we know how to go get these?", the answer is:

1. `src1` / `src2` reducer:
   yes, concrete LLDB + installed-vtable + upstream-from-wrapper decode path
2. dst-coordinate semantics:
   yes, concrete writer/consumer static-disasm path from the already-located lattice plumbing
3. four-zoom merge topology beyond accumulator and entry signature:
   yes, structured four-zoom closure path using the canonical quartet; accumulator participation and IRAMP entry signature are already four-zoom proven
4. C6 routing:
   yes, concrete dispatcher/filter and upstream-image-path routes
5. final merge acceptance / rejection logic:
   yes, but it is still a broader "finish decoding the bounded IRAMP inner body" path rather than a single pinpointed gate
