# WSJF Priority

This file is planning guidance.

It is not a truth authority.

If this file and the canonical blocker or claim docs disagree, the blocker and claim docs win.

## Purpose

This file prioritizes the current blocker set toward the actual end goal:

- independently read an LRI without Lumen code/binaries
- compute the fully merged image from admitted formulas and policies
- write a correctly tagged modern-photo-app-readable image
- ghost-free parity-grade merge quality
- correct framing and participation
- stability across `28mm`, `35mm`, `70mm`, and `150mm`

## Current Priority (TRUTH 3.0.350)

This section supersedes every ranking and lane recommendation below it. The
older tables are retained only as campaign provenance.

TRUTH `3.0.350` reopens `CLM-DENOISE-002` at one concrete pixel-bearing
boundary. CNR source lane 3 is installed `guide^2`, while Phoenix uses the
disproven constant `1`; the exact AWB-stage guide source and normalization are
still unknown. This is WSJF rank 1: value `9`, time criticality `9`, risk
reduction `9`, job size `3`, score `9.0`. Follow the heap denoise task's
`+0x60` guide backward inside the RTTI-named `setWhiteBalance::$_22` lambda,
match it against the lambda's `Image<u16>`, `CapturedImage`, and `Stats`
inputs, close the normalization, then run four-focal/two-body discriminators.
Do not port a brightness-correlated proxy.

The deterministic-schedule Unit-2 `70mm` level-0 Guidance/source/record/G-42/
G-43 boundary comparison from TRUTH `3.0.349` is rank 2. The Phoenix
implementation audit and exact skip-mask repair may proceed in parallel, but
neither substitutes for the CNR guide-source proof.

TRUTH `3.0.349` removes stock-Lumen depth nondeterminism as a confounder. The
bit-level mechanism is executor-parallel shared G-43 payload RMW plus a scoped
pre-G42 scheduler-sensitive parent decision; ascending sequential `0x2d30`
execution suppresses the complete index-5 variation across the admitted
two-body wide/tele controls. The next highest-leverage implementation task is
therefore a deterministic-schedule Unit-2 `70mm` level-0 boundary comparison:
capture Lumen Guidance and four sources, composed records, G-42 cost, and G-43
payload/argmin under that control and compare Phoenix at the first unequal
boundary. Do not tune against an arbitrary stock-Lumen draw.

TRUTH `3.0.348` completes the fresh Unit-2 `70mm` range-generation comparison.
The installed and Phoenix range formulas both replay exactly on their own
prior maps, while the first parity divergence occurs at full-band level 0
before range propagation. The range item therefore leaves the queue as a
causal hypothesis. The highest-leverage target is now the first level-0
operand boundary that can explain different argmins: compare same-render
Lumen/Phoenix selected Guidance and four source planes first, then composed
records, raw G-42 cost vectors, and G-43 accumulation/argmin in that order.
Stop at the first unequal boundary; do not widen bands or fit depth afterward.

TRUTH `3.0.347` completes the formerly ranked mode-0 whole-overlap replay:
all 4,489 patches and all 272,484 pre-combine words match exactly. That item
leaves the queue. The end-to-end color audit's final receipts already resolve
the master as camera-linear and retire the invented inverse-CCM/Bradford join;
it is not the next investigation target. The highest-leverage implementation-
validation target is a fresh same-generation Unit-2 `70mm` per-level Range-map
comparison between Lumen and Phoenix. The historical `53.1%` local-band figure
is unverified because its raw inputs did not survive, while the current
controlled tele output still fails spatial controls. Re-capturing both local
lower/upper bands under one recipe will decide whether the compressed tele
depth originates in range construction or later cost/SGM selection. TRUTH
`3.0.348` completes that comparison and redirects work to level-0 operands,
cost, and SGM; no range formula may be changed from the historical percentage.

TRUTH `3.0.346` corrects the mode-0 auxiliary-mean origin, Wiener operand
roles, inverse axis order, packed-flow low-word behavior, and terminal combine
with exact live replays. The next highest-leverage implementation-validation
target was then to regenerate the complete `522x522` overlap image from every
flow-selected patch. TRUTH `3.0.347` closes that target.

TRUTH `3.0.345` removes the immediate production MonoFusion color-wrapper
implementation gap. The exact response/opponent basis, inverse, public
sensor-type/black/white origins, and live scalar replacement are now admitted;
a scalar RGB ratio is not a valid substitute. This adds no selected-profile
claim blocker. The next implementation-validation target is complete
mode-0 scalar/output replay against installed tiles before evaluating the
remaining end-to-end selected color-camera reconstruction depth.

The selected-profile `CLM-CORRECTION-001` item reopened at TRUTH `3.0.339` is
closed. Public camera-ID matrix selection, public-RAW amount fit, installed
A/B/C tables, generated IR, AWB matrix preparation, scalar Bayer recurrence,
coordinates/boundaries, limiter, and output blend now replay exactly on two
physical bodies, with prior Unit-1 four-focal and exact-`70mm` Unit-2 liveness.
The sensor/variant/camera-group/CCT selector inputs are also public-origin
closed at TRUTH `3.0.341`.
The cross-cutting current-reference `rcpss`/`rcpps` bit mapping is portable and
exact at TRUTH `3.0.342`; implementation audits must not retain exact division
as a parity-path waiver.

The following zero-blocker statement is superseded by TRUTH `3.0.350`. The
then-next investigation-depth item was an end-to-end selected color-camera reconstruction
for A1/A3/A4/A5 from public RAW through the already admitted normalization,
hot-pixel, cross-talk, demosaic, vignetting, and color boundaries. This is a
join/coverage audit, not permission to refit or replace an admitted formula;
it becomes a ledger blocker only if the deterministic replay exposes a real
pixel-changing omission.

TRUTH `3.0.338` previously closed the operand-pyramid omission inside the admitted
MonoFusion mode-0 flow path. Public-LRI-derived reference and corrected A2
source level 0 plus exact FastCollapse levels 1 through 4 replay completely
across two physical exact-`28mm` bodies. This correction does not create a
new WSJF item; at that version it did not change the then-zero selected-profile blocker count. It removes
captured operand pyramids as an implementation shortcut.

TRUTH `3.0.337` admits exact selected color-camera sensor-level normalization
and public vignetting over two-body A1 plus one four-model movable-camera
discriminator. It narrows public RAW/color reconstruction depth but does not
create or remove a selected-profile blocker, so no item enters the WSJF queue.
End-to-end A1/A3/A4/A5 numerical reconstruction and untested compatibility
arms remain investigation depth rather than a substitute ranked blocker.

Before corrective TRUTH `3.0.339`, no selected-profile parity blocker remained
ranked. That statement is superseded by the cross-talk item above. TRUTH `3.0.335` closes the
former hot-pixel global-edge item with the exact six-pixel parity-preserving
upper-median halo and complete full-frame replay over Unit-1 `28/35mm` plus
Unit-2 exact-`28mm`. TRUTH `3.0.334` had already removed the parallel
MonoFusion A1 reference-operand reconciliation with complete two-body public
replay.

TRUTH `3.0.333` corrected the focused hot-pixel proof: one residual and a
row-varying selector are exact for the complete Unit-1 exact-28mm inset
interior, while 118 global-edge samples remained. Standard padding substitutes
were disproven; version `3.0.335` supplies the subsequent direct
`0x178b0` / `0x2e8cc0` boundary closure.

TRUTH `3.0.332` corrects an implementation-level omission inside the already
closed MonoFusion mode-0 claim: the complete five-stage nonzero flow field,
quadratic refinement, and public vignetting/gain rejection rule now replay all
`430,946` vectors bit-for-bit across two physical exact-28mm inputs. This does
not reopen the claim; it removes zero residual gather as a permissible parity
implementation and becomes the immediate implementation-reconciliation item.

TRUTH `3.0.331` resolves the demonstrated Skip-mask-consumption gap: nonzero
pattern-2 pixels use zero local photometric cost but remain ordinary SGM and
first-argmin output pixels. The later guided 2x upsample does not fill those
positions. TRUTH `3.0.330` resolves the preceding G-42/G-43 representation
gap: local cost is binary32-scaled by `(1/27)/source_count` and truncated in
place, with no per-pixel minimum-subtraction pedestal. Both items leave the
queue immediately because installed proof, exact-focal two-body runtime, and
existing Unit-1 four-focal liveness satisfy the admission standard.

TRUTH `3.0.329` resolves the next demonstrated builder gap: the predecessor
range pool is a Skip-mask-aware asymmetric 4x4 min/max over offsets
`{-1,0,1,2}` on each axis, not a symmetric radius-2 all-pixels fit. The item
leaves the queue immediately because the installed callbacks, exact
five-transition replay, Unit-1 four-focal receipts, and Unit-2 control close
it.

TRUTH `3.0.328` resolved the preceding builder gap:
coarse index-5 projection uses fixed full-domain Images and projection records
plus exact cell-center coordinate lifting, not a level-conjugated homography.
The item leaves the queue immediately because installed proof and the complete
two-body/four-focal runtime matrix close it. The former next reconciliation
priority, exact cost representation entering G-43, is closed by TRUTH
`3.0.330`; the minimum-subtraction pedestal is refuted.

The current implementation-reconciliation audit is
`docs/audits/implementation_repair_handoff_2026-07-16.md`. It removes
unadmitted substitute algorithms from the parity path and maps the rebuild to
the live claim/evidence set. TRUTH `3.0.305` closes the semantic identity of
payload slot 15 as conditional linear-ProPhoto/D50 materialization, and TRUTH
`3.0.306` closes tested profile-3 incidence as exact copy across the Unit-1
quartet plus targeted Unit-2 controls. Generic unequal-selector formulas are
not selected by the admitted route.

TRUTH `3.0.307` closes the residual Pile-2 bookkeeping gap by independently
admitting the full calibration-payload digests, all 28 exact VST rows, the
selected leaf wire contract, and four explicit repeated-float `packed=true`
options. This is nonpixel provenance/schema closure and does not create a new
ranked blocker.

TRUTH `3.0.308` bounds initial editor construction to the admitted profile-3
IRAMP topology at all four focal tiers and one tested 28mm brush rerender to
prepared-state reuse. TRUTH `3.0.309` then joins both recovered standalone
body packages to 81 new photographs, confirms no third public firing topology,
and keeps package-only zoom/hot-pixel artifacts outside selected profile-3
input requirements. TRUTH `3.0.311` closes tested 28mm editor-pyramid geometry,
the immediate default level-4 `PipelineCache ->` per-level Color-pipeline
route, correct type-13 record custody, and installed conditional BGRA/RGBA byte
packing. Byte-exact before/after joins isolate the editor/export difference to
seven named callbacks. None creates a new ranked base-render blocker; remaining
display-specific callback formulas and alternate DOF/mode behavior stay
reference-only compatibility work.

TRUTH `3.0.312` closes the selected ACRE core with a byte-exact clean-room
replay over one full `256x256` worker tile and removes tested lens/contrast
from the pixel-changing set. Remaining editor work is now specifically the
the selected color-conversion replay, index-10 color-correction math,
EV/LUT public origins, and alternate cache/mode routes; version `3.0.313`
closes the selected conversion below, while the others remain reference-only
and have no base-render WSJF rank.

TRUTH `3.0.313` also closes the selected `0xabf20 -> 0xac600`
linear-ProPhoto-to-sRGB conversion with a second byte-exact full-tile replay.
At that checkpoint the remaining editor items were index-10 color correction,
EV/LUT public origins, and alternate cache/mode routes.

TRUTH `3.0.314` closes the selected EV/LUT origins through exact public
capture-normalization arithmetic and the installed `light_v1` enum/curve
dispatch. TRUTH `3.0.315` closes selected index-10 color correction through
two-body public calibration, an exact clean-room optimizer/endpoint join, and
full-image byte replay. Alternate cache/mode and other untested editor behavior
remain reference-only and have no base-render WSJF rank.

TRUTH `3.0.316` closes public mode-1 DOF activation, the exact local cache
predicate, and one changed final packed buffer at Unit-1 `28mm`. Internal DOF
math and other modes/focals/bodies remain reference-only and have no
base-render WSJF rank.

TRUTH `3.0.317` closes the installed optical tuple, public
`sensor_data_surface.data_scale` pitch ancestry, exact focus-range math, and
SSE tile-radius bucketing. `ImageCircleFilter`, layered/occlusion composition,
modes `2/3/4`, and other-focal DOF runtime remain reference-only and have no
base-render WSJF rank.

TRUTH `3.0.318` closes both installed vec4/scalar `ImageCircleFilter`
specializations as uniform integer-disk averages with clamped-edge replication.
Layered/occlusion composition, modes `2/3/4`, and other-focal DOF runtime remain
reference-only and have no base-render WSJF rank.

TRUTH `3.0.321` closes the installed mode-1 layered compositor, including
signed layer construction/membership, neighbor opacity, native/scaled blur
dispatch, exact five-tap Gaussian, 64-phase cubic B-spline resampling, and
reverse premultiplied source-over. Its runtime proof is Unit-1 `28mm`; modes
`2/3/4`, nonempty secondary-layer and distinct-rectangle runtime, other
bodies/focals, and general edit semantics remain reference-only and have no
base-render WSJF rank.

`CLM-COMPAT-002` is removed at TRUTH `3.0.296`: the two complete
`28mm`/tele variants and one complete `74mm`/wide variant are joined to public
reference/firing selection, scorer incidence, MonoFusion/C6, primary
stereo/range/upsample, five warp records, exact direct IRAMP contributors and
scale, public crop/orientation, and completed HDR. The two tele variants span
both physical calibration signatures; no body or firmware cause is claimed.

All rankings below this current-priority section are historical campaign
provenance. Their old "open" wording and numeric ranks do not constitute the
live blocker set.

`CLM-DENOISE-002` is removed at TRUTH `3.0.274`: the selected radius-2 and
radius-4 bilateral formulas and callback roles are closed by SHA-pinned
static proof and two-body runtime replay, joined to prior Unit-1 four-focal
liveness/store coverage.

TRUTH `3.0.275` removes Skip-mask population from `CLM-STEREO-001`. The
reduced job size reflects two remaining stereo-policy gaps, not claim closure.

TRUTH `3.0.276` removes disparity convention from `CLM-STEREO-001`; only the
exact Guidance component semantics remain in this blocker.

TRUTH `3.0.278` removes Guidance component semantics and live SoftISP
configuration. The unchanged score now applies to the load-bearing default
hot-pixel stage's exact rank-neighborhood coordinate policy and clean-room
Bayer noise-LUT generator. TRUTH `3.0.279` then closes the LUT generator
bit-for-bit, leaving only the rank-neighborhood coordinate policy.

TRUTH `3.0.280` closes both rank-neighborhood coordinate formulas and removes
`CLM-STEREO-001` from the ranking.

TRUTH `3.0.281` captures all four-focal undistorted and depth-map artifacts.
The validation lane remains first but changes from boundary discovery to a
repeat-class campaign because tele maps refute one deterministic golden hash.

TRUTH `3.0.282` completes that ten-sample campaign, proves full-map
index-to-depth coupling, and admits the layered validation oracle.
`CLM-VALIDATION-001` leaves the ranking; `CLM-STATE-001` is the sole remaining
canonical blocker.

TRUTH `3.0.283` closes `CLM-STATE-001` through the operational State/object
closure audit. TRUTH `3.0.284` then reopens the audit-discovered
`CLM-COMPAT-002` supported-input variant lane.

`CLM-INPUT-001` is removed from the ranking: exact raw locations, packing,
dimensions/stride, and Bayer/mono phase are admitted across both calibration
bodies and all four exact-focal tiers.

`CLM-DEMOSAIC-002` is removed from the ranking at TRUTH `3.0.256`: installed
formula proof and same-thread guide/2x2 replays close all four Bayer variants.

`CLM-AWB-001` is removed at TRUTH `3.0.257`: embedded schema, full corpus
layout/validity census, and four-zoom/two-body runtime joins close the stored
gain origin and reciprocal policy.

`CLM-RESAMPLE-001` is removed at TRUTH `3.0.258`: installed static replay and
a complete runtime capture agree on all 4096 table bytes, while prior
four-focal packets close application and boundary mechanics.

`CLM-WARP-004` is removed at TRUTH `3.0.259`: installed/static public custody,
two-body runtime discriminators, and eight exact-focal public checks close the
Brown-Conrady model and exact 4096-entry table.

At TRUTH `3.0.260`, `CLM-VALIDATION-001` closes ten-repeat distributions for
all four canonical focal tiers and is rescored for its remaining stage-map
work. At TRUTH `3.0.261`, `CLM-OUTPUT-002` closes exact RGBE quantization,
orientation, and file-body mechanics and is rescored for its narrower
remaining contract.

At TRUTH `3.0.262`, `CLM-CORRECTION-001` advances to `PARTIAL`: the live
public grids and hall-code interpolation are known, so this lane now proceeds
directly through bit-exact profile replay and IR-model classification.

At TRUTH `3.0.263`, the four concrete cross-talk row workers are excluded
from the canonical quartet, shrinking correction work to vignetting shaping
and IR-model roles.

At TRUTH `3.0.264`, two-body public profile replay closes hall-code selection,
interpolation, and shaping. Correction now has one narrow row-sampler replay
before promotion.

`CLM-CORRECTION-001` was removed at TRUTH `3.0.265`; that removal was
temporarily superseded at TRUTH `3.0.339` when the real scalar-true callback
was found live. TRUTH `3.0.340` independently replays that callback from
public inputs and removes it again at the corrected proof boundary.

`CLM-LRI-001` is removed at TRUTH `3.0.266`: the installed three-record-type
reader, public schemas, field-order preference merge, four-focal live
crop/exposure-target formulas, flash/GPS exclusions, and all-complete-corpus
record/field census close the image-producing container contract. The census
also moves non-normal public orientation placement explicitly into the
remaining output lane.

At TRUTH `3.0.267`, `CLM-OUTPUT-002` closes that non-normal placement lane:
installed formulas plus exact-35mm two-body runtime cover clockwise and
counter-clockwise, while prior four-focal evidence covers normal. The output
rank remains first but its job size falls to `2`, reflecting only tagged
independent export work.

`CLM-OUTPUT-002` is removed at TRUTH `3.0.268`: installed matrix extraction
pins ProPhoto/D50 colorimetry, and a deterministic independent linear-TRC ICC
plus float32 RGB TIFF round-trips unclamped HDR samples and is accepted by
macOS ImageIO and independent TIFF/profile readers.

`CLM-COMPAT-001` remains reference-only for unclosed alternate-profile and
GUI/editing behavior. The profiles-`1/2` MonoFusion mode-1 scalar body is
formula-closed at TRUTH `3.0.336`; its initial profile-3 GUI pyramid has a four-focal
IRAMP-topology join, while post-edit reuse is scoped to one `28mm` brush.
Exact output-byte packing and the immediate tested default level-4 display
route are closed and its seven callbacks are byte-bounded; selected index-10
color correction is formula- and byte-closed at Unit-1 `28mm` default level 4.
Public mode-1 DOF activation, exact local cache selection, and one final-buffer
effect are closed at a scoped Unit-1 `28mm` treatment. The installed optical
tuple, public pitch scale, focus-range math, and tile-radius formula are also
closed; both circle-filter specializations and the mode-1 layered compositor
are now exact too. Public five-mode identity/dispatch, the complete live
DebugView selector/target set, and the default QuickSelect mask/blend are also
closed at Unit-1 `28mm`; one active public stroke is additionally joined
through binary mask to exact output support, and RefocusSlider visualization
math plus the RefocusPoint post-DOF range overlay are exact. Mode-1's five-tap
split, confidence gate, both-axis boundary extension, invalid fallback, and
final scalar blend are exact under the admitted two-body scope. Debug-object formulas/public meanings, internal QuickSelect
segmentation/commit behavior, and other
untested editor behavior remain open.
Non-baseline profile-3 LRI variants remain separated into `CLM-COMPAT-002`.

## Inputs

This prioritization is grounded in:

- [PARITY_BLOCKERS.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/canonical/PARITY_BLOCKERS.md)
- [ENDGOAL_UNKNOWNS.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/canonical/ENDGOAL_UNKNOWNS.md)
- [BLOCKER_PATHS.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/canonical/BLOCKER_PATHS.md)

## WSJF Method

Standard WSJF:

`WSJF = Cost of Delay / Job Size`

Using these planning factors:

- `Business Value`
  How directly the blocker affects the end-goal quality bar.
- `Time Criticality`
  How much delaying this keeps the project from making honest forward progress now.
- `Risk Reduction / Opportunity Enablement`
  How much closing this blocker unlocks other blockers or prevents wrong implementation.
- `Job Size`
  Estimated investigation size, based on how concrete the existing resolution path already is.

Scale:

- benefit factors: `1..10`, higher is more important
- `Job Size`: `1..10`, higher is larger

## Historical Raw WSJF Table (Superseded)

Do not use this table for current scheduling; it predates the closures listed
above.

| Blocker | BV | TC | RR/OE | CoD | JS | WSJF | Notes |
|---|---:|---:|---:|---:|---:|---:|---|
| Pair-grid producer depth semantics / LRI origins / public names | 10 | 9 | 8 | 27 | 3 | **9.00** | Geometry-critical; storage, consumer formula, row matrix chain, tracked map-provider runtime path, descriptor custody boundary, four-zoom depth-builder handoff, `0x29ed90` worker formula, `StereoLayer<false>` index-5 source custody, generated Range map / Cost volume / minimum-cost depth-hypothesis index / Depth map names, exact `lt::CapturedImage` identity for selected `state+0xe0` objects, per-image composed geometry-record whole-field operational identity and lookup-count role, sampled Guidance / Pixel buf / Min cost buf / Line buf operand names, complete sampled SGM recurrence roles over generated Line/Min-cost/local-cost/Cost-volume storage, Guidance public producer `StereoISP::CreateStereoImage`, final `[Y,U,V,1]` formula and public AWB/`SENSOR_AR1335` origins, body-independent SGM `P1` / guide-adaptive `P2/P1` / guide-decay tuning origins, generated reciprocal near/far lookup-vector mechanics, selected endpoint constants, internal count production, the lookup's internal ray-depth hypothesis-grid role, public `mm` / reciprocal `mm^-1` unit custody, and the later `state+0x448` box input's public `geometry.distortion.polynomial` plus sensor-size ancestry are proven; remaining work is unclassified image-consequential `CapturedImage` fields, complete selector-bank and whole-State identity, stable full-map distributions, and broader public calibration/LRI/protobuf mapping |
| Exact pre-fusion merge/reduction mechanism behind `src1` / `src2` | 10 | 10 | 10 | 30 | 6 | **5.00** | Highest architectural leverage; `N-to-1 reducer` remains only a search shorthand; the visible wrapper, `PipelineCache+0x8` level-vector metadata correction, visible `src1` lookup key, visible `src1` payload constructor path / level vector, visible `src1` versus direct-contributor payload-family and secondary-callable splits, visible `src1` secondary-callable live handoff, visible `src1` worker/projection-record handoff to `0x3e4c50` / `0x3e42e0`, static `0x3e42e0` coordinate-transform formula and field-pack producer, visible `src1` source-image producer topology, first captured visible-`src1` source-producer virtual target `0x65b3c8/+0x30 = 0x341770`, capped lower virtual target census (`0x33f3e8` / `0x33f94f` all four zooms, `0x33ffd4` wide-tier only under tested gate), capped target-family visible-body static classification, bound indirect target chains `0x342d99 -> 0x342b80 -> 0x2eb560` and `0x3449f0 -> 0x345920 -> 0x2f53d0`, `0x341770` helper `0x2e8680` / callback `0x2e8cc0` as one-source Bayer/RAW region-helper work, visible `src2` hot state/static body boundary, and accumulator surfaces are four-zoom observed/static-bounded, while `0x3e2e90` / `0x3e4c50`, source-producer region-adapter/helper/census target visible bodies, owner cache selection, owner alternate-cache setup, visible `+0x678` constructor/runtime record-pack, selected layer virtual, record-consumer, `StereoLayer<false>::runPass` cost-path, sibling `StereoLayer<false>::compute` cost-path, prefusion callable-gate, candidate-scoring surfaces remain excluded as closure points; the immediate `0x2f53d0` helper chain is bounded as validation / descriptor-vector setup / bilateral-kernel-size dispatch / executor dispatch, its executor callback bodies are bounded as local descriptor transform / filtering / interpolation / normalization / accumulation surfaces, the tested first-visible-`src1` route selects `0x2fb320` rather than `0x2f78e0`, the selected `0x2fb320` worker is bounded as descriptor / `vec4` coefficient / normalized weighted-store mechanics, all thirteen corrected upstream `CalibDataProcessor::State ()` `operator()` bodies are now four-zoom runtime-live with count pattern `(1,1,4,4,4,1,1,1,5,5,5,5,1)`, and follow-up runtime proof bounds the ordered dispatcher return skeleton for the canonical no-auto-LRIS quartet; `0x247390` is excluded from that State census and refuted as a State body; public helper-field semantics, public descriptor-field names/origins, public State meanings, semantic `src1` contents, and merge/reducer closure remain excluded as closure points |
| Tele odd-camera routing, especially C6 | 8 | 7 | 8 | 23 | 3 | **7.67** | Strong tele-risk; keyed-helper/vector-builder and projection field-pack dispatcher boundaries are excluded, key `15` is proven constructed active then later cleared at `0x3c90a5`, focused identity proof ties the active key-list helper observations at `0x1bdbab` / `0x1bdbdd` to the same item pointer cleared at the mutation boundary, helper `0x1bdb60` is classified as key-list bookkeeping, all 58 direct static `call 0xf2720` sites now have admitted tele runtime census coverage, the newly covered active sites are constructor-adjacent key/container/tree materialization helpers, a post-mutation same-byte watchpoint sees the tracked active byte still `0` at 18 later stops per tele render, a selected-field watch proves pre-output `+0x60/+0x64` reads and cleanup-only watched pair/type ranges, the immediate post-mutation state consumer is proven, the downstream rect-vector builder, ImagePyramid consumer route, and immediate per-level zero-fill callsite are proven, selected later static `context+0x538` candidate families are zero-hit under complete four-zoom bridge HDR, representative first-8-byte data watchpoints have zero hits after zero-fill, expanded tele-grid first/middle/last data watchpoints across all five zero-filled levels have zero hits at `70mm` and `150mm`, candidate context consumer `0x3c9540 -> 0xe6c30` is zero-hit under tested tele bridge HDR, and both the direct candidate loop and stereo-side keyed-record loop prove key `15` is filtered by post-mutation `object+0x30 = 0`; remaining work is untested-field/alias proof, final effect of the watched `+0x60..+0x67` reads, unprobed range proof for later write/read behavior and final C6 image-merge effect of the zero-filled ImagePyramid/geometry route, alternate image-effecting routes, or terminal-filter proof |
| Full merge topology beyond the four-zoom-proven accumulator, entry signature, and direct contributor identity | 10 | 8 | 9 | 27 | 7 | **3.86** | Essential for spec closure, but now starts after `CLM-MERGE-002`, `CLM-MERGE-003`, and `CLM-MERGE-004` are four-zoom proven |
| Merge acceptance / rejection logic beyond accumulator | 10 | 7 | 9 | 26 | 9 | **2.89** | Very important; local SAD/WTA/refinement/bilinear tuple write, `0x36cde0` scalar production, `0x36e530` accumulator-prep source/weight wiring, first downstream tuple-consumer multiply-add, immediate post-reciprocal weighted add, immediate post-weighted-add shaping, caller-side post-IRAMP square-copy handoff, caller-side post-square vector-scale handoff, caller-side `0x3e5720` executor setup, row-callback binary16 conversion, owner `+0xf0` storage sink, first owner `+0xf0` downstream expansion family, immediate expansion handoff, expansion destination-context backing, first selected-cache route to `0x36f800`, first-owner branch census, first direct-branch post-route handoff, global branch-site caller/slot census, global post-route family classification, global parent-chain ancestry, static parent-chain body classification, static helper-surface classification, static selected-cache/post-route classification, static downstream direct-caller census, static selected-cache caller census, static `0x3e5720` caller census, static `0x3d4e10` caller census, static/runtime `0x3d5400` executor-route liveness, first weighted `0x36f800` store, row-cache helper formula, fresh first-dispatch row-plan segment coverage, and full-render leading/trailing row-cache reachability are now bounded, but downstream row-image/final policy after the classified caller/helper/post-route/direct-caller/selected-cache-caller/`0x3e5720`-caller/`0x3d4e10`-caller/`0x3d5400`-executor-route families and final suppression policy remain open |

Public gate-name refinement: `CapturedImage+0x30` is exact public
`CameraModule.is_enabled`. This removes an anonymous Lane B field and names
the C6 mutation/tested gates, but does not change either blocker's WSJF score,
status, terminality boundary, or final-effect boundary.

Further capture-field refinement names direct public `CapturedImage`
`sensor_exposure`, `sensor_analog_gain`, `sensor_digital_gain`, and
`sensor_temparature` copies. This narrows Lane B naming only; scores, status,
remaining bank-field unknowns, and final-effect boundaries are unchanged.

Static RTTI/construction custody now closes the lookup-context name itself as
retained `shared_ptr<lt::RawImageFactory>` backed by `lt::CaptureStack`.
Generated-parser/copy proof now also closes the secondary key as public
`CameraModule.frame_index`, with factory `+0x10` selecting frame `0` on the
admitted renderer-owner path. Complete installed-writer census plus exact-focal
two-body watches now also map `CalibStage 0 = factory` at
`CapturedImage+0x180` and `CalibStage 1 = current` at `+0x12c`. Complete bank
field names remain open; no score or status changes.

## Historical Raw Ranking (Superseded)

Pure WSJF gives this order:

1. pair-grid producer calibration semantics / LRI origins
2. C6 routing
3. `src1` / `src2` pre-fusion merge/reduction mechanism
4. four-zoom merge topology closure
5. final merge acceptance / rejection logic

## Historical Dependency-Adjusted Order (Superseded)

Lane A refinement: SHA-pinned RTTI joined to admitted four-focal payload
packets now names visible `src1` exactly as A1-wide/B4-tele
`lt::ReferenceImageCache`, direct contributors as B1..B5/C1..C5
`lt::SourceImageCache`, and owner `+0x6a8` as `lt::ImageCaches`. Interpret the
raw-table phrase "semantic `src1` contents" as superseded; exact visible
`src2` semantics are now narrowed by exact RTTI to
`PipelineCache::processLevel1` one-source resampling/materialization.
Public identity of its `FusionCacheBayer` source descriptor and the distributed
reduction/acceptance policy remain open.

Raw WSJF is not the whole story, because some blockers unlock others.

Recommended execution order:

1. `src1` / `src2` pre-fusion merge/reduction mechanism
2. pair-grid producer calibration semantics / LRI origins
3. C6 routing
4. four-zoom merge topology closure
5. final merge acceptance / rejection logic

## Why The Dependency-Adjusted Order Differs

### 1. `src1` / `src2` should start first even though it is third in raw WSJF

- Reason:
  it is the most central architectural unknown and partially conditions both tele routing and full topology closure.
- Scope:
  the search should not require one tidy `N-to-1` reducer closure; it should accept a distributed mechanism if the proof points there.
- Current narrowed boundary:
  tele promoted target-2 records are now proven to enter concrete `0x2416d0` selected-index vectors, the small admitted promoted sets reach `(state=5,target=2)` stores, watched state-5 records continue into `0x244560 -> 0x25d090`, `0x25d090` is four-zoom-bounded as block pair-vector growth plus descriptor/geometry/active-byte gating, the `0x244560` / `0x245a40` caller decisions after paired `0x25d090` calls are four-zoom-bounded to exactly one active block with zero watched sentinel-fill aborts and live `0x2457c0` callsites, four-zoom runs prove `0x2457c0` materializes finite non-sentinel coordinate pairs into `state+0x1e8` from the state-5 store path, representative emitted pairs are read by `0xe8e70` vector-copy work under both State-helper copy-out paths, representative copied-destination pairs are touched again by `0xe8e70` under State-helper recopy plus higher node-vector materialization/copy sites, representative `0x22a61a` node-vector destination pairs now reach non-copy candidate/index/scoring-selection code under `0x21b2e0` / `0x21c4f0`, one copied node-destination pair per focal tier is now proven by same-address custody to be rewritten through `0x21b923` / `0x21b92a` into full `(-1.0, -1.0)` at `0x21b930` and sampled by downstream watchpoint stops while it remains `(-1.0, -1.0)`, and that downstream path executes coordinate-pair sentinel invalidation writes at `0x21b923` / `0x21b92a` across the four-zoom quartet, selected sentinel-marked pairs are later touched by downstream copy/record, coordinate scan, and scoring/materialization surfaces while sampled values remain `(-1.0, -1.0)`, selected tele sentinel-marked pairs that reach the `0x218b30` scoring/materialization guard are skipped at `0x218bc4 -> 0x218cb8` under admitted `70mm` / `150mm` samples, with direct branch-step confirmation for six watched `70mm` samples and three watched `150mm` samples, and static + runtime local-loop proof shows those admitted branch-step samples bypass the positive-coordinate `xmm1` accumulation / `r10d` update / `r9d` increment body, complete wide direct-census runs collect `152` sentinel pairs at `28mm` / `106` at `35mm` while `0x218bc4` records zero hits, and the sampled four-zoom `0x20b912` sentinel stops are now statically bounded to a local `0x20b5e0` nonpositive/sentinel path that bypasses `0x20bac0..0x20bac8`; downstream image/source-contribution consequences after the bounded `0x25d090 -> 0x244560/0x245a40 -> 0x2457c0` block-decision / coordinate-output path, downstream image/source-contribution consequences of sentinel-marked coordinate touches outside the admitted tele guard samples, wide direct-guard zero-hit site, and sampled `0x20b5e0` branch window, final acceptance/rejection, and reducer closure remain open.
- Latest additional narrowing:
  sampled tele sentinel-coordinate stops inside the `0x216f60` scan/count window are now statically bounded as local non-counting inputs because that body counts only pairs with both lanes positive and requires at least eight counted entries before continuing. Newer same-address runs also carry one representative copied node-destination pair per focal tier through `0x20b912` and prove the local `0x20b5e0` branch path steps to `0x20ba90` and then `0x20bafd` without visiting `0x20bac0..0x20bac8`, carry copied node-destination identity into the `0x20ca00` second local vector copy as a source-read at `0x20d309`, preserve the earlier capped non-selection result for the first watched representative per tier, and now extend selected representatives through destination-slot identity and `0x20d363 -> 0x20d565` at Unit-1 `28mm` (`source_index == gate_index == 5394`) and `70mm` (`source_index == gate_index == 77`) plus one Unit-2 `35mm` twin-capture run (`source_index == gate_index == 12`). Unit-2 anchor no-match windows and a no-match targeted repeat keep exact incidence non-constant. Byte-verified static proof further shows those selected branches bypass local keyed-node/coordinate writes and `ceres::Problem::AddResidualBlock`, while post-loop `ceres::Solve` remains outside the skip; vtable/typeinfo proof names `0x20ca00` as the `void(int,int,int)` callback inside `lt::Triangulator::refine3dPoints()`; pointer/stride custody binds its post-solve triple to the captured owner's `0x14`-stride record vector, whose `+0x10` field immediately feeds a positive min/max reduction into owner `+0x78/+0x7c`; exact cost type/formula proof names the bypassed residual as a unit-Cauchy one-scalar `ReProjectionCost` ray-depth objective; one complete Unit-1 `28mm` solve-only run captures 1,229 solve/write groups, 279 changed scalars, and final `record+0x10 == f32(solved scalar)` in every group; one watched Unit-1 `70mm` final `record+0x10` value now has same-address custody into helper `0x218940`'s local positive z-gated record/transform score body and `lt::SparseMirrorAngleOptimizer::optimize(...)::$_2` caller `0x219210`'s `[r14+0x18][r15]` output-vector store path; and one complete Unit-1 `70mm` parent-consumer packet carries the exact same closure, 1,089-float return-vector header, and begin pointer through 64 sampled post-store callback hits and matched `0x216f60` consumer, whose min-like scan selects index `505` before side-output gates and selected-record materialization for `0xf33d0`. The same evidence also links one representative tele copied node-destination address at `70mm` plus one at `150mm` to same-address `0x216f60` scan/count and `0x218bc4` score-guard operand samples while still full sentinel. Fresh same-address runs advance both pair identities through `0x218bc4 -> 0x218cb8`, proving representative exclusion from that loop's local score sum, over-threshold count, and positive-pair count. These narrow sampled local surfaces only; they do not close public depth units/LRI origin, stable solved-value distributions across bodies/focals/renders, shared-solve terminality, downstream image/source contribution, final acceptance/rejection, or reducer closure.
- Consequence:
  solving it early reduces the chance of spending time validating the wrong topology story.

### 2. pair-grid calibration semantics should start immediately as a parallel peer

- Reason:
  it has the best raw WSJF and a tight existing decode path; the storage, consumer formula, row matrix chain, tracked map-provider runtime path, descriptor custody boundary, and four-zoom depth-builder handoff are already proven.
- Consequence:
  it is ideal for a parallel lane because it is geometry-bounded and mostly independent of the `src1` / `src2` path.

### 3. C6 routing should start early but not be forced to full closure before `src1` / `src2`

- Reason:
  it already has concrete exclusions, a proven constructor/mutation chain, focused active key-list helper observations tied to the same item pointer cleared at `0x3c90a5`, helper-body classification as key-list bookkeeping, admitted tele runtime census coverage for all 58 direct static `call 0xf2720` sites, same-byte post-mutation watchpoint proof that the tracked active byte remains `0` at all observed later active-byte stops, selected-field watchpoint proof for pre-output `+0x60/+0x64` reads and cleanup-only watched pair/type ranges, a proven immediate post-mutation state consumer, a proven immediate downstream rect-vector builder path plus ImagePyramid consumer, a proven immediate per-level zero-fill callsite, selected later static `context+0x538` candidate families excluded by zero-hit four-zoom liveness, representative first-8-byte data watchpoints showing zero later touches for those watched ranges, expanded tele-grid first/middle/last data watchpoints across all five zero-filled levels showing zero later touches at `70mm` and `150mm`, and two proven filter points: the visible-`src1` keyed helper / vector-builder boundary and projection field-pack dispatcher boundary are excluded as positive C6 routes; key `15` is constructed active, queried while active by key-list and constructor-adjacent helper surfaces, later cleared at `0x3c90a5`, and then observed inactive in the state-classification walk; candidate downstream context route `0x3c9540 -> 0xe6c30` is zero-hit under tested tele bridge HDR; and the direct candidate loop plus stereo-side keyed-record loop both filter key `15` by post-mutation `object+0x30 = 0` under canonical bridge HDR runs. Final closure still needs untested-field/alias proof, proof of the final effect of watched `+0x60..+0x67` reads, unprobed range proof for later write/read and image-merge effect of the zero-filled ImagePyramid/geometry route, alternate image-effecting route proof, or terminal-filter proof.
- Consequence:
  early work is valuable, but final closure may depend on blocker `#1`.

### 4. Four-zoom topology closure should follow the structural answers

- Reason:
  this is partly validation closure, not just discovery.
- Consequence:
  it should consume the results of `src1` / `src2`, pair-grid, and tele routing work rather than trying to substitute for them.

### 5. Final merge acceptance / rejection logic remains high value but broad

- Reason:
  it matters enormously to ghost-free quality, but it is currently the least pinpointed of the five.
- Consequence:
  it can run as a deeper disassembly lane, but it should not block the more sharply-bounded investigations from going first.

## Parallelization Map

Yes, this blocker set naturally parallelizes.

Recommended immediate lanes:

### Lane A

- Topic:
  `src1` / `src2` reducer
- Primary doc:
  [BLOCKER_PATHS.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/canonical/BLOCKER_PATHS.md)
- Nature:
  LLDB + installed-vtable + upstream-from-wrapper decode; accepted selector-1 custody through keyed wide mean reprojection scoring / keyed tele matrix materialization, both outcomes of the corrected wide minimum selector, exact existing `state+0x448` record transfer into the per-camera `state+0xe0` selector-1 CalibStage bank, terminal BA camera-map normalization, same-key CalibStage write-back, one exact second-pass terminal-helper read, and post-terminal calibration-sibling finalization/diagnostic-route/constructor-publication/exact-owner-slot exclusion are closed. Sequential canonical four-focal joins also close the actual downstream route: the terminal whole-State root is retained at `PipelineCache+0x180` and feeds five `0x3f7040 -> PipelineCache+0x258` records, while the replacement sibling does not. The next step is whole-State public naming, tele alias/alternate-route proof, or another distributed merge/acceptance surface

### Lane B

- Topic:
  pair-grid producer calibration semantics / LRI origins
- Primary doc:
  [BLOCKER_PATHS.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/canonical/BLOCKER_PATHS.md)
- Nature:
  runtime/static tracing from the proven producer records back to public calibration fields and LRI block origins

### Lane C

- Topic:
  tele odd-camera routing / C6
- Primary docs:
  `c6_destination_and_depthcache.md`, `a2_destination.md`
- Nature:
  later write/read behavior and final C6 image/merge effect of the zero-filled post-mutation ImagePyramid/geometry route, untested-field/alias proof, final effect of watched `+0x60..+0x67` reads, alternate-route proof, or terminal-filter proof after the now-proven active `0x1bdbab` / `0x1bdbdd` observations, helper-body bookkeeping classification, `0x3c90a5` mutation, same-byte post-mutation watchpoint, selected-field watchpoint, state-classification consumer, rect-vector builder/ImagePyramid/zero-fill path, and post-mutation `object+0x30 = 0` direct/stereo gates
- Dependency note:
  can start immediately, but final closure may depend on Lane A

### Lane D

- Topic:
  final merge acceptance / rejection logic
- Primary docs:
  `iramp_substages_verified.md`, `iramp_kernel_body.md`
- Nature:
  bounded deeper disassembly after the now-proven IRAMP inner body, caller-side square-copy handoff, post-square vector-scale handoff, `0x3e5720` executor setup, row-callback binary16 conversion, owner `+0xf0` storage sink, first owner `+0xf0` downstream expansion family, immediate expansion handoff, expansion destination-context backing, first selected-cache route to `0x36f800`, first-owner branch census, first direct-branch post-route handoff, static selected-cache/post-route classification, static downstream direct-caller census, static selected-cache caller census, static `0x3e5720` caller census, static `0x3d4e10` caller census, static/runtime `0x3d5400` executor-route liveness, first weighted `0x36f800` store, row-cache helper formula, and fresh first-dispatch row-plan segment coverage
- Dependency note:
  mostly independent, but broader and expected to close slower than the higher-ranked lanes

### Lane E

- Topic:
  four-zoom topology closure
- Primary doc:
  [VALIDATION_POLICY.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/corpus/VALIDATION_POLICY.md)
- Nature:
  claim completion and validation on the canonical quartet
- Dependency note:
  should consume outputs from Lanes A through C before being frozen into spec

## Historical Near-Term Execution (Superseded)

If only one lane runs:

1. `src1` / `src2` reducer
2. pair-grid calibration semantics / LRI origins
3. C6 routing

If two lanes run in parallel:

1. Lane A: `src1` / `src2`
2. Lane B: pair-grid calibration semantics / LRI origins

If three lanes run in parallel:

1. Lane A: `src1` / `src2`
2. Lane B: pair-grid calibration semantics / LRI origins
3. Lane C: C6 routing

If four lanes run in parallel:

1. Lane A: `src1` / `src2`
2. Lane B: pair-grid calibration semantics / LRI origins
3. Lane C: C6 routing
4. Lane D: IRAMP acceptance / rejection decode

## Historical Practical Answer (Superseded)

If the question is "what should we do next, WSJF-adjusted, and can it be parallelized?", the answer is:

1. Start `src1` / `src2` decode now.
2. Start pair-grid calibration semantics / LRI origin decode in parallel immediately.
3. Start C6 routing work in parallel if capacity exists.
4. Hold four-zoom closure as the integration / validation lane.
5. Treat final merge acceptance / rejection logic as an important but broader deep-decode lane.
