# Merge-Critical Truth

This document contains only the facts that currently matter most for achieving a ghost-free L16 merge on modern hardware.

It is intentionally narrower than `docs/TRUTH.md`.

Every statement here must trace back to `CLAIM_LEDGER.md`.

## Success Standard

The target outcome is:

- no ghosting
- no trailers
- no contributor misregistration
- stable merge behavior across `28mm`, `35mm`, `70mm`, and `150mm`

If a claim does not change that outcome, it does not belong here.

## Current Closure Notes (TRUTH 3.0.295)

These notes supersede older caveat text later in this file:

- C6 is terminally excluded from canonical `70mm`/`150mm` bridge-HDR
  super-resolution image contribution. `CLM-C6-001` is
  `PROVEN`/`SPEC_READY`; non-bridge/GUI paths are out of scope.
- IRAMP overlap weights are the separable half-sample Hann window; shaping is
  orthonormal `I1/I2/I3 -> RGB`; the post-square vector is AWB; cache pixels
  are `Vec3<Float16>`; working rows are `vec4x32f`; and the tested four-focal
  CLI output is `linear_prophoto_rgb` to Radiance RGBE.
- Outer IRAMP converts both direct contributors and cropped `src2` from RGB
  into the installed I1/I2/I3 domain before patch preparation. Exact float32
  constants are `a=0.5773500204086304`, `b=0.7071099877357483`,
  `c=0.40825000405311584`, `d=0.8165000081062317`, with
  `I1=a(R+G+B)`, `I2=b(R-B)`, and `I3=cR-dG+cB`; lane 3 is preserved.
- SGM tuning is installed/body-independent: `P1=1`, nominal adaptive
  `P2/P1=500`, guide decay `log2(e)/(18,48,48)`.
- The terminal whole calibration State, not its replacement sibling, feeds
  five `0x3f7040 -> PipelineCache+0x258` warp-field records at every canonical
  focal tier.
- The active merge-critical unknowns remain the distributed pre-fusion
  `src1`/`src2` mechanism, residual implementation-required public-state
  semantics, and the complete final contributor acceptance/rejection
  predicate.

## Verified Core

### 1. IRAMP is real and merge-critical

- `CLM-MERGE-002`
- `ImageResolutionAmp` / IRAMP contains a real multi-source weighted accumulator inside `libcp`.
- The accumulator at `0x369fa1..0x369fa8` is now runtime-observed across the canonical `28mm`, `35mm`, `70mm`, and `150mm` bridge HDR seed files.

### 1a. The local IRAMP partner gate is now bounded, but not closed end-to-end

- `CLM-MERGE-005`
- Static installed-bundle proof shows `0x3692dc` compares a local partner-record vector begin/end pair and `0x3692e4` jumps to `0x369f2a` when the vector is empty.
- Static installed-bundle proof shows non-empty partner vectors fall through toward first SAD at `0x3694b1`.
- Partner records are `0x280` bytes.
- Runtime non-empty partner-vector states and first SAD hits are observed at `28mm`, `35mm`, `70mm`, and `150mm`.
- Runtime empty partner-vector states are observed at `28mm` and `70mm`; `35mm` and `150mm` empty-gate runtime hits are not proven.
- Follow-up branch-target proof bounds the local index sentinel gate: capped packets reach `0x36931b` with `eax == 0x80000000` and reach `0x369320` with non-sentinel `eax` values whose low table dword at `r12 + rsi * 8` matches `eax` on all four canonical focal tiers. This is local branch-target proof, not full candidate-policy closure.
- Static plus runtime proof now shows the first-hit partner-record append/population path reaches `0x368b02` at `28mm`, `35mm`, `70mm`, and `150mm`.
- A populated `0x280` record is physically four int32 scalar fields followed by thirteen contiguous `0x30` descriptor-like blocks.
- Static plus runtime proof now also bounds the live non-empty consumer path through coarse SIMD SAD / `phminposuw` winner selection, local absolute-difference refinement, guarded float refinement, 16x16 bilinear vec4 resampling, `0x36cde0`, and a three-float scratch write at `0x369e7e..0x369e91` across all four canonical zooms.
- Follow-up proof closes the clean-room formula of `0x36cde0`: candidate lane-0 L1 normalization, 256-sample fine and 64-sample coarse structural vectors, fixed lifting/detail factors, per-scale `min4`, and `sqrt(coarse_min*fine_min)`. One canonical Unit-1 `35mm` prepared-input replay matches live float bits exactly.
- Follow-up proof closes the color-domain entry point before that score path. Direct contributors are converted in place at `0x366fd0..0x3670a8`; cropped `src2` is converted at `0x368ce0..0x368db8` before `0x3692c6 -> 0x36b920`; and `src1` remains the separate byte guide. Both loops use the exact installed forward I1/I2/I3 matrix and preserve lane 3. `0x36b920` contains no cross-channel lane mixing and is patch/spatial preparation, not color conversion. Installed formula plus complete prior IRAMP liveness covers all four canonical focals; the new coefficient capture observes both roles at Unit-1 `28mm`.
- Follow-up W5 reproduction proof captures representative non-degenerate `0x36e511 -> 0x36e515` score arithmetic on all four canonical focal tiers; the admitted fact is live `sqrt(xmm0*xmm1)` magnitude behavior, not Opus's exact sample rows as constants.
- Follow-up proof closes the clean-room formula of `0x36e530`: five `vec4` reciprocal normalizers selected by `min(v2(x),v2(y),4)`, then exact inverse CDF 9/7 lifting at strides `8,4,2,1`, horizontal before vertical, returning `scratch+0x1580`. All `65,536` basis outputs and two live whole-buffer replays match exactly.
- Follow-up proof closes the matching reference prior: `0x36b920` seeds all 256 numerator coefficients with exact float32 `0.2f * forward97(src2)` and seeds all five scale denominator vectors with exact float32 `0.2f` before candidate additions. A retained baseline-only packet reconstructs the raw `src2` patch through the admitted inverse to maximum absolute float32 lifting error `1.9073486328125e-06`; existing complete reports join the fixed construction to all four canonical focals.
- Follow-up proof bounds the first downstream tuple consumer: it reads the third tuple scalar at `0x36a7d8`, reads the first two tuple floats at `0x36a803` / `0x36a814`, passes an adjusted coordinate pair to `0x372a00`, forms multiplier `(t + 2 * max(0, t - 0.5), t, t, t)`, reaches multiply-add loop `0x36a8c0..0x36a8cb` across all four canonical zooms, and adds the third scalar into a running scalar sum initialized from `0x5df904 = 0.200000003`.
- Follow-up proof bounds the immediate post-reciprocal weighted-add path: `0x19e7d0` copies/scales descriptor-backed `vec4` buffers by the reciprocal vector, and `0x36aa30..0x36aa57` blends `reciprocal * 0.2` into lane 3, applies `weight[inner] * weight[outer]`, adds into the destination `vec4`, and is reached across all four canonical zooms.
- Follow-up W5 reproduction proof captures non-common `xmm2` denominators at `0x36a938` on all four canonical focal tiers and verifies the post-`rcpss` value approximates `1/xmm2`; this proves representative denominator magnitude behavior, not a full distribution.
- Follow-up proof bounds the immediate post-weighted-add shaping path: `0x36abf0..0x36ac15` applies a lane-3-weighted clamped vector update with first-hit scale `(2,0,0,0)` and clamp bounds `[-0.1,0.1]`, and `0x36ad50..0x36adac` applies a fixed 3-vector transform with lane 3 forced to `1.0`; both sites are reached across all four canonical zooms.
- Follow-up proof bounds the caller-side handoff after IRAMP returns: the caller validates the `rbp-0x60` descriptor dimensions against ROI, wraps it at `rbp-0x88`, calls helper `0xd76a0`, and static helper inspection shows source `vec4` lanes are squared into the destination descriptor; the handoff is reached across all four canonical zooms.
- Follow-up proof bounds the caller-side vector-scale handoff after square-copy: the caller builds a wrapper over the `rbp-0x70` descriptor with a vector at wrapper `+0x10`, calls helper `0x2d7320`, and static helper inspection shows source `vec4` lanes are multiplied by that vector into the destination descriptor; the handoff is reached across all four canonical zooms.
- Follow-up proof bounds the caller-side `0x3e5720` executor setup after vector-scale: it allocates/resizes a 6-byte-element destination descriptor, builds callback vtable `0x66b020`, dispatches generic executor `0x5670`, and its visible worker maps source 16-byte `vec4` rows to destination 6-byte rows before calling row callback `0x38a30`; the setup is reached across all four canonical zooms.
- Follow-up proof bounds row callback `0x38a30`: for the observed `512`-wide rows, it repacks source `vec4` lanes 0..2 into float triples, calls `0xbfef0` with `ecx = 0` and count `3 * width`, and the used `0xbfef0` branch converts float channels to 16-bit binary16 bit patterns; first captured callback rows match the static conversion formula across all four canonical zooms.
- Follow-up proof bounds the immediate caller-side storage sink for the `0x3e5720` conversion output: body `0x3ec960` is vtable slot `0x65f5e0+0x30`, computes destination descriptor `(*rsi)+0xf0`, allocates/resizes it with element size `6`, passes it to `0x3e5720`, then destroys only the temporary descriptor and returns; runtime packets across all four canonical zooms show the owner-backed descriptor populated as `512x512`, stride `512`.
- Follow-up proof bounds the first proven downstream consumer family for owner `+0xf0`: `0x3d50f0` allocates a `16`-byte-element destination, dispatches executor `0x5670` with row worker `0x3d5290`, and the selected converter path reaches `0x2ff00 -> 0xc0410`; runtime packets across all four canonical zooms show live `rsi` at `0xc0410` inside the exact owner `+0xf0` data range with `ecx/cl = 0`.
- Follow-up proof bounds the immediate handoff after that owner `+0xf0` expansion family: `0x3d4e10` calls `0x3d50f0`, and runtime packets at `0x3d502e` across all four canonical zooms show the local source descriptor at `rbp-0x60` points inside the exact owner `+0xf0` data range while the local expanded descriptor at `rbp-0x90` has `16`-byte elements and a first `vec4` sample with lane 3 = `1.0`.
- Follow-up proof bounds the destination backing store for that expansion handoff: `0x3d4e10` receives a caller-provided context whose `+0x10` field points to the persistent 16-byte destination descriptor, and runtime packets across all four canonical zooms show local destination descriptor `rbp-0x90` is a clipped view into that context descriptor with matching `qword_28`, in-range data pointer, and 16-byte alignment.
- Follow-up proof bounds the first captured route after that expansion destination context: the accepted first route uses active callable branch `0x3d4842`, active callable slot `0x3ec960`, parent `0x3d01b0` output descriptor `rbp-0x148` as `context+0x10`, caller return `0x3d084d`, and passes the same temporary descriptor to `0x36f800` at `0x3d08ce` across all four canonical zooms.
- Follow-up first-owner census proves sibling direct branch `0x3d4864` is live for the first captured owner `+0xf0` descriptor at `28mm`, `70mm`, and `150mm`; `35mm` accepted only `0x3d4842`. Every accepted census packet still uses active callable slot `0x3ec960`, returns to selected-cache caller `0x3d084d`, and preserves the parent/context destination equality checks.
- Follow-up direct-branch post-route proof shows the first owner-matching direct branch at `28mm`, `70mm`, and `150mm` reaches `0x3d08ce -> 0x36f800` with `rsi` equal to the same temporary descriptor captured as `context+0x10`; `35mm` has no owner-matching direct branch under that first-owner probe.
- Follow-up global branch-site census removes the first-owner gate and counts every hit at `0x3d4842` / `0x3d4864` during complete bridge HDR renders. Across the canonical quartet, all hits preserve the same parent/context equality checks and fall into caller set `{0x3d0732, 0x3d084d, 0x3ecc5a}` plus active callable slot set `{0x3ec960, 0x3e4a80}`.
- Follow-up post-route family proof classifies those caller families across the canonical quartet: `0x3d0732` is exact-size cleanup with no post call, `0x3d084d` reaches `0x3d08ce -> 0x36f800`, and `0x3ecc5a` reaches `0x3ecc74 -> 0x3edb80` visible-`src1` one-image normalization.
- Follow-up parent-chain ancestry proof shows `0x3d0732` returns through `0x3b07a9 -> 0x41a8d3 -> 0x3adfce -> 0x280e`, `0x3d084d` returns through `0x3bb822 -> 0x3adfce -> 0x280e`, and visible-`src1` `0x3ecc5a` returns through `0x374cf3 -> 0x3665da -> 0x365f50 -> 0x3ec7df -> 0x3eca4b -> 0x3d4842` with some nested read-context continuations. Exact hot direct-branch hit totals are evidence-run counts, not algorithm constants.
- Follow-up static parent-chain body classification separates callback/iteration glue (`0x280e`, `0x3adfce`) from selected owner-cache/direct-render tile surfaces (`0x3b0740`, `0x41a7d0`, `0x3b9770`, `0xfbda0`, `0x3bb2b0`) and visible-`src1` / IRAMP nested wrapper plus owner `+0xf0` sink surfaces (`0x374ac0`, `0x3661b0`, `0x365960`, `0x3ec770`, `0x3ec960`). This narrows the downstream policy search, but does not identify the final file/display sink or final contributor acceptance/rejection.
- Follow-up static helper-surface classification bounds helper surfaces exposed by that route: `0x31b110` is source/RAW/STD adapter into `0x33fb30`; `0xfe720` builds/clamps 16-byte rectangle records; `0x106cb0` constructs/interpolates vignetting data; `0x2e20` dispatches callbacks; `0xf3570`, `0x3b9660`, `0x3c6ac0`, `0x1bea20`, `0x1bea00`, and `0x1be970` are owner/tile/map/field helpers. This is route plumbing, not final file/display sink, final contributor acceptance/rejection, or `src1` / `src2` semantic closure.
- Follow-up static selected-cache/post-route classification bounds the visible selected-cache bodies exposed by that route: `0x3d01b0` is level/ROI tile-read executor; `0x3d0650` is exact-size read or read-then-`0x36f800` rescale; `0x3d47d0` is read-context branch routing; `0x3d4e10`, `0x3d50f0`, `0x3d5290`, `0x2ff00`, and `0xc0410` are clipped-view / 6-byte-to-vec4 / 16-bit-to-float conversion plumbing; and `0x3edb80` is one-image `sqrt(max())` normalization. This is selected-cache/post-route plumbing, not final file/display sink, final contributor acceptance/rejection, or `src1` / `src2` semantic closure.
- Follow-up static downstream direct-caller census bounds direct callers of selected downstream helpers in the repo-local static callgraph: `0x36f800` direct callers are selected-cache read/rescale, TileCache-like read/rescale, and IRAMP-internal resample handoff; `0x3d01b0` direct callers are selected-cache reads, visible-`src1` read, source-adapter caller, and DOFCache render caller; `0x3edb80` direct callers are visible-`src1` and visible-`src2` one-image normalization wrappers; and `0x3d50f0` has only the already classified `0x3d4e10` direct caller. This is direct-callgraph coverage only, not indirect/vtable caller closure, final file/display sink, final contributor acceptance/rejection, or `src1` / `src2` semantic closure.
- Follow-up static selected-cache caller census bounds direct callers of `0x3d0650` in the repo-local static callgraph. The 14 direct callers fall into source-adapter-style caller windows, small owner-cache selector `0x3b0740`, multi-branch owner/tile-cache surface `0x3bb2b0`, owner `+0xf0` output-sink branch body `0x3ec960`, and later helper/adaptor caller surfaces around `0x42fb40` and `0x42fd30`. This is direct-callgraph coverage only, not runtime liveness for every static caller, indirect/vtable caller closure, final file/display sink, final contributor acceptance/rejection, or `src1` / `src2` semantic closure.
- Follow-up static `0x3e5720` caller census bounds direct callers of the row-conversion executor setup in the repo-local static callgraph. The only direct callers are active-callable-slot / owner `+0xf0` writer body `0x3e4a80`, owner `+0xf0` output-sink body `0x3ec960`, and DOFCache render body `0x3f0b90`; ancillary `0x432db0` coverage bounds the later selected-cache caller surface `0x42fb40 -> 0x3d0650 -> 0x432db0`. This is direct-callgraph coverage only, not runtime liveness for every static caller, indirect/vtable caller closure, final file/display sink, final contributor acceptance/rejection, or `src1` / `src2` semantic closure.
- Follow-up static `0x3d4e10` caller census bounds direct callers of the owner `+0xf0` expansion handoff in the repo-local static callgraph. The only direct callers are the two already bounded branch-router post-branch handoffs at `0x3d484a` and `0x3d486c`, plus separate indexed-entry loop caller `0x3d5468` inside body `0x3d5400`; `0x3d50f0` has only direct caller `0x3d5029` inside `0x3d4e10`, and `0x3d5290` has no direct callers because it is worker-dispatch plumbing. Follow-up static/runtime proof binds that separate loop caller to vtable route `0x66a728/+0x30 -> 0x3d53c0 -> 0x3d5400 -> 0x3d5468 -> 0x3d4e10`, and first-hit probes prove liveness at `28mm`, `35mm`, `70mm`, and `150mm`. This is direct-callgraph plus first-hit executor-route coverage only, not full-render counts, indirect/vtable caller closure outside the bounded route, final file/display sink, final contributor acceptance/rejection, or public semantic closure.
- Follow-up proof bounds the first gated `0x36f800` worker path after that owner `+0xf0` route: callback slot `0x3721d0` enters static worker body `0x372210`, runtime packets reach after-prologue worker-entry probe `0x372224`, and the first captured store at `0x372488` equals four captured source `vec4`s multiplied by four captured weight `vec4`s across all four canonical zooms.
- Follow-up proof bounds row-plan/cache helper activity inside that same route: `0x372210` converts offset/scale doubles to signed 16.16 fixed-point, `0x372500` builds the row-plan/cache struct, captured `0x372760` row-cache stores match the reconstructed 4-tap horizontal `vec4` formula across all four canonical zooms, and fresh first-dispatch row-plan packets capture all four unique worker regions per zoom with only the middle row-cache segment predicted/live in that dispatch.
- Follow-up full-render row-cache segment census proves leading/trailing `0x372760` store sites are live at `28mm` and `70mm`, while `35mm` and `150mm` have zero leading/trailing hits under the tested canonical bridge HDR runs.
- This refutes absolute "28mm never runs SAD" language, "`0x3d4842` only" owner-route generalizations, any assumption that exact hot direct-branch / first-hit probe totals are algorithm constants, and any global-dead claim for the installed leading/trailing row-cache loops. It does not close public semantic names for partner-record fields, tuple fields, weights, shaping vectors, the post-square scale vector, row-callback channels / pixel format, owner-cache/direct-render tile surfaces, downstream row-image/final policy after the classified caller/helper/post-route/direct-caller/selected-cache-caller/`0x3e5720`-caller/`0x3d4e10`-caller/`0x3d5400`-executor-route families, the complete candidate predicate, or final acceptance/rejection.

### 2. IRAMP is not a single-anchor function

- `CLM-MERGE-003`
- On the tested bridge HDR path, IRAMP receives two anchor-side IGs plus a 5-element contributor vector.
- Static bundle proof ties that live call shape to `PipelineCache+0x238`, `+0x248`, `+0x270`, `+0x258`, `+0x1e8`, and the ROI passed into `processLevel0`.
- LLDB runtime proof now directly verifies that same `src1`, `src2`, `srcs[5]`, `warps[5]`, scale, and ROI signature on the canonical `28mm`, `35mm`, `70mm`, and `150mm` bridge HDR seed files.
- Follow-up LLDB runtime proof inside `0x3661b0` verifies the live count-use window at `0x366a50..0x366a65`: the body reads a vector header through `r15+0x18`, computes `(end-begin)/16`, and reaches `0x366a65` with live count `5` across 16 capped packets per canonical focal tier. This confirms count use only, not complete contributor acceptance or full reducer closure.
- That input shape matters because a parity renderer that collapses IRAMP to one anchor plus ad-hoc extras will target the wrong merge topology.

### 3. The direct IRAMP contributor vector identity is known

- `CLM-MERGE-004`
- On the corrected canonical bridge HDR quartet, the five direct IRAMP contributor source-vector items are `B1..B5` at `28mm` and `35mm`.
- On the corrected canonical bridge HDR quartet, the five direct IRAMP contributor source-vector items are `C1..C5` at `70mm` and `150mm`.
- This matters because contributor identity is no longer a free variable for the direct IRAMP vector.
- This does not close `src1` / `src2` composition or C6 routing.

### 4. The warp-grid is ROI-derived and transformed before block handling

- `CLM-WARP-001`
- `CLM-WARP-002`
- `CLM-WARP-003`
- `0xf540` does allocation/setup, not pair writing.
- The backing store holds packed int32 `(x, y)` pairs written later at `0x366520..0x366523`.
- Installed-bundle proof now also shows the first grid is derived from the ROI rect on the 8-pixel lattice, then a second same-sized transformed pair grid is produced before bbox / clipping handling.
- Installed-bundle proof now also ties the second-grid transform records to the live `PipelineCache+0x258` vector passed into IRAMP and consumed per matching source-vector index.
- Installed-bundle proof now also gives the consumer-side transform formula: sampled-map-modulated vec4 projection, divide by component 2, `[-8, dim + 7)` acceptance, add `0.5`, and int32 pair write.
- Installed-bundle proof now also bounds the producer side: `0x3f7040` dispatches same-category and cross-category paths, both converge on `0x25e500`, `0x25e500` writes row fields through `0x25e0c0` and stores the caller-supplied map pointer at `+0x40`, the post-wrapper `initResAmp` insertion path normalizes `+0x48/+0x4c` to `(1.0, 1.0)`, the row producer computes `source_b_product * inverse(source_a_product)`, the immediate source-record constructor / map-provider topology is bounded, `0x23faf0` is bounded as source-record composition, `0x264980` as a two-axis field-shift helper, `0x264460` as a positive two-axis scale helper, `state+0xe0` object resolution is bounded through `0x1be970` / `0xe6ba0`, `0x264270` helper access is bounded through `0xf34e0` CalibStage banks, `0xf3350`, and `0xf3360`, and `state+0x448` is bounded as a keyed tree/control object with a first visible byte-gated insertion loop, first payload-field copies, and later direct payload writes through `+0x80`. Four-zoom runtime proof now binds the tracked post-wrapper map-provider path to cross-category `0x3f72f0`, `UpsampleLayer` vtable address point `0x658eb0`, slot `+0x90 = 0x26b590`, `0x26b590` returning `UpsampleLayer+0x90`, and that return written to `record+0x40`; accepted `28mm`, `35mm`, and `70mm` writer-core probes additionally bind `0x26ac13 -> 0xf340` to the populated `UpsampleLayer+0x90` descriptor copied before provider use. Four-zoom builder probes bind `0x26aa10 -> 0x29ed90 -> 0x2673a0 -> 0x26ac13` as the depth-path builder that turns a previous-layer `+0x90` `2080 x 1560` descriptor into the `4160 x 3120` `UpsampleLayer+0x90` descriptor consumed through `record+0x40`; installed debug strings label that descriptor as `depth_... .dp`.
- Follow-up Lane B public-origin proof admits one scoped `state+0x448` payload slice: the first visible insertion/update path copies public 32,832-byte intrinsics-block pose rotation into payload `+0x00..+0x20` and the matching translation into `+0x24..+0x2c`, with anchor `A1` at `28mm` / `35mm` and anchor `B4` at `70mm` / `150mm`, shared across first-pass inserted keys. Tele public-fired `C6` is not inserted by that first visible path, and checked later `+0x30..+0x3c` source slices have zero exact public fixed32-sequence hits. A follow-up later-box formula proof closes formula-level meaning for those later fields: payload `+0x30/+0x34` is uniform scale and `+0x38/+0x3c` is box origin from `0x260e40` over the `0x145980(object)` box and `object+0x114/+0x118 = [4160,3120]`. Full `state+0x448` payload semantics remain open.
- Companion static-origin, embedded-schema, and two-body runtime proof closes the public source of that later-box formula: the size pair is the LRI-stored `CameraModule.sensor_data_surface.size`, and the converted same-camera record supplying `0x145590 -> 0x145980` is copied exactly from `FactoryModuleCalibration.geometry.distortion.polynomial.{distortion_center, normalization, coeffs, fit_cost}`. The computed envelope, uniform scale, and full `state+0x448` payload remain derived rather than direct public fields.
- Deterministic extraction of the installed protobuf descriptors, checked against representative wide/tele LRIs from both physical calibration bodies, names the prior anonymous Lane B carriers as `LightHeader.modules`, `CameraModule.id/mirror_position/lens_position/sensor_exposure/sensor_temparature/sensor_data_surface.size`, and `FactoryModuleCalibration.geometry.per_focus_calibration[].intrinsics.k_mat/focus_hall_code/extrinsics.canonical.rotation/translation`. Combined with the accepted runtime custody, the captured `0x1f96e0` K helper is focus-dependent intrinsics evaluation over calibrated focus Hall codes at live lens position. Later complete installed-writer census and exact-focal two-body watches map `CalibStage 0 = factory` at `CapturedImage+0x180` and `CalibStage 1 = current` at `+0x12c`; this still does not identify the full `state+0xe0` / `state+0x448` records as direct protobuf records.
- SHA-pinned `shared_ptr_emplace<lt::CapturedImage>` RTTI and same-process pointer custody identify every selected index-5 `state+0xe0` object exactly as `lt::CapturedImage` across the Unit-1 focal quartet plus exact-28mm Unit-2. Static RTTI/construction/pointer custody further names `state+0xe0/+0xe8` as retained `shared_ptr<lt::RawImageFactory>`, backed by `shared_ptr<lt::CaptureStack>`. Embedded-schema/parser/copy proof names `CapturedImage+0x64` as public `CameraModule.frame_index` and factory `+0x10` as its selected-frame lookup key; the admitted renderer-owner path selects frame `0`. `CalibStage 0` is `factory` at `CapturedImage+0x180`; `CalibStage 1` is `current` at `+0x12c`. Whole `state+0x448`, complete bank-field identity, and final effect remain open.
- Embedded-schema, two-body raw-wire, pinned constructor-copy, and Unit-1 four-focal runtime proof names `CapturedImage+0x30` exactly as public `CameraModule.is_enabled`. The C6/key-15 clear at `0x3c90a5` and tested downstream `+0x30` gates therefore mutate/read the runtime `is_enabled` copy. This does not prove terminality, alternate-route absence, or final image effect.
- Pinned constructor copies plus `42` Unit-1 four-focal events name four more direct capture fields: `CapturedImage+0x38 = CameraModule.sensor_exposure`, `+0x40 = sensor_analog_gain`, optional `+0x44 = sensor_digital_gain`, and optional `+0x104 = sensor_temparature`. Exact-wide/tele Unit-2 LRIs verify the public carriers, not a Unit-2 constructor-runtime join. The separate frame-index proof uses discriminating burst LRIs on both bodies. Numeric selector-bank names are now closed; remaining object/bank fields and final effect stay open.
- Follow-up four-zoom custody proof binds that previous-layer `2080 x 1560` low-resolution float source descriptor to `StereoLayer<false>` index `5`, mode `8`, tile `1`, descriptor `+0x2a8`, and vtable slot `+0x90 = 0x26fb50` returning `this+0x2a8`; public LRI origin and public semantic name remain open.
- Follow-up four-zoom runtime/static proof bounds the immediate upstream producer for the `0x267010` source descriptor: `0x26e4c6 -> 0x299c70` receives `this+0xf8`, builds a 2-byte descriptor at caller `rbp-0xe0`, moves it into `rbp-0x80`, and passes that same descriptor to `0x267010`; the lookup-vector argument is `this+0xe0`. A follow-up four-zoom worker proof binds callback address point `0x6680f0` through generic executor `0x5440` slot `+0x30` to `0x29a670` and validates the sampled source-record min-cost `uint16` write formula. Public field names, LRI/protobuf origin for the source-index descriptor, source-record public names, physical meaning, remaining full-map distributions beyond the admitted source-local byte-span/mask census, and final merge effect remain open.
- Follow-up four-zoom runtime watchpoint proof bounds the internal construction/custody for those same tracked index-5 inputs: `StereoLayer<false>+0xe0` is populated through the `0xf02d0` path with final observed write at `0xf043e` and counts `752` for `28mm` / `35mm` and `1472` for `70mm` / `150mm`; `StereoLayer<false>+0xf8` receives a source-control write at `0x26be62`; and later populated samples at `0x26e4c6`, `0x299c70`, and `0x267010` preserve the `this+0xf8` / `this+0xe0` argument relationships with source dimensions `2080 x 1560`, stride `2080`. Public origin, physical meaning, remaining full-map distributions beyond the admitted source-local byte-span/mask census, and final merge effect remain open.
- Follow-up lookup-vector public-origin verifier proves the tracked `StereoLayer<false>+0xe0` table is internally generated through `0x28fa60` / `0x28f5a0` / `0x28f860` from retained fields `this+0x298/+0x29c = [200.0, 640000.0]` as an exact float32 reciprocal near/far ramp, copied by `0xf02d0`, and later consumed unchanged by `0x267010`; direct LRI block and public calibration fixed32 checks find zero hits for the vector. This closes internal generated-table mechanics only; source-index descriptor semantics, source-record public names, public calibration/LRI/protobuf origin/names, and final merge effect remain open.
- Follow-up endpoint/count verifier proves the selected `[200.0, 640000.0]` endpoint pair comes from static binary float tables at `0x609428` / `0x609430`, is propagated through `0x3ff43c -> 0x2681b0 -> 0x26ba90`, and is stored in `this+0x298/+0x29c`; the lookup count is computed by `0x28f5a0` from five `0xa8` source records in `this+0x258`, `this+0x18`, first-record scalar, endpoint reciprocal span, clamp `0x1000`, and mode rounding by `this+0xc = 8`. Deterministic depth-bound custody traces that same selected pair through `state+0x100/+0x104` and Triangulator owner `+0x70/+0x74` into Ceres lower/upper bounds on the one-scalar ray-depth reprojection problem, while complete canonical Unit-1 reports select mode `0` and `[200.0,640000.0]` at all four focal tiers. This admits the lookup's internal reciprocal ray-depth hypothesis-grid role; source-index/source-record public semantics, public calibration/LRI/protobuf origin and names, and final merge effect remain open.
- Follow-up SHA-pinned static proof plus complete Unit-1 four-focal runtime and an exact-focal Unit-2 `28mm` body discriminator joins all six index-5 descriptors and exact `UpsampleLayer+0x90` descriptor through depth-cache promotion to the public GDepth writer. The admitted chain preserves length dimensions through reciprocal conversion/resize/reconversion and serializes the provider's exact extrema with `GDepth:Units="mm"`. Ray-depth scalars, `[200,640000]` bounds, and depth-map pixels therefore use `mm`; the generated reciprocal lookup uses `mm^-1`. Complete installed caller census further proves the sole owner call hardcodes mode `0`, so the pair is installed Triangulator ray-depth lower/upper bounds, not public LRI/calibration/protobuf fields. Public source-index names and unrelated whole-record semantics, and final merge effect remain open.
- Installed-label xrefs joined to the admitted range-builder, source-index custody, worker, and endpoint/count reports exactly name `StereoLayer+0x2a8` as `Depth map`, `+0x208` as `Skip mask`, `+0xf8` as `Cost volume`, `+0x240` as `Images`, and the generated `(lower,count)` descriptor as `Range map`. The `0x299c70` formula makes its output a minimum-cost depth-hypothesis index map over per-pixel cost-volume records. The parallel `+0x258` vector is one per-image composed geometry record per Images item, built through `state+0xe0`, same-key `state+0x448`, `0x264440`, and `0x23faf0`.
- SHA-pinned static proof plus complete Unit-1 four-focal runtime and an exact-focal Unit-2 `28mm` discriminator closes those five records' whole-field operational identity as derived per-image, tier-anchor-relative calibrated camera models. Their fields carry composed focus-dependent intrinsics, anchor-relative extrinsics, a two-axis adjustment tuple, exact same-camera public distortion coefficients, and a composed distortion normalization/center matrix. Wide order is `A1,A5,A2,A3,A4` around `A1`; tele order is `B4,B2,B5,B1,B3` around `B4`; `0x28f5a0` uses their maximum inverse-extrinsic-center separation. They are derived rather than direct protobuf copies. Remaining cost-volume operand names/origins, complete selector-bank and whole-State identity, and final merge effect remain open.
- Pinned installed labels plus the accepted four-focal `0x276860` operand packets name sampled worker storage as tier-anchor `Guidance` (`A1` wide, `B4` tele), `Pixel buf`, `Min cost buf`, and `Line buf`. Guidance reuses the first `Images` descriptor and its bytes are locally expanded into Pixel buf vectors.
- The omitted G-40 predecessor pool is now exact: selected `StereoLayer+0x14=4` gives clamped spatial offsets `{-1,0,1,2}` on both axes; `0x298ff0` computes min/max prior hypothesis indices over that 4x4 footprint using only positions whose prior `Skip mask` byte is nonzero, with empty-set output `(65535,0)`. The later range-builder applies separate hypothesis padding `target+0x10=1`. Exact five-transition replay is Unit-1 `28mm`; kernel/liveness receipts cover Unit-1 four-focal plus exact-focal Unit-2 `28mm`. Symmetric radius-2 and mask-ignoring substitutes are refuted.
- Installed-static proof plus complete exact-focal Unit-1/Unit-2 `28/35/70/150mm` captures closes the mode-8 per-level projection policy. Guidance levels `0..5` are `65x49,130x98,260x195,520x390,1040x780,2080x1560`; `StereoLayer+0x1c` is `32,16,8,4,2,1`; all five `Images` stay `2080x1560`; all projection scales stay `(1,1)`; and each source's full composed record is invariant across levels. Each coarse coordinate is lifted by `full=min(step*level_coord+floor(step/2),image_extent-1)` before the unchanged full-domain correspondence. `H_level=D*H*D^-1` is refuted for this path.
- SHA-pinned constructor/recurrence proof closes target `+0x56 = 1` as adjacent-hypothesis SGM penalty `P1`, target `+0x58 = 500.0` as the nominal guide-adaptive `P2/P1` ceiling scale, and target `+0x60 = log2(e)/(18,48,48)` as the three-channel exponential guide-distance decay vector. Their producer uses installed literals/constants with no LRI/calibration input, so they are body-independent tuning.
- SHA-pinned allocator/helper/worker proof plus accepted four-focal packets closes the sampled recurrence roles: predecessor candidates and current path output use `Line buf`; `%xmm2` is the prior `Min cost buf` directional minimum and normalization baseline; `%xmm3` is that baseline plus guide-adaptive `P2`; and `[r10+2*rdx]` is the local matching-cost temporary. The current minimum returns to the other `Min cost buf` half and current path cost accumulates into the Cost-volume payload. These are generated SGM terms. Stable full-map distributions remain validation scope; Guidance component/formula semantics are closed below.
- The G-42-to-G-43 representation is exact: raw summed `uint16` costs are multiplied in binary32 by `float32(float32(1/27)/float32(source_count))` and truncated toward zero in place. Selected source count `4` gives `0.0092592593282461166f`; G-43 reads the same normalized temporary directly. There is no per-pixel band-min pedestal. Exact-focal Unit-1/Unit-2 `28mm` custody joins prior Unit-1 four-focal liveness.
- Selected pattern-2 consumption is exact: mask byte `0` computes the normalized G-42 unary term, while nonzero bytes provide an all-zero local vector. Both arms execute all eight SGM paths, accumulate an ordinary per-pixel Cost-volume record, and reach the first-minimum absolute-hypothesis selector. Exact-focal Unit-1/Unit-2 `28mm` captures show nonzero completed records for skipped pixels. The later guided 2x upsample consumes an already-complete `2080x1560` Depth map rather than filling pattern-2 holes.
- SHA-pinned RTTI/call-chain proof plus a Unit-1 `28mm` descriptor watch names first `Images` and reused `Guidance` as the tier-anchor A1-wide/B4-tele `CapturedImage` product of `lt::StereoISP::CreateStereoImage`, with public output type `Image<vec4x8ui>`. The exact returned descriptor is cached by camera key, recovered by `0x226410`, and copied into `StereoLayer+0x240` `Images[0]`; public `CapturedImage`, `CalibData`, and `SoftISP` inputs feed the generated product. Per-component `vec4x8ui` semantics and a direct Unit-2 descriptor-equality packet remain open.
- Follow-up component-route proof binds key `0` to the A1/A1 producer call. Complete Unit-1 four-focal plus exact-focal Unit-2 property capture and all-phase E3 proof close the pre-YUV collapse2 intermediate as `[R,0.5*(G1+G2),B,1]`. Complete-frame Unit-1 exact-28mm proof corrects the selected default hot-pixel worker to one rolling-row rank-6 residual, bit-exact LUT generation, `4*LUT` threshold, marker lifecycle, and a row-varying isolation selector. Superseding installed/runtime proof closes its global edge through the exact six-pixel parity-preserving upper-median halo and complete full-frame replays on Unit-1 exact-`28mm`, Unit-2 exact-`28mm`, and Unit-1 canonical-`35mm`. Corrective direct-custody/full-plane proof closes the following `StereoISP::ConvertToYUV` matrix, signed-power, offset, and byte pack, naming final Guidance `[Y,U,V,1]`. The YUV formula has prior Unit-1 four-focal liveness and complete Unit-1 second-scene plus exact-focal Unit-2 `28mm` discriminators.
- Follow-up four-zoom static/runtime proof bounds the immediate field assembly path for the tracked `StereoLayer<false>+0xf8` source object: `0x29a140` produces stack locals, `0x26be5b` writes control value `8` into `this+0xf8`, `0x28f420` moves the three-qword header into `this+0x100`, and `0xf340` moves the `2080 x 1560`, stride-`2080` descriptor into `this+0x118`. Public origin, physical meaning, remaining full-map distributions beyond the admitted source-local byte-span/mask census, and final merge effect remain open.
- Follow-up four-zoom static/runtime proof now bounds the immediate `0x29a140` producer body behind that field-assembly edge: it stores incoming control `8`, zeroes output fields `+0x08..+0x48`, calls `0x299eb0`, calls `0x28f490` with destination `output+0x08`, `rsi = rax`, and `edx = 0x40`, then calls `0x299fd0`. Runtime probes validate the caller/entry arguments, zero-to-populated header transition, zero-to-populated `2080 x 1560`, stride-`2080` descriptor transition, sampled record-base/offset-table state, caller-post local continuity, and later `this+0xf8` / `this+0xe0` continuity into `0x299c70` / `0x267010`. A follow-up validator proves the full `0x299eb0` returned byte span and sampled `0x299fd0` offset/record headers from live input and `this+0x208` mask data: `rounded = ceil(input_u16_2 / 8) * 8`, factor `2` for nonzero mask bytes and `3` for zero mask bytes, `record_size = 8 + factor * rounded`, and record header `(input_u16_0, input_u16_2, 1, rounded)`. The specific byte-span and mask-census values recorded in the evidence doc are runtime samples from that admitted run, not stable constants. Public origin, physical meaning, stable record constants, exact `0x28f490` helper semantics beyond the observed header-population boundary, and final merge effect remain open.
- Follow-up four-zoom watchpoint proof now bounds sampled source-record payload mutation custody after `0x299fd0`: hardware write-watchpoints armed on zeroed `record+0x08` payload bytes for the first two source-local records stop at `libcp+0x277a16`, and static disassembly plus register disambiguation attribute those sampled writes to the preceding `libcp+0x277a10` SIMD store `movdqu xmmword ptr [r9 + 2*rdx], xmm5`. The `%xmm5` arithmetic, full-map payload distributions, public origin/meaning, and final merge effect remain open.
- Follow-up four-zoom vector proof validates the sampled `0x2779b0..0x277a10` unsigned-16 SIMD recurrence feeding that payload writer: live vectors from `[rsi + 2*rax]`, `[rdi + 2*rdx]`, `[r10 + 2*rdx]`, `%xmm1`, `%xmm2`, and `%xmm3` reconstruct `%xmm0`, the side-store to `[rcx + 2*rdx]`, and watched-lane saturating accumulation into `%xmm5` / `[r9 + 2*rdx]`. A stricter rerun also validates narrow custody for stable inputs/destinations: `object+0x108/+0x138/+0x130` match the source-local record base / offset table / stride after `0x299fd0`, `r9` is `object+0x108 + sampled_record_offset + 8`, `r10` is the `rbp-0x2e0` temporary pointer, and `%xmm1` is the unsigned-16 broadcast of object `P1` at `+0x56`. A follow-up early-terminate packet proof pairs `0x27786b`, `0x27791d`, and `0x277945` on the same target-index-5 context across all four focal tiers, proving sampled `%xmm2` is prepared from a `uint16` lookup through `rbp-0x210`, sampled `%xmm3` is prepared from live post-add `edx`, and both broadcast-ready registers match those paired scalar values. A further non-degenerate step proof validates sampled `%xmm3` pre-add/post-add scalar math from captured guide weight, `P1`, the guide-adaptive `P2/P1` scale, and the paired minimum-cost table value on one stepped packet per focal tier. A `%xmm4` origin proof then reconstructs the sampled guide weight from the preceding `0x27786f..0x277903` local arithmetic over two Pixel buf vectors, the guide-distance decay coefficients, absolute-value/horizontal-sum/sign/clamp operations, and polynomial/exponent-bit assembly. Later allocator/helper/worker proof closes the sampled recurrence roles; full-map payload distribution, all records/lane positions, and final merge effect remain open.
- Installed-bundle proof now also shows the row producer is a structured 4x4 double matrix chain: `0x25ec70` is matrix multiply, and `0x25e0c0` writes output row fields from the final chain result.

### 5. `FusionCacheBayer` is not the bridge HDR merge entry point

- `CLM-MERGE-001`
- This is a negative fact but still important because it prevents implementation from targeting the wrong subsystem.

### 6. Camera participation follows two archive-validated firing regimes

- `CLM-FIRING-001`
- Wide tiers (`28mm`, `35mm`) are dominated by `5A+5B`.
- Tele tiers (`70mm`, `150mm`) are dominated by `5B+6C`.
- No zoom tier's dominant firing set is `C-only`.

### 7. Focal framing is tiered, not globally 28mm-based

- `CLM-ZOOM-003`
- `28mm` and `35mm` live on a `28mm` reference tier.
- `70mm` and `150mm` live on a `70mm` reference tier.
- Centered internal crop happens before final output rasterization.

### 8. 35mm bridge behavior is not "computational synthesis"

- `CLM-ZOOM-001`
- The tested bridge behavior is crop-plus-upsample, not the stale synthesis story.

## Historical Merge-Critical Narrowing

This section preserves the narrowing sequence that led to closure. It is not
the current claim-status table. Any "open", "partial", or anonymous-wrapper
wording below is superseded by the `PROVEN` / `SPEC_READY` ledger rows and the
current proven section above.

### 1. `src1` / `src2` are tiered generated anchor-side inputs

- `CLM-PREFUSION-001`
- They behave like wrappers over a shared upstream callable.
- Installed-bundle proof now shows the first visible wrapper bodies at `0x3ecc10` and `0x3ecd80` are exclusion anchors, not reducer closure, and already resolve to concrete `PipelineCache` backing fields.
- LLDB runtime proof now shows the canonical four-zoom bridge HDR quartet all hit the visible `src1`, `src2`, and contributor wrapper bodies at `0x3ecc10`, `0x3ecd80`, and `0x3eced0`.
- LLDB runtime proof now identifies `PipelineCache+0x8` as a five-entry packed `(int32 width, int32 height)` level-vector header, not an image/composite pointer. `28mm` / `35mm` entry `0` is `10432x7824`; `70mm` / `150mm` entry `0` is `8896x6672`; all four runs use entry `1 = 4160x3120` for the visible `src1` / `src2` wrapper dimension fields.
- Installed-bundle proof now also bounds the post-wrapper `initResAmp` branch at `0x3eb3c0` to per-key map/vector/record/wrapper construction at `PipelineCache+0x258` and `PipelineCache+0x270`, not exposed merge/reduction math.
- Installed-bundle proof now also bounds the visible `PipelineCache+0x270` per-key wrapper read path to single-payload ROI/tile processing plus square-root normalization, not exposed merge/reduction math.
- Installed-bundle proof now also bounds the visible `src1` read path (`0x3ecc10`, `0x3e0af0`, `0x3d01b0`, `0x3edb80`) to map/tree lookup, checked single-source level/ROI tile read, and one-image square-root normalization.
- Installed-bundle proof now also bounds visible `src1` payload provenance: `PipelineCache+0x170/+0x178` is copied from the caller's `+0x6a8/+0x6b0` shared-ptr-like pair, `0x3dfcc0` builds the first map/tree, and `0x3e2db0 -> 0x3e27a0` constructs the `0x490`-byte payload returned by `0x3e0af0`.
- LLDB runtime proof now confirms the visible `src1` payload constructor path across the corrected canonical quartet: `0x3dfcc0 -> 0x3e2db0 -> 0x3e27a0` is live, the constructor key is `0` at `28mm` / `35mm` and `8` at `70mm` / `150mm`, the level vector is `(4160,3120)`, `(2080,1560)`, `(1040,780)`, `(520,390)` in all four runs, and the success packet produces the same `0x490` payload family with vtable `0x65f140`, secondary address point `0x65f388`, and payload self-pointer at `+0x68`.
- LLDB runtime proof now bounds the visible `src1` lookup key across the corrected canonical quartet: key `0` at `28mm` / `35mm`, and key `8` at `70mm` / `150mm`. This is a lookup-key fact, not payload-composition closure.
- LLDB runtime proof now also bounds the visible `src1` payload family across the corrected canonical quartet: the visible `src1` lookup returns a `0x490` payload family with vtable address point `0x65f140`, while adjacent direct contributor lookups return `0x1f0` payloads with vtable address point `0x65f490`. This proves a payload-family split, not payload-composition closure.
- SHA-pinned RTTI/control-block proof now exactly names that split: owner `+0x6a8/+0x6b0` is `shared_ptr<lt::ImageCaches>`, visible `src1` payload vtable `0x65f140` is `lt::ReferenceImageCache`, direct contributor vtable `0x65f490` is `lt::SourceImageCache`, and visible wrappers are `PipelineCache::initResAmp::$_1/$_2`. The four-focal keys make visible `src1` A1 wide and B4 tele, with B1..B5 or C1..C5 direct source caches. This supersedes older "composite-ish src1" shorthand; the next item names visible `src2`, while complete reduction policy remains open.
- SHA-pinned constructor/accessor proof now also shows that `ReferenceImageCache` carries exactly one public `CapturedImage::Camera` key through one `RawImageFactory`: A1/key `0` at canonical `28mm` / `35mm`, B4/key `8` at canonical `70mm` / `150mm`. Visible `src1` construction/access is therefore not an N-to-1 reducer; the remaining distributed policy is outside it.
- SHA-pinned outer IRAMP custody resolves the operand roles: `src1` supplies the coarse byte registration guide and warp-grid bounds, `src2` supplies the full `vec4` reference/baseline patch, and each paired direct source/warp supplies a candidate patch. `0x36cde0` and `0x36e530` have complete clean-room formulas. Exhaustive installed-body census closes local candidate/sentinel policy and proves surviving scores are continuous with no threshold. A return-only `t=0` intervention has final Radiance-file effect at representative canonical `35mm` and `70mm`, joined to complete four-focal score-use and descriptor-to-writer custody; no later per-contributor predicate remains on the tested profile-3 CLI route. Installed selector proof formally excludes reachable profiles-1/2 MonoFusion mode `1` from canonical profile `3`.
- SHA-pinned RTTI plus the admitted four-focal executor packets exactly name visible `src2`: `PipelineCache::initResAmp::$_2 -> PipelineCache::processLevel1 -> ImageWarpClamped<ResamplerFilter=2, vec4x32f>`, callback `0x65f7e8/+0x30 = 0x3ed2e0`. The adjacent direct-contributor wrapper is distinct `initResAmp::$_3` at `0x65f768/+0x30 = 0x3eced0`. This is exact level-1 one-generated-descriptor resampling/materialization identity.
- Pinned installed-class proof plus complete Unit-1 four-focal and Unit-2 28mm/70mm packets identify that generated descriptor's tested ancestry. Wide constructs A1-target/A2-source `lt::MonoFusion`; tele constructs no MonoFusion and uses direct B4. Follow-up SHA-pinned static/LRI and Unit-1 28mm/35mm plus Unit-2 28mm runtime decodes production-profile mode `0`: public A1/A2 exposure/analog source normalization, installed type-3 panchromatic VST rows, initializer scaling, normalized 5/3 lifting with exhaustive boundary/packing schedules, exact coefficient weights, patch-noise/Wiener math, half-Hann overlap-add, final target/source scalar blend, and the secondary-map callback formula. Corrective constructor proof refutes the different public type-2 LRI VST rows as coefficient custody. Immediate `0x1b3530` post-worker math is also decoded. A four-profile selector matrix proves mode `1` is real for profiles `1` / `2` but outside canonical profile `3`; its separate scalar formula is now closed under scoped `CLM-COMPAT-001` proof, and the canonical distributed policy remains closed.
- Bundle plus LLDB proof now also bounds the payload-internal secondary callable split at `+0x60`: visible `src1` uses address point `0x65f388` with substantive slot `0x3e4a80`, while direct contributors use address point `0x65f4d8` with substantive slot `0x3e78d0`. This proves a secondary-callable split, not payload-composition closure.
- LLDB runtime proof now confirms the visible `src1` secondary callable body at `0x3e4a80` is live across the corrected canonical quartet; the first captured call-site packet at `0x3e4b09` passed the same `0x490` payload to `0x3e2e90` with `level = 0` in all four runs. This proves live participation and handoff for the first captured tile only, not payload-composition closure.
- LLDB runtime proof now also ties the first captured worker/projection-record path beneath that handoff to visible `src1` across the corrected canonical quartet: worker and projection packets share the backtrace through `0x3e4b0e <- 0x3d4842`, the worker callback fields are source image / output image / default vector / projection record / weight table, and the first captured projection record loads `payload+0x150` from visible-payload field `+0x170` with callable address point `0x65f188` and slot `+0x30 = 0x3e42e0`. This proves runtime topology for the first captured worker packet, not payload-composition closure; the projection-record index `0` is payload-internal and is not a physical camera id.
- Installed-bundle proof now decodes `0x3e42e0` as a coordinate-transform body: it reads transform state through callable `+0x8`, computes three row equations over input `(x, y)`, divides by the third row, subtracts center fields, applies a radius-indexed scale table, and writes two output floats. This is projection math, not reducer closure; public field names and LRI calibration origins remain unproven.
- Installed-bundle proof now also bounds the producer for the fields consumed by `0x3e42e0`: constructor `0x3e27a0` calls dispatcher `0x3f6170`, same-category work routes through `0x3f6200`, cross-category work routes through `0x3f6940`, both converge on `0x145580` / `0x144f50`, and `0x144a70` forces the radius table to `4096` floats. This proves field-pack topology, not `src1` semantic contents or reducer closure.
- LLDB runtime proof now also bounds that projection field-pack dispatcher boundary across complete canonical bridge HDR runs with `.lris` auto-loading disabled: `0x3f6170`, `0x3f61b8`, `0x3f61ca -> 0x3f6200`, and `0x3f61e1 -> 0x3f6940` are live; observed keys are `0,5..9` at `28mm` / `35mm` and `8,10..14` at `70mm` / `150mm`; tele key `15` is not observed at this boundary. This excludes the tested dispatcher boundary as a positive C6 route, not C6 globally.
- LLDB runtime proof now bounds the direct `0xe59a4 -> 0xf2770` constructor callsite across complete four-zoom bridge HDR runs: wide seeds construct keys `0,4,6,8,9,1,2,3,5,7`; tele seeds construct keys `6,8,9,14,5,7,11,10,12,13,15`; all captured items are initially active at item `+0x30 = 1`; input `+0x30` equals output item `+0x60`; and input `+0x28/+0x18` carries the same two-int pair later observed at item `+0x58/+0x5c`. Lane B public-meaning audit and embedded-schema proof bind the captured constructor fields to public `CameraModule` fields, including exact `input+0x28 -> item+0x58/+0x5c = sensor_bayer_red_override.{x,y}` over all `42` events; exact-focal Unit-2 carriers preserve the same wide/tele pattern. This proves tele key `15` / C6 is born active with public override `(-1,-1)` in the tested constructor path, while selector purpose and final contribution remain open.
- LLDB hardware write-watchpoint proof then captures tele key `15` / C6 changing from item `+0x30 = 1` to `0` at writer `libcp+0x3c90a5` inside body `0x3c8f90` in both canonical tele seeds. Static inspection shows the local gate clears item `+0x30` for key `15` when the grouped context `+0x44` value is not group ordinal `2`.
- Follow-up LLDB identity proof ties the active `0x1bdbab` / `0x1bdbdd` key-list helper observations, active mutation-body observations, the `0x3c90a5` store, immediate inactive state at `0x3c90a9`, and later inactive context-walk read at `0x3b2143` to the same tracked key-15 item pointer in both canonical tele seeds. Static inspection bounds helper `0x1bdb60` itself to key-list construction bookkeeping, not image-buffer work. A post-mutation state-consumer probe then proves the immediate caller path consumes the constructed `ctx+0xa0` object, walks eleven entries, observes key `15` inactive at `0x3b2143`, writes derived state fields `+0x0 = 3` and `+0x4 = 1` to context `+0xc8`, and queues context `+0x4b0 = 5`. A downstream rect-vector probe proves that state is reread, fallback `0x3c8c00` is reached, `context+0x4b0 = 5` is passed into `0x3c8d00`, and a five-entry vector of 16-byte integer tuples is returned in both canonical tele seeds. A four-zoom consumer probe then proves that the rect-vector route feeds five `context+0x4c0` delta-dimension pairs into `0x3982b0` ImagePyramid construction, with the resulting ImagePyramid stored at `context+0x538`. A four-zoom follow-up proves the caller immediately iterates those five ImagePyramid levels, builds full-image descriptors from each level image, and invokes direct zero-fill callsite `0x3b2f54 -> 0xf7c0` once per level with bytes-per-pixel argument `4`; runtime descriptor values satisfy the static contiguous zero-fill condition for all twenty level descriptors, and after-return first-32-byte samples are zero for all twenty descriptors. A four-zoom downstream-candidate probe re-hits the zero-fill checkpoints and records zero hits at selected later static `context+0x538` candidate families. A representative four-zoom hardware data-watch probe records zero read/write watchpoint hits on the first 8 bytes of selected zero-filled level buffers before clean render completion. An expanded tele hardware data-watch grid records zero read/write watchpoint hits on first/middle/last 8-byte ranges across all five zero-filled ImagePyramid levels at `70mm` and `150mm` before clean render completion. A focused candidate-consumer probe also excludes `0x3c9540 -> 0xe6c30` as a live downstream constructed-context route under complete canonical `70mm` and `150mm` bridge HDR renders; final image/merge effect of the zero-filled ImagePyramid/geometry route, later writer/read effects outside watched representative/tele-grid byte ranges or through unprobed helpers/aliases, other unprobed downstream consumers, alternate routes, and terminality remain unproven.
- Post-mutation same-byte watchpoint proof now shows the tracked key-15 `item+0x30` byte remains `0` at all 18 recorded later read/write stops per complete canonical tele render, including active-byte gates outside the direct `0xf2720` callsite census. This narrows same-byte reactivation; it does not close other C6 fields, aliases, image-buffer custody, all alternate routes, or terminality.
- Post-mutation selected-field watchpoint proof now bounds watched ranges of the same tracked key-15 item at `item+0x58..0x5f`, `item+0x60..0x67`, and `item+0x100..0x107` in complete canonical tele renders. Pre-output libcp samples preserve active byte `0`, pair `(-1,-1)`, key `15`, and type `3`; the watched `+0x60..+0x67` range is read at `0xf2727` (`+0x60`) and `0xf3327` (`+0x64`); and watched pair/type ranges record only allocator-cleanup stops after output write. This narrows selected fields; it does not close untested fields, aliases, final effect of the watched `+0x60..+0x67` reads, all alternate routes, or terminality.
- LLDB runtime proof now bounds the direct payload candidate loop immediately upstream of `0x3e05f5 -> 0x3f6170`: under complete bridge HDR runs with `.lris` auto-loading disabled, the loop visits keys `0..9` at `28mm` / `35mm`, all with `object+0x30 = 1`; it visits keys `5..15` at `70mm` / `150mm`; tele key `15` / C6 has post-mutation `object+0x30 = 0` and skips before active-pass, class-compare, cross-category, and dispatcher-call sites. This proves one tested C6 filter point, not global C6 non-use or alternate-route closure.
- LLDB runtime proof now also bounds the stereo-side keyed-record loop inside the `0x3f2c40` constructor branch: under complete bridge HDR runs with `.lris` auto-loading disabled, the loop visits keys `0..9` at `28mm` / `35mm`, all with `object+0x30 = 1`; it visits keys `5..15` at `70mm` / `150mm`; tele key `15` / C6 has post-mutation `object+0x30 = 0` and skips before the post-gate path and before both tested `0xf2720` getter callsites. This proves a second tested C6 filter point, not global C6 non-use or alternate-route closure.
- Installed-bundle proof now also bounds the source-image producer topology beneath the visible `src1` `0x3e2e90` worker handoff: keyed helpers `0x1bdc80` / `0x1be750`, vector builder/updater `0x1be270`, ROI/source validator `0x31abd0`, wrappers `0x31af30` / `0x31acf0`, lower producers `0x33ede0` / `0x33f480`, and shared per-source iterator `0x33f180`. This proves source-producer topology and per-source virtual dispatch, not semantic `src1` contents or reducer closure.
- LLDB runtime proof now bounds that keyed-helper/vector-builder boundary under complete bridge HDR runs with `.lris` auto-loading disabled: `0x1bdc80` and count site `0x1bdcfb` are live across `28mm`, `35mm`, `70mm`, and `150mm`; every summarized invocation saw count `1`; `0x1be750`, helper lazy callsites into `0x1be270`, and direct builder sites `0x1be270` / `0x1be291` / `0x1be2fb` / `0x1be306` had zero hits under the same quartet. Observed tele helper keys were `5..14`, with no key `15`.
- Gated LLDB runtime proof now bounds the first captured per-source virtual target beneath that visible `src1` source-producer topology across the corrected canonical quartet: the first captured descendant path is `0x3e3279 -> 0x31af30 -> 0x33ede0 -> 0x33f180 -> 0x33f3e8`, with vtable address point `0x65b3c8` and slot `+0x30 = 0x341770`. Static inspection bounds `0x341770` to per-source region-adapter / record-update work, not reducer closure.
- Follow-up gated LLDB runtime census broadens that lower producer view: after the first visible-`src1` `0x3e4b09` gate, complete bridge HDR runs observed `0x3e3279 -> 0x31af30` at all four zooms, observed `0x33f3e8` and `0x33f94f` at all four zooms, and observed `0x33ffd4` at `28mm` / `35mm` with zero `0x33ffd4` hits at `70mm` / `150mm` under the same tested gated runs. Nonzero virtual-site counts hit a `512` cap, so they are lower bounds and target-family lists are capped-window observations, not exhaustive full-render totals.
- Installed-bundle static classification now bounds the inspected visible bodies from that capped lower target-family set to thunk / descriptor / region / materialization / cache / executor surfaces. Follow-up runtime/static proof binds the two prior indirect target gaps under the first visible-`src1` gate: `0x342d99` resolves `0x65b948/+0x30 = 0x342b80 -> 0x2eb560`, and `0x3449f0` resolves `0x65c798/+0x30 = 0x345920 -> 0x2f53d0` across the canonical quartet. A further gated runtime/static proof bounds the immediate `0x2f53d0` helper chain to validation, descriptor/vector setup, bilateral-kernel-size dispatch, callback-object dispatch through `0x5440`, and one row-executor dispatch through `0x5670`; `0x3048b0` has zero hits under the accepted gated probes. Follow-up static classification bounds the executor callback bodies under that chain to local descriptor transform / filtering / interpolation / normalization / accumulation surfaces. Follow-up runtime proof bounds the selected `0x2fb320` worker under that route as readable same-shaped descriptor fields, a `vec4` coefficient pointer, and approximate reciprocal-normalized weighted `vec4` stores. This does not expose reducer closure, final contributor acceptance/rejection, or public helper-field semantics.
- Installed-bundle proof now bounds the helper called by `0x341770`: `0x2e8680` validates one source-domain / Bayer-image descriptor, prepares one destination descriptor through `0xf540`, installs callback vtable `0x659fc0`, and dispatches substantive callback slot `0x2e8cc0` through `0x5440`. Static inspection bounds `0x2e8cc0` to one-source 16-bit SIMD region/pixel work, not reducer, final blend, C6 routing, or acceptance closure.
- LLDB runtime proof bounds the visible `src2` hot path across the corrected canonical quartet to a tiered `PipelineCache+0x1e0` resample-state object and one-image normalization. Outer worker `0x3ed2e0` is one-generated-descriptor resampling/materialization; the descriptor producer is tier-dependent exact `lt::MonoFusion` for A1/A2 wide versus direct B4 tele. The earlier constructor flag is a same-group negative-override MonoFusion eligibility gate. This closes wrapper/state/descriptor ancestry and most production mode-0 payload math, not its scoped residuals, distributed reduction, or final acceptance.
- Installed-bundle proof now also bounds the first visible payload runtime surfaces beneath that constructor: `0x3e53a0` / `0x3e54c0` are destructors, `0x3d0120` / `0x3e55f0` are callable-slot helpers, `0x3e2dc0` / `0x40b370` / `0x40b330` are setup/config work, and `0x3e2e90` / `0x3e3f90` are single-level ROI/process helpers rather than exposed merge/reduction math.
- Follow-up installed-bundle proof bounds the deeper `0x3e2e90` callback worker too: address point `0x65f408` has slot `+0x30 = 0x3e4c50`, and `0x3e4c50` is a row/range worker that projects each output sample through one callable, samples one source image/cache buffer through a 4x4 SIMD neighborhood and 64-entry cubic table, then writes one `vec4f` output per sample.
- Follow-up installed-bundle proof also bounds the owner cache-selection layer: the owner builds `+0x6a8/+0x6b0`, constructs `+0x688/+0x690` through `0x3eaf00 -> 0x3ea7d0` using `&owner+0x6a8` and `&owner+0x678`, selects `owner+0x688` or `owner+0x6b8` at `0x3b0740`, and dispatches the selected cache through `0x3d0650` one-cache level/ROI read and rescale work rather than exposed merge/reduction math.
- Follow-up installed-bundle plus four-zoom runtime proof bounds the constructor-side owner `+0xf0` callable-install custody edge: `0x3ea980` is a direct `0x3d0120` caller, the installed stack callable address point is `0x65f5e0`, `0x65f5e0/+0x30 = 0x3ec960`, and complete canonical bridge HDR runs prove that address point installed into target inline callable storage before the already-bounded owner `+0xf0` sink body is reached. This is custody proof, not semantic `src1` / `src2` closure, final output semantics, or acceptance/rejection proof.
- Follow-up installed-bundle proof also bounds the owner alternate-cache/setup surface: optional `+0x698/+0x6a0` is constructed through `0x3d8b70 -> 0x3d8780` from `+0x6a8` and `+0x678`, `+0x6b8/+0x6c0` is constructed through `0x3f06f0 -> 0x3f04d0` from `+0x688`, `+0x698`, and a `0x18`-byte state block, and the inspected bodies are setup/callback/cache-construction surfaces rather than exposed merge/reduction math.
- Follow-up installed-bundle proof also bounds the visible `+0x678/+0x680` constructor and immediate runtime surfaces: `0x3f2c40` completes through keyed-record construction, six descriptor/layer seeds, `this+0x280` helper construction, and bitset storage; `0x3f75e0`, `0x3f7a40`, `0x3f7b20`, `0x3f7c00`, and `0x3f7ec0` are stop/cleanup, level-gate, callable-slot, byte-count, and record/buffer-materialization surfaces rather than closure of the exact pre-fusion merge/reduction mechanism.
- Follow-up installed-bundle proof also bounds the selected `+0x40` and `+0x90` layer virtual targets reached from that branch to direct setters/accessors, bounds `0x3f8b30` as a consumer/writer of `0x3f7ec0` materialized record/buffer output, and previously located the `StereoLayer<false>::runPass(int)` action path at `0x276790 -> 0x276860 / 0x277e70`.
- Follow-up installed-bundle plus LLDB runtime proof now bounds that `StereoLayer<false>::runPass(int)` action path further: `0x276790` dispatches on `layer+0xc`; both worker bodies route through `0x275630` into `0x2730c0` / `0x2732f0`; `0x275630` is a per-tile state builder; `0x2732f0` is a projection/sampling cost body; and the canonical `28mm`, `35mm`, `70mm`, and `150mm` bridge HDR quartet all reached `0x276790 -> 0x276860 -> 0x275630 -> 0x2732f0` with sampled `layer+0xc == 8`. The scoped zero-hit result for `0x277e70` and `0x2730c0` applies only to those tested full renders.
- Follow-up installed-bundle plus no-auto-LRIS LLDB runtime proof bounds the sibling `StereoLayer<false>::compute()` cost surface: installed table `0x667c28` uses operator wrapper `0x274b10`, which tail-jumps to worker `0x2727f0`; static inspection shows `0x2727f0` shares the `0x275630` / `0x2730c0` / `0x2732f0` projection-cost family. Complete bridge HDR probes at `28mm`, `35mm`, `70mm`, and `150mm` recorded zero hits for `0x274b10`, `0x2727f0`, adjacent setup helpers `0x272100` / `0x272640`, and their probed callsites, while the same runs hit live `runPass(int)` controls. This is scoped exclusion, not proof of dead code and not reducer closure.
- Follow-up installed-bundle plus no-auto-LRIS LLDB runtime proof now classifies high-address helper callers `0x42cb5d -> 0x3f6170`, `0x42cbc2 -> 0x3f7040`, and `0x42cc5a -> 0x3e55f0` as part of a `HigherWarpDebug::renderDebugView` local callback surface. Complete bridge HDR probes at `28mm`, `35mm`, `70mm`, and `150mm` recorded zero hits for the debug-view entry/callsite/callback sites, while live helper controls at `0x3e05f5` and `0x3eb72d` hit five times per tier. This is scoped exclusion, not proof of global dead code, not a debug-only classification for the shared helpers, and not reducer closure.
- Follow-up installed-bundle plus LLDB runtime proof also bounds the sampled prefusion callable gate: `state+0x220` is an inline `state+0x200` callable; constructor vtable `0x6673f0` slot `+0x30` is false-return `0x230220`; and four-zoom bridge HDR runtime samples at the gate sites used vtable `0x66b0f0`, false-return slot `0x230640`.
- Installed-bundle proof also now exposes a concrete upstream `CalibDataProcessor::State()` `operator()` family above that wrapper layer. Corrected vtable proof identifies the thirteen State bodies as `0x229df0`, `0x229ec0`, `0x22a0e0`, `0x22a9b0`, `0x22aaf0`, `0x22ae60`, `0x22af80`, `0x22bdf0`, `0x22bee0`, `0x22c350`, `0x22cd00`, `0x22d250`, and `0x22e1d0`; follow-up four-zoom runtime census proves all thirteen are live under complete accepted bridge HDR renders with count pattern `(1,1,4,4,4,1,1,1,5,5,5,5,1)`. `0x247390` is refuted as a State body and belongs to an adjacent `SparseLNR::markInliers(..., void(int,int,int))` callback table. This is entry-liveness proof, not public State semantics or reducer closure.
- Installed-bundle static proof now bounds the terminal corrected State body `0x22e1d0` and shared dispatcher `0x22f0f0`: `0x22e1d0` is keyed vector/tree/object-lookup/helper dispatch over per-key records, calls `0x23c5f0`, `0xe6ba0`, and `0xf33d0`, and returns State value `9`; `0x22f0f0` invokes registered State function objects through slot `+0x30`, stores returned `eax` into the current State slot at `r14+0x6c`, and can notify a callback at `r14+0xe0`. This bounds the terminal State-machine surface away from direct pixel reducer closure; public State semantics and the exact `src1` / `src2` reducer remain open.
- Follow-up no-auto-LRIS runtime proof now bounds the ordered State-return skeleton for that dispatcher path across the canonical quartet: each run records `38` paired `0x22f3f6` / `0x22f3ff` calls, exits cleanly with `10432x7824` HDR output, and has the same ordered `(operator, pre-state, returned State)` sequence. This proves return ordering under the tested path; public State meanings, semantic `src1` / `src2` contents, reducer closure, and final acceptance/rejection remain open.
- Follow-up installed-bundle static proof now isolates each exact State operator body. The thirteen State operator bodies contain zero indirect calls; dispatcher `0x22f0f0` contains the expected indirect dispatch calls. The exact State bodies have zero direct calls to the listed known IRAMP/wrapper/owner-route VAs. This is direct-call-surface proof only; helper transitive behavior and final image/merge effect remain open.
- Follow-up installed-bundle and no-auto-LRIS runtime proof now bounds helper `0x23c5f0` plus selector-gated `0xf33d0`: static extraction classifies `0xf33d0` as selector `0` / selector `1` field-copy paths into two destination offset banks, and runtime proof shows `0x23c5f0` hits four times per render from State bodies `0x22af80` and `0x22e1d0` across the canonical quartet. The static `0x23c5f0 -> 0xf33d0` callsite at `0x23d38d` / return `0x23d392` is live with selector `1` at all four focal tiers. Follow-up census/watch proof maps `0=factory` and `1=current`. Public State meanings, complete bank-field meanings, helper transitive behavior, final image/merge effect, reducer closure, and final acceptance/rejection remain open.
- Follow-up no-auto-LRIS four-zoom runtime proof now bounds `0x23c5f0` post-`0xf33d0` local integer coverage and normal-exit local tree shape. Every tested run pairs four `0x23c5f0` entries with four normal pre-destroy exits, with no JSON errors, step cap, or tree traversal truncation. The post-`0xf33d0` local `rbp-0x4e0` integer coverage and pre-destroy node `i32_0x20` sets split wide versus tele as documented in [lldb_state_helper_23c5f0_exit_snapshot_four_zoom.md](/Users/ryaker/Dev/L16_Lumen_ReverseEngineering/docs/evidence/lldb_state_helper_23c5f0_exit_snapshot_four_zoom.md). This is local helper-field / pre-destroy tree custody proof only; public field semantics, image effect, reducer closure, and final acceptance/rejection remain open.
- Follow-up four-zoom runtime proof now shows objects populated by the live `0x23c5f0 -> 0xf33d0` selector-`1` copy path are reused by `0x23c5f0 -> 0x264440 -> 0x264270 -> 0xf34e0` before `0x23c5f0` exits. Each canonical no-auto-LRIS run matches nine prior destination objects and `204` selector-`1` `0xf34e0` calls. This is internal transitive helper-custody proof only; post-`0x23c5f0` downstream image effect, source contribution, reducer closure, and final acceptance/rejection remain open.
- Follow-up four-zoom runtime proof now bounds the next `0x23c5f0` record-chain step after the `0xf34e0` custody edge. Each canonical no-auto-LRIS run records `26` ordered four-site groups through `0x23cbab`, `0x23cbc1`, `0x23ce5e`, and `0x23d025`; the output record at `rbp-0x378` changes across `0x23cbbc -> 0x23faf0`, remains stable afterward, and its mapped fields are materialized into local tree-node fields in all `104` admitted groups. Node `+0xa0` is first forced to `0`, then receives observed final `{0,9,11}` values. This is helper-record-to-local-tree custody proof only; public field semantics, post-node image effect, source contribution, reducer closure, and final acceptance/rejection remain open.
- The representative inspected `State()` bodies in that family are state-materializers over lookup / pair-list / bitset helpers, not direct reducer closure.
- Installed-bundle proof now also bounds the first post-`State()` helper chain (`0x224cc0`, `0x224d70`, `0x242a80`, `0x258ea0`, `0x258f00`, `0x242d00`, `0x242dc0`, `0x243770`, `0x243870`) to setup/copy/reset work, and pushes the next deeper candidate boundary to the first heavier downstream consumers `0x244560` and `0x245a40`.
- Installed-bundle proof now further shows those two heavy consumers are still feature / pyramid / candidate state surfaces with explicit pyramid and feature-size guards, not exposed direct multi-camera pixel-blend loops.
- Installed-bundle proof now further bounds the next deeper dispatcher / selector layer too: `0x248580`, `0x248960`, and `0x2481a0` all feed a shared gate at `0x2439b0`, and `0x2439b0` plus `0x241fd0` / `0x2416d0` still only count and relabel 0x2c-stride records while gating installed blocks.
- Installed-bundle proof now further bounds the next selector-helper tranche too: `0x249020` is only selection-index permutation, `0x247900` is only 32-bit vector copy / growth, `0x249410` only materializes repeated 24-byte bitset entries, and the visible continuation of `0x2416d0` after those helpers is still bitset-driven record promotion rather than exposed reducer math.
- Follow-up bundle proof now also shows the callback object that `0x2416d0` installs before executor dispatch is not a fresh State-family worker: it uses adjacent `SparseLNR::markInliers` vtable address point `0x6589e0`, whose substantive `+0x30` slot is `0x247390`, bounded as coordinate/bitset-style state work rather than exposed pixel reduction.
- Installed-bundle proof now also bounds the candidate block-geometry helper family (`0x25c990`, `0x25ca70`, `0x25d090`, `0x25d2a0`, `0x25d4d0`) to coordinate delta/scale, geometry predicate, active-block coordinate-pair propagation/validation, descriptor build, and int-pair vector copy work.
- Follow-up four-zoom LLDB runtime proof now bounds the admitted `0x25d090` effect as block-owned pair-vector growth plus descriptor-build / geometry-predicate / active-byte gating under complete canonical no-auto-LRIS bridge HDR renders. Accepted active entries grow both block pair-vector families and return true; the only admitted active-byte clears are two `70mm` geometry rejects. This is block-state effect proof only, not final image contribution, reducer closure, or final acceptance/rejection.
- Follow-up four-zoom LLDB runtime proof now also bounds the `0x244560` / `0x245a40` caller-side block-decision cascade after paired `0x25d090` calls: every admitted decision join continues with exactly one active block, avoids the watched sentinel-fill path, and reaches `0x2457c0` callsites. This is downstream coordinate-output custody proof only, not final image contribution, reducer closure, or final acceptance/rejection.
- Installed-bundle proof now also bounds the visible feature-selection lane (`0x258fe0`, `0x2598a0`, `0x259b40`, `0x25a010`, `0x25a360`, `0x25ab50`) to feature / pyramid / candidate-record generation, candidate suppression, and scaled coordinate-output support.
- Installed-bundle plus LLDB runtime proof now also bounds the downstream candidate-scoring family: under the corrected canonical four-zoom bridge HDR quartet, `28mm` and true-`35mm` hit the `0x24c320` entry and not `0x24d610`, while `70mm` and `150mm` hit the `0x24d610` entry and not `0x24c320`. Static proof shows both entry bodies consume `0x24`-stride candidate records, perform bounds / projection / local SIMD patch scoring, and write `0x2c`-stride candidate-result or sentinel records; helpers `0x24cf90`, `0x24e070`, and `0x24e350` are local patch/search scorers.
- Follow-up LLDB runtime proof now binds those candidate-scorer output records to `0x2439b0`: family A is installed through `0x667788/+0x30 -> 0x24c2d0 -> 0x24c320` and passes scorer-context `+0x18` as the output vector; family B is installed through `0x667808/+0x30 -> 0x24d5c0 -> 0x24d610` and passes scorer-context `+0x28` as the output vector. Complete canonical `28mm`, `35mm`, `70mm`, and `150mm` bridge HDR runtime proof shows the family gate call and matched `0x2439b0` entry carry the same output-vector pointer and record count. This is candidate-output custody, not semantic reducer closure or final acceptance/rejection.
- Follow-up LLDB runtime proof now also bounds `0x2439b0` as a live record-state gate for those custody-bound output vectors: wide family-A runs (`28mm`, `35mm`) are unchanged at the before/after histogram boundary, while tele family-B runs (`70mm`, `150mm`) promote target-2 records from state `3` to state `4` at that boundary. Static proof ties the conditional state stores to `0x243b2c` for target `1` and `0x243cac` for target `2`; the observed tele mutation matches the target-2 path. Sampled downstream `0x241fd0` / `0x2416d0` entries and watched store sites did not match the exact scorer-output vector under this probe. This is boundary behavior, not public record-state semantics, reducer closure, or final acceptance/rejection.
- Follow-up tele hardware data-watch proof now shows selected target-2 records promoted by `0x2439b0` from state `3` to state `4` are later consumed by downstream code under clean canonical `70mm` and `150mm` renders, and at least one watched record per tele seed advances to state `5` through `0x2416d0`. This proves a downstream consumer path for watched promoted records only; it does not prove all promoted records, public state names, final image contribution, reducer closure, or final acceptance/rejection.
- Follow-up tele selected-index proof now shows promoted target-2 record indices captured at `0x2439b0` later enter concrete `0x2416d0` selected-index vectors under clean canonical `70mm` and `150mm` renders, and the small promoted sets captured by that probe reach state `5,target=2` stores. This proves selected-index/state-relabel flow for admitted promoted sets only; it does not prove public acceptance semantics, final image contribution, reducer closure, or final acceptance/rejection.
- Follow-up tele later-watch proof now shows watched promoted records that become `state 5,target 2` continue downstream into `0x244560` and the already-bounded `0x25d090` candidate block-geometry / active-block helper family under clean canonical `70mm` and `150mm` renders. This proves later state/candidate/geometry flow only; it does not prove final image contribution, reducer closure, or final acceptance/rejection.
- Follow-up four-zoom state-5 coordinate-output proof now shows `0x2457c0` is live and normally returning under clean canonical `28mm`, `35mm`, `70mm`, and `150mm` bridge HDR renders; sampled hits at the admitted `0x24593b` store-path site have `record+0x24 == 5`; and every admitted return leaves finite non-sentinel coordinate pairs in `state+0x1e8`. This proves coordinate-output materialization only; it does not prove downstream image contribution, reducer closure, or final acceptance/rejection.
- Follow-up four-zoom state-5 coordinate-consumer proof now shows representative finite non-sentinel coordinate pairs emitted by `0x2457c0` into `state+0x1e8` are later read by `0xe8e70` vector-copy work under both State-helper copy-out paths (`0x224d70 -> 0x245a40` and `0x224e50 -> 0x245a20 -> 0x244560`) across clean canonical `28mm`, `35mm`, `70mm`, and `150mm` bridge HDR renders. This proves coordinate-vector custody / copy-out only; it does not prove image-effecting non-copy use of the copied destination, reducer closure, or final acceptance/rejection.
- Follow-up four-zoom coordinate-copy-destination proof now shows representative finite non-sentinel destination pairs copied out by the State-helper `0xe8e70` path are touched again by `0xe8e70` vector-copy work across clean canonical `28mm`, `35mm`, `70mm`, and `150mm` bridge HDR renders. Static/runtime evidence binds the admitted later caller frames to State-helper recopy sites plus higher node-vector materialization/copy sites at `0x22a61a -> 0xe8e70 -> 0x22a61f` and `0x22c93a -> 0xe8e70 -> 0x22c93f`. This proves coordinate-vector custody / propagation only; it does not prove image-effecting non-copy use of the propagated destination vectors, reducer closure, or final acceptance/rejection.
- Follow-up four-zoom coordinate-node-destination proof now shows representative finite non-sentinel destination pairs copied into the `0x22a61a -> 0xe8e70 -> 0x22a61f` node-vector destination reach non-copy candidate/index/scoring-selection code under `0x21b2e0` and its `0x21c4f0` callback path across clean canonical `28mm`, `35mm`, `70mm`, and `150mm` bridge HDR renders. The admitted capped window followed at least one finite node-destination pair per run, while the sibling node-vector copy site `0x22c93a -> 0x22c93f` had zero observed call/return hits in this proof. This proves representative non-copy candidate/index/scoring-selection consumption only; it does not prove image effect, reducer closure, or final acceptance/rejection.
- Follow-up four-zoom node-destination sentinel-custody proof now shows one finite non-sentinel coordinate pair copied into the `0x22a61a -> 0xe8e70 -> 0x22a61f` node-vector destination per focal tier is later the same runtime address rewritten through `0x21b923` / `0x21b92a` into full `(-1.0, -1.0)` at `0x21b930`, then sampled in downstream touches while still sentinel. This proves representative same-address custody from node-vector destination through sentinel invalidation and sampled downstream touches only; it does not prove all copied pairs, image effect, source contribution, reducer closure, or final acceptance/rejection.
- Follow-up four-zoom node-destination `0x20b5e0` branch-custody proof now shows one representative copied node-destination pair per focal tier reaches `0x20b912` at the same runtime address after sentinelization, still reads `(-1.0, -1.0)`, steps through `0x20b91d -> 0x20ba90` and `0x20baab -> 0x20bafd`, and does not visit the local update-write block at `0x20bac0..0x20bac8`. This proves same-address custody into one sampled local branch skip only; it does not prove all copied pairs, image effect, source contribution, reducer closure, or final acceptance/rejection.
- Follow-up four-zoom node-destination `0x20ca00` source-copy proof now shows one representative copied node-destination pair per focal tier is source-read at the same runtime address by `0xe0ae0` under caller return `0x20d309`, the second local vector copy inside `0x20ca00`, while still full `(-1.0, -1.0)`. This proves same-address custody into the `0x20ca00` source-copy surface only; it does not prove destination-slot identity, local gate selection, image effect, source contribution, reducer closure, or final acceptance/rejection.
- Follow-up four-zoom node-destination `0x20ca00` source/gate index proof now shows that, for one representative copied node-destination pair per focal tier, every captured `0x20d309` source/gate packet before the watchpoint cap has readable `source_index` and parent `gate_index` with `source_index != gate_index` (`117`, `117`, `106`, and `106` captured packets respectively). This proves capped local non-selection for one watched address per tier only; it does not prove destination-slot terminality, image effect, source contribution, reducer closure, or final acceptance/rejection.
- Follow-up selected-representative node-destination `0x20ca00` gate-custody proof now joins prior copied node-destination identity through sentinelization, the `0x20d309` source copy, computed destination-slot identity, and `0x20d363 -> 0x20d565` for one `28mm` pair (`source_index == gate_index == 5394`) and one `70mm` pair (`source_index == gate_index == 77`). Both copied destinations remain full `(-1.0, -1.0)` at the gate. The selected `35mm` row is cap-limited and the selected `150mm` row proves no source-copy hit for one watched address only. This proves representative local gate-skip custody, not all-pairs terminality, image effect, source contribution, reducer closure, or final acceptance/rejection.
- Follow-up selected cross-unit validation now observes the same full-sentinel `0x20d363 -> 0x20d565` mechanism on Unit-2 in one complete `35mm` twin-capture run at pair index `12`. Unit-2 `28mm` / `70mm` anchors and a targeted Unit-2 `35mm` repeat are cap-limited no-match windows. This proves cross-unit mechanism observation only; exact incidence, body causation, all-pairs terminality, image effect, source contribution, reducer closure, and final acceptance/rejection remain open.
- Follow-up byte-verified local-effect proof shows the admitted selected `0x20d363 -> 0x20d565` branches bypass keyed-node materialization, coordinate-to-double record writes, and imported `ceres::Problem::AddResidualBlock` at `0x20d560`. Those selected full-sentinel iterations add no residual through that local call. Post-loop `ceres::Solve` and other accepted pairs remain outside the skip, so shared-solve terminality for skipped pairs, broader solved-value distributions, image effect, source contribution, reducer closure, and final acceptance/rejection remain open.
- Follow-up vtable/typeinfo proof identifies `0x20ca00` as substantive slot `+0x30` of a `void(int,int,int)` lambda inside `lt::Triangulator::refine3dPoints()`, dispatched through generic executor `0x5670`. Historical method-entry labels are inexact. This proves callback identity only; by itself it does not close argument names, public output meaning, runtime values, image effect, source contribution, reducer closure, or final acceptance/rejection.
- Follow-up byte-verified solved-record custody proof traces the parent owner through callable `+0x08` into the callback, binds the post-solve triple to fields `+0x08/+0x0c/+0x10` of the same owner's `0x14`-stride record vector, and proves the parent immediately reduces positive `record+0x10` values into owner `+0x78/+0x7c`. This proves internal output ownership and one scalar range consumer only; by itself it does not prove public triple meaning, runtime values, later range consumers, image effect, source contribution, reducer closure, or final acceptance/rejection.
- Follow-up typeinfo/import/formula proof identifies `0x667240` as the one-parameter, two-residual `AutoDiffCostFunction<lt::Internal::ReProjectionCost,...>` wrapper, carries a unit-payload captured `CauchyLoss` into `AddResidualBlock`, and verifies the exact scalar-ray 3x4 reprojection residual formula. This proves internal ray-depth reprojection semantics and names the residual excluded by admitted sentinel skips; by itself it does not prove public units/LRI origin, runtime solved values, all-pairs terminality, image effect, source contribution, reducer closure, or final acceptance/rejection.
- Follow-up solve-only runtime proof for one complete Unit-1 `28mm` render captures 1,229 unique solve/write groups across ten returned callback frames. The bounded ray-depth scalar changes in 279 groups, remains bit-identical in 950, and every final selected `record+0x10` equals the float32 solved scalar; the second transform leaves each first transformed triple bit-identical under this run. This closes scoped runtime solved-value/materialization behavior only; stable cross-body/focal/render distributions, public calibration/LRI origins and names, all-candidate behavior, shared-solve terminality, image/source contribution, reducer closure, and final acceptance/rejection remain open.
- Follow-up static + reused-runtime proof pins helper `0x218940` and interprets the admitted Unit-1 `70mm` `record+0x10` downstream-watch packet: the watched finite positive z reaches `0x2189c4` 37 times with unchanged bits, and installed code skips only nonpositive/unordered z at `0x2189c8 -> 0x218aeb`. This proves representative local score-window admission into the record/transform body only; it is not direct branch-step proof, all-record behavior, public depth semantics, image/source contribution, reducer closure, or final acceptance/rejection.
- Follow-up caller proof binds those same 37 helper samples to `lt::SparseMirrorAngleOptimizer::optimize(...)::$_2` caller `0x219210`: every sample returns to `0x21937a`, and installed code immediately stores helper `xmm0` into `[r14+0x18][r15]` at `0x219381`. This proves representative caller output-vector custody only; stored values, later consumers, image/source contribution, reducer closure, and final acceptance/rejection remain open.
- Follow-up parent-consumer proof now runtime-binds that callback output-vector family: one complete Unit-1 `70mm` packet carries the exact same closure, 1,089-float return-vector header, and begin pointer through 64 sampled post-store `0x219210` callback hits and the matched `0x216f60` parent consumer at `0x217a68`; the parent min-like scan selects index `505` before helper side-output gates and selected 24-byte record materialization for `0xf33d0`. This proves one-render same-runtime callback-store to parent-consumer vector custody only; record-specific score proof, public acceptance/rejection semantics, image/source contribution, reducer closure, and final acceptance/rejection remain open.
- Follow-up canonical four-focal plus exact-focal Unit-2 `35mm` proof closes the captured local `0x216f60` parent gate after that vector scan. The parent selects the minimum callback score, applies selected-side `<= 0.25`, selected-side `<= center_side`, and optional float32 `selected_score <= 0.8 * center_score` gates, then materializes the selected 24-byte record and completes `0xf33d0` only for accepted winners. All 26 captured packets match reconstructed arithmetic to runtime flags; 12 accepted packets complete `0xf33d0` and 14 rejected packets do not call it. These run counts are not constants. Public vector/record names, downstream image/source contribution, distributed reducer closure, and final merge acceptance/rejection remain open.
- Follow-up canonical four-focal plus exact-focal Unit-2 `35mm` hardware-watch proof carries the first accepted selector-1 destination bank per run beyond `0xf33d0`: exact source bytes occupy destination `+0x12c..+0x17f`, and `0x264270` later reads the watched bank unchanged through direct `0xf34e0` bank-copy and `0xf3350` accessor-side paths before a later State-helper selector-1 overwrite. Selected `0x3f7ec0` materialization sites are zero-hit under these complete no-auto-LRIS runs. This proves accepted-bank-to-State/helper-record-assembly custody and a scoped route exclusion only; public record names, final image/source contribution, distributed reducer closure, and final merge acceptance/rejection remain open.
- Follow-up canonical four-zoom two-phase one-hit watch proof carries the exact `0x264270` assembly output into `0x23faf0`, which receives the same runtime address as `rdx` / `rbx` and copies it into `rdi` / `r12`, then carries that exact destination into its first later consumer. Wide routes pass through `0x239e00 -> 0x239ac0 -> State 0x22d250` and first read composer fields at `0x23a179` into local score-input state before the `0x23a200` loop. Tele routes pass through `0x20afb0 -> 0x20ada0 -> State 0x22ae60` and first read the composer destination at `0x20dbef` as scalar input to the static `0x20dbe0` three-row SIMD matrix composition. This proves assembly-output-to-composer-to-first-transform/score-state custody only; public record semantics, downstream image/source contribution, distributed reducer closure, and final merge acceptance/rejection remain open.
- Follow-up canonical four-zoom transform-materialization proof carries the composer destination through those calculations, first durable stores, and later watch boundaries. Wide `0x239e00` returns a mean 2D reprojection residual, whose exact bits are stored, read unchanged, and compared with keyed node `+0x28`. Corrected outcome-targeted proof establishes a local minimum selector: a lower candidate is materialized into the keyed record, while a higher candidate retains the lower existing `state+0x448` record and copies its three slices byte-exactly into the same-key per-camera `state+0xe0` selector-1 CalibStage bank; both effects align node/object key with public `CameraModule.id`. Exact post-transfer custody carries that selected bank through terminal composition into a byte-identical keyed BA camera-map copy; normalization changes that copy while preserving the source, then `0x23c0f0`, `0x2406a0`, and selector-1 `0xf33d0` convert/compose the exact same-key record and change the same camera object's CalibStage bank. One outcome-gated Unit-1 `35mm` transaction carries that key-`5` bank unchanged into terminal pass 2 and its exact `0x23cba6 -> 0x264440` read; exact-focal Unit-1 and Unit-2 `35mm` controls share the same two-pass keyed topology. Tele `0x20dbe0` produces a keyed `3x4` matrix whose tracked first-eight-byte prefix has no intervening touch before recursive cleanup. This proves internal transform/score materialization, a concrete wide calibration-record selection/BA-normalization/write-back/second-pass-helper-consumer boundary, and a scoped tele route exclusion only; complete public names, post-helper image/source effect, reducer closure, and final acceptance remain open.
- Follow-up post-terminal proof binds that path to the final `StereoAsyncAPI::ProcessingState` lambda (`$_7`). Complete exact-focal Unit-1 and Unit-2 `35mm` runs skip optional JPEG/overlay body `0x227b00`, replace owner `+0x28` with a different non-null calibration sibling through `0x226240 -> 0x239a90`, return target state `8`, and enter visible completion signalling/status work. An exact-slot read/write watch observes no later touch before owner destruction clears/releases the replacement, and complete constructor decoding excludes publication by `0x239a90 -> 0x2399a0`. Sequential canonical four-focal joins prove the terminal whole-State root is retained at `PipelineCache+0x180` and passed five times per render through `0x3f7040` to build the five `PipelineCache+0x258` records from `state+0xe0/+0x448`; the replacement `State+0x2a8` sibling is a different pointer and is not the record-builder argument. This proves the whole-State calibration-to-warp-record consequence and closes the proposed sibling feed, not whole-State public naming, reducer closure, or final acceptance.
- Follow-up tele node-destination scan/score identity proof now shows that one representative copied node-destination pair at `70mm` and one at `150mm` are later sentinelized at the same runtime address and then sampled at the same address inside the `0x216f60` scan/count window and at the `0x218bc4` score/materialization guard operand while still full `(-1.0, -1.0)`. This links tele node-destination identity to already-bounded local scan/count and score-guard surfaces only; it does not prove same-address branch-step, all copied pairs, image effect, source contribution, reducer closure, or final acceptance/rejection.
- Fresh same-address tele branch-custody proof advances those two representative pair identities through `0x218bc4 -> 0x218cb8`. Flags and direct stepping prove each full-sentinel pair takes the x-nonpositive skip; SHA-pinned static proof shows that skip bypasses pair-y loading, transform/score formation, local score-sum update, over-threshold-count update, and positive-pair-count update. This closes representative local score/count exclusion only; all-pairs/alias/alternate-route terminality, shared-solve terminality, image/source contribution, reducer closure, and final acceptance/rejection remain open.
- Follow-up four-zoom node sentinel-write proof now shows that the downstream `0x21b2e0` path executes coordinate-pair sentinel invalidation writes at `0x21b923` and `0x21b92a` across clean canonical `28mm`, `35mm`, `70mm`, and `150mm` bridge HDR renders. Runtime samples show finite non-sentinel coordinate pairs before the x-lane store and x already changed to `-1.0` before the y-lane store; static disassembly proves both stores write raw bits `0xbf800000` (`-1.0`). This proves coordinate invalidation/rejection writes only; it does not prove image effect, reducer closure, or final acceptance/rejection.
- Follow-up four-zoom node sentinel-downstream watch proof now shows that selected sentinel-marked node-vector coordinate pairs are touched later by downstream code across clean canonical `28mm`, `35mm`, `70mm`, and `150mm` bridge HDR renders. Watchpoints were armed only after the full pair read `(-1.0, -1.0)` immediately after `0x21b92a`, and every sampled later touch still observed `(-1.0, -1.0)`. Sampled downstream surfaces include State-family copy/record propagation plus coordinate scan/scoring/materialization windows. This proves downstream sentinel-coordinate custody / consumption only; it does not prove image effect, source contribution, reducer closure, or final acceptance/rejection.
- Follow-up State `0x22ae60` copy/record-surface proof now narrows that sampled State-family copy/record bucket: `0x20bd60` / `"point BA"` is keyed record materialization, `0x25e4b0` is the no-map `0x25e0c0` row-producer variant, `0x20dca0` is keyed record storage, `0x20ca00` is selected Ceres setup with positive-coordinate gates, and `0x239ac0` / `0x239e00` are keyed pair-vector propagation surfaces. Follow-up copied-slot gate proof shows one 70mm watched sentinel copied by the second `0x20ca00` local vector copy has `source_index == gate_index == 774`, is read at `0x20d363` as `(-1.0, -1.0)`, and skips to `0x20d565`; the admitted 28mm/35mm/150mm packets are capped no-match windows for watched sentinel pairs. This is surface classification plus one exact copied-slot gate-skip proof; it does not prove sentinel terminality, image effect, source contribution, reducer closure, or final acceptance/rejection.
- Follow-up static/runtime `0x216f60` scan-count proof now shows that sampled tele sentinel-coordinate stops inside the `0x216f60` window still read `(-1.0, -1.0)`, while the local vector/scalar paths count only coordinate pairs where both lanes are positive and require at least eight counted entries before continuing. This proves local non-counting behavior for sampled sentinel reads only; it does not prove whole-vector terminality, image effect, source contribution, reducer closure, or final acceptance/rejection.
- This matters because the exact behavior behind `src1` / `src2` is still unresolved before IRAMP sees the contributor vector.

### 2. Tele-tier odd-camera routing is still incomplete

- `CLM-C6-001`
- C6 fires in the tested `70mm` and `150mm` tele LRIs.
- C6 is absent from the directly observed five-item IRAMP contributor vector at `70mm` and `150mm`.
- The tested visible-`src1` keyed helper / vector-builder boundary at `0x1bdc80` / `0x1be750` / `0x1be270` is not a positive C6 route under complete canonical bridge HDR runs; tele keys observed there were `5..14`, with no key `15`.
- The tested visible-`src1` projection field-pack dispatcher boundary at `0x3f6170` / `0x3f6200` / `0x3f6940` is also not a positive C6 route under complete canonical bridge HDR runs; tele keys observed there were `8,10..14`, with no key `15`.
- The direct `0xe59a4 -> 0xf2770` constructor callsite constructs tele key `15` / C6 with item `+0x30 = 1`.
- Hardware write-watchpoint proof captures tele key `15` / C6 changing from item `+0x30 = 1` to `0` at writer `libcp+0x3c90a5` inside body `0x3c8f90` in both canonical tele seeds; static local gate shows this writer clears key `15` when the grouped context `+0x44` value is not group ordinal `2`.
- A focused 24-site direct `0xf2720` callsite census under complete `70mm` and `150mm` bridge HDR runs observes key `15` active at `0x1bdbab` / `0x1bdbdd` key-list helper sites and at mutation-body sites `0x3c9043` / `0x3c9098`, then inactive at later selected key-query sites `0x3b2143`, `0x402df7`, and `0x40d219`; follow-up identity proof ties the active observations, the `0x3c90a5` store, immediate inactive `0x3c90a9` state, and later inactive `0x3b2143` observation to the same tracked item pointer per tele run. A remaining-direct 34-site census covers the rest of the static direct `call 0xf2720` inventory: it observes active key `15` at constructor-adjacent key/container/tree materialization sites `0xe327e`, `0xe32f3`, `0xe4063`, `0xe5fd9`, and `0xe6020`, and inactive key `15` at shared-object lookup site `0xe6be0`. Together, the focused and remaining-direct proofs cover all 58 static direct `call 0xf2720` sites under the canonical tele bridge HDR path. This is key-query/helper census proof, not image contribution, terminality, or non-`0xf2720` route closure.
- A post-mutation active-byte read/write watchpoint on the same tracked key-15 `item+0x30` byte arms at immediate inactive state `0x3c90a9`; complete canonical `70mm` and `150mm` bridge HDR renders both record 18 later watchpoint stops, all with `item+0x30 = 0`, `item+0x60 = 15`, `item+0x58/+0x5c = (-1,-1)`, and `item+0x100 = 3`. The stopped libcp VAs include active-byte gates outside the direct `0xf2720` inventory, and the final non-libcp stop is allocator cleanup after output write. This narrows same-byte reactivation and later active-byte consumers, not whole-object terminality, other fields, aliases, or final image effect.
- A selected-field post-mutation watchpoint on the same tracked item arms at immediate inactive state `0x3c90a9` and watches `item+0x30`, `item+0x58..0x5f`, `item+0x60..0x67`, and `item+0x100..0x107`; complete canonical `70mm` and `150mm` bridge HDR renders both complete and write output. Pre-output libcp samples preserve active byte `0`, pair `(-1,-1)`, key `15`, and type `3`; the watched `+0x60..+0x67` range is read at `0xf2727` (`+0x60`) and `0xf3327` (`+0x64`); watched pair/type ranges record only allocator-cleanup stops after output write. This narrows selected fields, not untested fields, aliases, final effect of the watched `+0x60..+0x67` reads, or final image effect.
- A post-mutation state-consumer probe under complete `70mm` and `150mm` bridge HDR runs proves the immediate caller path after `0x3c8f90` consumes the constructed `ctx+0xa0` object, walks the eleven-entry item vector, observes key `15` inactive at `0x3b2143`, writes state fields `+0x0 = 3` and `+0x4 = 1` into a state object stored at context `+0xc8`, and queues context `+0x4b0 = 5`. This proves state-classification consumption, not image contribution or route closure.
- A downstream rect-vector probe under complete `70mm` and `150mm` bridge HDR runs proves the immediate caller segment rereads context `+0xc8` as state fields `+0x0 = 3` and `+0x4 = 1`, receives `0` from `0x40b0e0`, takes fallback branch `0x3c8c00`, rereads context `+0x4b0 = 5`, passes that value as `r8d` into `0x3c8d00`, and returns a five-entry vector of 16-byte integer tuples.
- A four-zoom follow-up proves that returned rect-vector route is consumed by the immediate caller: five `context+0x4c0` delta-dimension pairs are derived from the tuples, passed to `0x3982b0`, and used to build a five-level `CIAPI::ImagePyramid` stored at `context+0x538`. This proves vector-consumer identity as ImagePyramid construction, not final C6 image contribution or route closure.
- A follow-up four-zoom probe proves the caller immediately iterates those five ImagePyramid levels, builds a full-image stack descriptor for each, and calls direct zero-fill site `0x3b2f54 -> 0xf7c0` once per level with bytes-per-pixel argument `4`; all twenty runtime descriptors satisfy `stride_pixels == width`, and the first 32 bytes sampled after return are zero for all twenty descriptors. This proves an immediate zero-fill consumer of the ImagePyramid route, not final C6 image contribution or route closure.
- A downstream-candidate liveness probe under complete canonical bridge HDR renders at `28mm`, `35mm`, `70mm`, and `150mm` re-hits `0x3b2abd` once and `0x3b2f59` five times, but records zero hits at the selected later static `context+0x538` candidate families. This excludes only those candidate families under the tested conditions.
- A representative hardware data-watch probe under complete canonical bridge HDR renders arms one read/write watchpoint per run after `0x3b2f59`; the watched first 8 bytes of the selected level buffer have zero hits at `28mm` level `0`, `35mm` level `2`, `70mm` level `2`, and `150mm` level `1`. This excludes only those watched byte ranges.
- An expanded tele hardware data-watch grid under complete canonical bridge HDR renders arms first/middle/last 8-byte read/write watchpoints across all five zero-filled ImagePyramid levels at `70mm` and `150mm`; all 30 admitted grid cells complete cleanly and record zero later watchpoint hits. This excludes only those watched tele byte ranges.
- The direct payload candidate loop immediately upstream of the dispatcher sees key `15` at both tele tiers, but runtime proof shows post-mutation `object+0x30 = 0`; key `15` skips before class compare and before the `0x3e05f5 -> 0x3f6170` dispatcher call.
- The stereo-side keyed-record loop inside `0x3f2c40` also sees key `15` at both tele tiers, but runtime proof shows post-mutation `object+0x30 = 0`; key `15` skips before both tested `0xf2720` getter callsites.
- Later write/read behavior outside the watched representative/tele-grid byte ranges or through unprobed helpers/data-pointer aliases, final image/merge effect of the zero-filled ImagePyramid/geometry route, untested C6 fields or object aliases after the watched active byte remains `0`, final effect of the watched `+0x60..+0x67` reads, terminality of the mutation, and alternate image-effecting routing outside the tested sites remain unresolved.

### 3. Closed: 150mm tele firing topology

- `CLM-ZOOM-002`
- Exact-focal public headers on both calibration bodies and completed
  canonical Unit-1 tele constructor runs prove `B1..B5 + C1..C6` at
  `150mm`; `70mm` verifies the same tele-tier mechanism.
- This initial firing set does not override the separately proven later C6
  clear/exclusion.

### 4. Missing-CCM cameras are real, but their exact routing is still incomplete

- `CLM-CCM-001`
- A2 and C6 are missing from Block 6 CCM coverage and the dispatcher filters missing-CCM cameras on the tested paths.
- That is safe as a negative fact.
- It is not yet enough to close where those cameras ultimately contribute downstream.

## Zoom Coverage Matrix

This table is the quick gate for whether parity work may safely generalize a merge-critical claim.

| Topic | 28mm | 35mm | 70mm | 150mm | Use In Spec? |
|---|---|---|---|---|---|
| IRAMP weighted accumulator exists | `VERIFIED` | `VERIFIED` | `VERIFIED` | `VERIFIED` | Yes |
| IRAMP tested signature = 2 anchor IGs + 5 contributor IGs | `VERIFIED` | `VERIFIED` | `VERIFIED` | `VERIFIED` | Yes |
| direct IRAMP contributor identity | `B1..B5` | `B1..B5` | `C1..C5` | `C1..C5` | Yes, direct vector only |
| visible `src1` payload constructor path and level vector | `VERIFIED` | `VERIFIED` | `VERIFIED` | `VERIFIED` | Structural only |
| visible `src1` payload family vs direct contributor payload family | `VERIFIED` | `VERIFIED` | `VERIFIED` | `VERIFIED` | Structural only |
| visible `src1` secondary callable vs direct contributor secondary callable | `VERIFIED` | `VERIFIED` | `VERIFIED` | `VERIFIED` | Structural only |
| visible `src1` secondary callable live handoff to `0x3e2e90` | `VERIFIED` | `VERIFIED` | `VERIFIED` | `VERIFIED` | First captured tile only |
| visible `src1` worker/projection-record handoff to `0x3e4c50` / `0x3e42e0` | `VERIFIED` | `VERIFIED` | `VERIFIED` | `VERIFIED` | First captured worker packet only |
| `0x3e42e0` projection callable coordinate-transform formula | `VERIFIED_SAME_MECHANISM` | `VERIFIED_SAME_MECHANISM` | `VERIFIED_SAME_MECHANISM` | `VERIFIED_SAME_MECHANISM` | Structural/math only |
| `0x3e42e0` projection field-pack producer | `VERIFIED_SAME_MECHANISM` | `VERIFIED_SAME_MECHANISM` | `VERIFIED_SAME_MECHANISM` | `VERIFIED_SAME_MECHANISM` | Structural/source-topology only |
| `0x3f6170` projection field dispatcher key boundary | keys `0,5..9`; no C6 scope | keys `0,5..9`; no C6 scope | keys `8,10..14`; no `15` | keys `8,10..14`; no `15` | Complete bridge HDR runs, `.lris` auto-load disabled |
| `0xe59a4 -> 0xf2770` item constructor callsite | keys `0,4,6,8,9,1,2,3,5,7`; all item `+0x30 = 1` | keys `0,4,6,8,9,1,2,3,5,7`; all item `+0x30 = 1` | keys `6,8,9,14,5,7,11,10,12,13,15`; all item `+0x30 = 1` | keys `6,8,9,14,5,7,11,10,12,13,15`; all item `+0x30 = 1` | Complete bridge HDR runs; constructor-state proof |
| C6 active-byte mutation watchpoint | `N/A` | `N/A` | key `15` changes `+0x30` from `1` to `0` at `0x3c90a5` | key `15` changes `+0x30` from `1` to `0` at `0x3c90a5` | Watchpoint stops at mutation; not output-completion evidence |
| direct C6 `0xf2720` route census + mutation identity | `N/A` | `N/A` | all 58 direct static callsites runtime-censused; active key-list helper hits at `0x1bdbab` / `0x1bdbdd`; active constructor-adjacent helper hits at `0xe327e`, `0xe32f3`, `0xe4063`, `0xe5fd9`, `0xe6020`; same tracked focused item cleared at `0x3c90a5` and later inactive | all 58 direct static callsites runtime-censused; active key-list helper hits at `0x1bdbab` / `0x1bdbdd`; active constructor-adjacent helper hits at `0xe327e`, `0xe32f3`, `0xe4063`, `0xe5fd9`, `0xe6020`; same tracked focused item cleared at `0x3c90a5` and later inactive | Direct `0xf2720` census only; helper bodies are key/container/tree/materialization surfaces, not image contribution, non-`0xf2720` closure, or terminality |
| post-mutation C6 active-byte read/write watch | `N/A` | `N/A` | 18 later stops; all watched `item+0x30 = 0`; active-byte gates include non-direct-`0xf2720` surfaces; output completes | 18 later stops; all watched `item+0x30 = 0`; active-byte gates include non-direct-`0xf2720` surfaces; output completes | Same-byte custody only; not whole-object terminality, other-field/alias absence, or final image effect |
| post-mutation C6 selected-field read/write watch | `N/A` | `N/A` | watched `+0x60..+0x67` pre-output reads at `0xf2727` (`+0x60`) and `0xf3327` (`+0x64`); watched pair/type ranges cleanup-only; output completes | watched `+0x60..+0x67` pre-output reads at `0xf2727` (`+0x60`) and `0xf3327` (`+0x64`); watched pair/type ranges cleanup-only; output completes | Selected-field custody only; not untested-field/alias absence, terminality, or final image effect |
| post-mutation C6 state consumer | `N/A` | `N/A` | `ctx+0xa0` walk hits; key `15` inactive; context `+0xc8` / `+0x4b0` state written | `ctx+0xa0` walk hits; key `15` inactive; context `+0xc8` / `+0x4b0` state written | State-classification proof only; not final image-effect proof |
| post-mutation C6 downstream rect-vector path | `N/A` | `N/A` | `context+0xc8` reread; `0x40b0e0 = 0`; fallback `0x3c8c00`; `context+0x4b0 = 5`; `0x3c8d00` returns five tuples | `context+0xc8` reread; `0x40b0e0 = 0`; fallback `0x3c8c00`; `context+0x4b0 = 5`; `0x3c8d00` returns five tuples | Vector-builder proof for tele path; consumer covered in next row |
| rect-vector consumer / ImagePyramid construction route | `VERIFIED`; five levels from `(10432,7824)..(652,489)` | `VERIFIED`; five levels from `(8320,6240)..(520,390)` | `VERIFIED`; five levels from `(8832,6624)..(552,414)` | `VERIFIED`; five levels from `(4160,3120)..(260,195)` | Structural ImagePyramid construction only; final C6 image effect unresolved |
| ImagePyramid immediate zero-fill route | `VERIFIED`; five descriptors satisfy `stride_pixels == width`; after-return first-32-byte samples zero | `VERIFIED`; five descriptors satisfy `stride_pixels == width`; after-return first-32-byte samples zero | `VERIFIED`; five descriptors satisfy `stride_pixels == width`; after-return first-32-byte samples zero | `VERIFIED`; five descriptors satisfy `stride_pixels == width`; after-return first-32-byte samples zero | Immediate zero-fill consumer only; later writer/read behavior and final C6 image effect unresolved |
| selected later `context+0x538` candidate families | zero hits under complete bridge HDR render | zero hits under complete bridge HDR render | zero hits under complete bridge HDR render | zero hits under complete bridge HDR render | Scoped negative for selected candidate VAs only; not terminality |
| tele ImagePyramid data-watch grid after zero-fill | `N/A` | `N/A` | first/middle/last 8-byte ranges across all five levels: zero later watch hits | first/middle/last 8-byte ranges across all five levels: zero later watch hits | Scoped negative for watched tele byte ranges only; not whole-buffer terminality |
| candidate C6 context consumer `0x3c9540 -> 0xe6c30` | `N/A` | `N/A` | zero hits; constructor/mutation custody sites hit | zero hits; constructor/mutation custody sites hit | Scoped negative for this candidate route only |
| direct payload candidate gate before `0x3e05f5 -> 0x3f6170` | keys `0..9`; all `object+0x30 = 1`; dispatcher keys `5..9` | keys `0..9`; all `object+0x30 = 1`; dispatcher keys `5..9` | keys `5..15`; key `15` has post-mutation `object+0x30 = 0`; dispatcher keys `10..14` | keys `5..15`; key `15` has post-mutation `object+0x30 = 0`; dispatcher keys `10..14` | Tested C6 filter point only; not global C6 non-use |
| stereo-side keyed-record gate inside `0x3f2c40` | keys `0..9`; all `object+0x30 = 1`; getter keys `0..9` | keys `0..9`; all `object+0x30 = 1`; getter keys `0..9` | keys `5..15`; key `15` has post-mutation `object+0x30 = 0`; getter keys `5..14` | keys `5..15`; key `15` has post-mutation `object+0x30 = 0`; getter keys `5..14` | Tested C6 filter point only; not global C6 non-use |
| visible `src1` source-image producer topology beneath `0x3e2e90` | `VERIFIED_SAME_MECHANISM` | `VERIFIED_SAME_MECHANISM` | `VERIFIED_SAME_MECHANISM` | `VERIFIED_SAME_MECHANISM` | Structural/dispatch only |
| visible `src1` keyed helper / vector-builder boundary | `0x1bdc80 live; 0x1be750/0x1be270 zero-hit` | `0x1bdc80 live; 0x1be750/0x1be270 zero-hit` | `0x1bdc80 live; 0x1be750/0x1be270 zero-hit; keys 5..14 no 15` | `0x1bdc80 live; 0x1be750/0x1be270 zero-hit; keys 5..14 no 15` | Complete bridge HDR runs, `.lris` auto-load disabled |
| visible `src1` source-producer first per-source virtual target `0x65b3c8/+0x30 = 0x341770` | `VERIFIED` | `VERIFIED` | `VERIFIED` | `VERIFIED` | First captured gated descendant; record-update only |
| visible `src1` gated lower virtual census: `0x33f3e8` / `0x33f94f` | `>=512 capped hits each` | `>=512 capped hits each` | `>=512 capped hits each` | `>=512 capped hits each` | Capped lower-bound dispatch only |
| visible `src1` gated lower virtual census: `0x33ffd4` | `>=512 capped hits` | `>=512 capped hits` | `0 hits under tested gated run` | `0 hits under tested gated run` | Scoped runtime census only |
| visible `src1` capped target-family visible-body classification plus bound `0x342ca0` / `0x344470` indirect targets | `STATIC/RUNTIME_BOUND` | `STATIC/RUNTIME_BOUND` | `STATIC/RUNTIME_BOUND` | `STATIC/RUNTIME_BOUND` | Prep/materialization and helper-dispatch exclusion only; not reducer closure |
| `0x341770` helper `0x2e8680` / callback `0x2e8cc0` | `STATIC_BOUND` | `STATIC_BOUND` | `STATIC_BOUND` | `STATIC_BOUND` | One-source Bayer/RAW region helper only |
| `0xf540` is alloc/setup, not pair writer | `VERIFIED_SAME_MECHANISM` | `VERIFIED_SAME_MECHANISM` | `VERIFIED_SAME_MECHANISM` | `VERIFIED_SAME_MECHANISM` | Yes |
| int32 pair-grid writer at `0x366520..0x366523` | `VERIFIED_SAME_MECHANISM` | `VERIFIED_SAME_MECHANISM` | `VERIFIED_SAME_MECHANISM` | `VERIFIED_SAME_MECHANISM` | Yes |
| ROI-derived first pair grid -> transformed second pair grid | `VERIFIED_SAME_MECHANISM` | `VERIFIED_SAME_MECHANISM` | `VERIFIED_SAME_MECHANISM` | `VERIFIED_SAME_MECHANISM` | Yes, structural only |
| `FusionCacheBayer` is not the bridge merge entry | `VERIFIED` | `OPEN` | `PARTIAL` | `OPEN` | Negative-only, scope-bound |
| Wide/tele firing regimes | `VERIFIED` | `VERIFIED` | `VERIFIED` | `VERIFIED` | Yes |
| Tiered focal reference and internal crop | `VERIFIED` | `VERIFIED` | `VERIFIED` | `VERIFIED` | Yes |
| 35mm bridge crop-plus-upsample | `VERIFIED` | `VERIFIED` | `OPEN` | `OPEN` | Yes, bridge-path only |
| Missing-CCM coverage / dispatcher filter | `VERIFIED` | `OPEN` | `VERIFIED` | `OPEN` | Negative-only, routing caveat |
| owner `+0xf0` expansion handoff at `0x3d502e` | `VERIFIED` | `VERIFIED` | `VERIFIED` | `VERIFIED` | Structural only; next route covered separately |
| owner `+0xf0` expansion destination context | `VERIFIED` | `VERIFIED` | `VERIFIED` | `VERIFIED` | Structural only; first selected-cache route covered separately |
| first owner `+0xf0` read/rescale route to `0x36f800` | `VERIFIED` | `VERIFIED` | `VERIFIED` | `VERIFIED` | First captured route only; first weighted store covered separately |
| first-owner `+0xf0` branch census | `0x3d4842 + 0x3d4864` | `0x3d4842 only` | `0x3d4842 + 0x3d4864` | `0x3d4842 + 0x3d4864` | First captured owner only; all accepted packets still return to `0x3d084d` |
| first direct-branch post-route to `0x36f800` | `VERIFIED` | `NO_DIRECT_BRANCH_OBSERVED` | `VERIFIED` | `VERIFIED` | First owner-matching direct branch only |
| global read-context branch-site caller/slot census | `3 callers / 2 slots` | `3 callers / 2 slots` | `3 callers / 2 slots` | `3 callers / 2 slots` | Complete render census at `0x3d4842` / `0x3d4864`; post-route classification covered separately |
| global read-context post-route family classification | `3 families` | `3 families` | `3 families` | `3 families` | Exact-size cleanup, owner-cache `0x36f800`, visible-`src1` `0x3edb80`; final policy still open |
| first owner `+0xf0` `0x36f800` weighted store | `VERIFIED` | `VERIFIED` | `VERIFIED` | `VERIFIED` | First captured store only; helper store covered separately |
| first owner `+0xf0` `0x36f800` helper row-cache / row-plan coverage | `VERIFIED` | `VERIFIED` | `VERIFIED` | `VERIFIED` | Captured helper formula plus first-dispatch row-plan coverage; full-render leading/trailing live at `28mm`/`70mm`, zero-hit at `35mm`/`150mm`; final policy open |
| exact `src1` / `src2` pre-fusion reducer | `OPEN` | `OPEN` | `OPEN` | `OPEN` | No |
| exact C6 routing | `N/A` | `N/A` | `PARTIAL`; all direct `0xf2720` callsites runtime-censused; active key-query/helper surfaces proven; same active byte remains `0` at 18 later watched stops; selected-field watch proves pre-output `+0x60/+0x64` reads and cleanup-only watched pair/type ranges; post-mutation state consumer, downstream rect-vector builder, ImagePyramid consumer, immediate zero-fill route, selected downstream-candidate zero-hit exclusions, representative data-watch zero-hit ranges, and expanded tele-grid zero-hit ranges proven | `PARTIAL`; all direct `0xf2720` callsites runtime-censused; active key-query/helper surfaces proven; same active byte remains `0` at 18 later watched stops; selected-field watch proves pre-output `+0x60/+0x64` reads and cleanup-only watched pair/type ranges; post-mutation state consumer, downstream rect-vector builder, ImagePyramid consumer, immediate zero-fill route, selected downstream-candidate zero-hit exclusions, representative data-watch zero-hit ranges, and expanded tele-grid zero-hit ranges proven | No; final image/merge effect, untested fields/aliases, alternate image-effecting routes, unprobed aliases/consumers, final effect of watched `+0x60..+0x67` reads, and terminality still unresolved |

## Current Rule For Implementation

Safe to code now:

- proven helper behavior
- proven accumulator math
- proven direct IRAMP contributor-vector identity
- proven negative architecture exclusions
- archive-verified wide/tele firing topology
- tiered crop/framing behavior
- proven 35mm bridge framing behavior

Not safe to freeze into a parity renderer yet:

- exact anchor pre-fusion reducer
- remaining producer-side row/map calibration semantics over the ROI-derived dst-coordinate lattice
- exact full merge topology
- tele odd-camera routing
- any merge-critical assumption generalized to 35mm or 150mm without proof
